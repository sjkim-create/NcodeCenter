# PRJ-04 · 교재(책) 등록·수정

**화면 ID**: PRJ-04

> 원본 — 화면 정의서 `docs/screens/` · PRD `docs/prd/PRJ-04_교재(책) 등록·수정.md`
> 화면 구성·상태는 화면 정의서에서, 기능 개요·흐름·정책·사용법·연결 화면은 PRD 에서 **자동으로 모아** 만든다. 이 파일을 직접 고치지 말 것.

---

## 2. 화면 구성

### 개요

교재(책) 1권의 **편집 내역과 정산 근거를 입력**하는 화면. **등록과 수정이 같은 화면**이며 진입 방법으로 모드가 갈린다 — `PRJ-03`의 [＋ 교재(책) 추가] = **교재(책) 추가**, 교재 행 클릭 = **교재(책) 편집 수정**. 여기서 입력한 **페이지 수와 심볼·기능 수량이 곧 청구액**이 되므로 편집팀 작업 기록이자 정산 입력 화면이다. 단가 3단 구조 — 기본 단가 → 고객사 단가(`MEM-02`) → **이 화면의 할인**.

### 기능 개요

교재(책) 1권의 **편집 내역과 정산 근거를 입력**하는 화면. **등록과 수정이 같은 화면**이며 진입 방법으로 모드가 갈린다.

| 진입 | 모드 | 주소 | 화면 제목 |
|---|---|---|---|
| `PRJ-03`의 **[＋ 교재(책) 추가]** | 등록 | `/projects/editing/{owner}/book/new?c={고객사}` | 교재(책) 추가 |
| `PRJ-03`의 **교재 행 클릭** | 수정 | `/projects/editing/{owner}/book/{idx}?c={고객사}` | 교재(책) 편집 수정 |

> **주소의 `?c={고객사}`** `PC-045` — **owner 번호는 여러 고객사가 함께 쓸 수 있다**(공유·커먼 코드, 같은 번호를 쓰는 별개 고객사). owner 만으로 찾으면 **다른 고객사의 교재**가 열리므로 고객사명을 함께 넘긴다. 기본 좌표(S/O)와 교재 목록·저장 위치가 모두 이 고객사 기준으로 정해진다.

> **모달이 아니라 별도 페이지**다. 이 화면에서는 편집 상세의 **목록·요약·필터를 감추고** 이 폼만 보여준다. 다만 **고객사 머리말은 유지**되어 어느 고객사의 교재인지 맥락이 남는다.

여기서 입력한 **페이지 수와 심볼·기능 수량이 곧 청구액**이 되므로, 편집팀 작업 기록이자 정산 입력 화면이다.

| 항목 | 내용 |
|---|---|
| 권한 | STAFF / ADMIN |
| 대상 단위 | 교재(책) 1권 = S/O/B 좌표 1개 |

### 화면 흐름 (Flow)

```
PRJ-03
  ├─ [＋ 교재(책) 추가] ─▶ PRJ-04 (등록)
  └─ 교재 행 클릭 ───────▶ PRJ-04 (수정)
                              ├─ 진행 상태 변경(완료 ⇄ 진행중)
                              ├─ [교재 삭제] ─ 확인 ─▶ PRJ-03 목록에서 제거
                              ├─ [목록] ────────────▶ PRJ-03 (입력값 저장 안 함)
                              └─ [저장] ────────────▶ PRJ-03 (목록·집계·청구액 갱신)
                                        └─▶ LOG-01 (등록=교재 추가 / 수정=교재 작업)
                                        └─▶ 공유 코드면 SOB-01 지도에 사용 고객사·편집 상태 반영
```

### 정책

| 정책 | 이 화면에서의 적용 |
|---|---|
| `P-16` 정산 = 항목별 단가표 | 심볼·기능을 **항목별 수량**으로 입력하고, 각 항목 단가와 곱해 합산한다 |
| 단가 3단 구조 | 기본 단가 → **고객사 단가(`MEM-02`)** → **이 화면의 할인**(할인율·추가 할인액) |
| `P-12` 공통(커먼) 코드 | 공유 코드 Owner의 교재는 **사용 고객사 입력이 필수** |
| `P-01` SOBP | Book은 **사용 가능한 번호**에서만 고른다. 수정 모드에서는 S/O를 바꿀 수 없다 |
| `P-14` 편집 = casterN | 저장하면 그 Book은 편집 상태가 되어 `SOB-01` 지도에 반영된다 |
| 완료 잠금 규칙 | **완료 처리하려면 ncp2 최종수정 날짜가 필요**하고, 완료 상태에서는 내용이 잠긴다 |

### 사용법

#### 4.1 진행 상태

| 상태 | 의미 |
|---|---|
| 진행중 | 편집 작업 중 (기본값) |
| 완료 | 작업 종료 — **내용 잠금** |
| 보류 | 중단·대기 |

- **완료로 바꾸려면 ncp2 최종수정 날짜가 입력돼 있어야 한다.**
- 완료를 해제하면 그때의 ncp2 최종수정 날짜가 **이력으로 보관**되고(해제 시각·해제자 포함), 화면에서 확인할 수 있다.

#### 4.2 코드 좌표 · 사용 고객사

| 항목 | 필수 | 규칙 |
|---|---|---|
| 할당된 S / O | ✔ | **등록 모드**에서 할당된 S/O가 여러 개면 선택. **수정 모드에서는 변경 불가** |
| Book | ✔ | **사용 가능한 번호**에서 선택 |
| 사용 고객사 | 공유 코드일 때 ✔ | 공유(커먼) 코드 Owner인 경우에만 표시·필수. 후보는 **그 커먼 코드를 체크한 고객사**만 나온다 |

묶음 색과 안내문이 코드 성격에 따라 갈린다. (S1 · S6)

| 코드 | 배지 | 안내 |
|---|---|---|
| 전용 | **전용 코드 · {고객사}** | *이 S/O는 {고객사} 전용입니다. 별도 고객사 입력이 필요 없습니다.* |
| 공유 | **공유 코드 · 여러 고객사 사용** | *여러 고객사가 Book 번호만 나눠 쓰는 코드입니다 — {커먼코드} 를 체크한 고객사만 후보로 나옵니다.* |
| 공유·후보 없음 | — | *후보 없음 — 고객사 관리에서 {커먼코드} 를 체크하세요.* |

#### 4.3 교재 정보

| 항목 | 필수 | 규칙 |
|---|---|---|
| 교재명 | — | 목록·검색에 표시 |
| ncp2 파일명 | — | |
| ncp2 파일 크기(byte) | — | 서버 저장 용량 파악용 |
| 타입 | — | 소리펜 / 필기펜 등 |
| Start Page | — | 0 이상 |
| Total Page | — | **적용비 계산 기준(페이지 수)** |
| 발급일자(Ncode 발급일) | — | |
| ncp2 최종수정 | 완료 시 ✔ | **완료 처리의 선행 조건** |
| 발급인 | — | 코드 할당자·사용자 명단에서 선택 |
| 펜 모델 | — | 여러 개 선택 가능(추가/삭제) |
| 편집방식 | — | 여러 개 선택 가능(추가/삭제) |

#### 4.4 심볼·기능 수량 (정산 근거)

**소리펜과 필기펜 입력 칸이 좌·우로 항상 함께** 표시된다. 타입이 무엇이든 두 블록이 다 나오며, 쓰지 않는 쪽은 0으로 둔다. 각 항목은 **수량 × 단가**로 계산된다. (S1 · S8)

| 블록 | 칸 수 | 입력 항목 |
|---|---|---|
| **소리펜 심볼 입력** (좌·넓음) | **14** | Ncode 편집(기본) · Compound 2·3·4·5·6·7·8언어 · 슬롯전환 · 그룹재생 · 게임 · 프롬프트 편집 · RAG 데이터 업로드 · 4도 Ncode 출력 |
| **필기펜 심볼 입력** (우) | **5** | none 편집비용 · Custom · action 변경 편집 · 노트서버 업로드 · 교원구몬/KEP |

전사 기본 단가(원)

| 소리펜 | 단가 | 필기펜 | 단가 |
|---|---|---|---|
| Ncode 적용 (페이지) | 500 | Ncode 적용 (페이지) | 500 |
| Ncode 편집(기본) | 1,000 | none 편집비용 | 1,000 |
| Compound 2 / 3 / 4언어 | 1,300 / 1,600 / 1,900 | Custom | 1,000 ⚠ |
| Compound 5 / 6 / 7 / 8언어 | 2,200 / 2,500 / 2,800 / 3,100 | action 변경 편집 | 1,500 |
| 슬롯전환 / 그룹재생 | 3,000 / 5,000 | 노트서버 업로드 | 10,000 |
| 게임 / 프롬프트 / RAG | 각 50,000 | 교원구몬/KEP | 1,000 ⚠ |
| 4도 Ncode 출력 (페이지) | 1,000 | | |

> ⚠ **Custom · 교원구몬/KEP** 단가는 미확정(`PC-018` 확인 대기).
> 페이지 단위 **Ncode 적용**은 Total Page로 자동 계산되므로 별도 수량을 입력하지 않는다.
> 단가 값의 정본은 정책(`P-16`)이며, 고객사별 단가는 `MEM-02`에서 지정한다.

#### 4.5 합산 줄

심볼 입력 아래에 파란 줄로 **이 교재의 편집량과 금액**이 즉시 표시된다. (S1)

> TOTAL PAGE **{n}** p ｜ 소리펜 합 **{n}** ｜ 필기펜 합 **{n}** ｜ 심볼 합계 **{n}**
> 적용비 **{금액}** ({n}p × {단가}) · 편집·기능비 **{금액}** (항목별 단가 합) · 청구액 **{금액}**

#### 4.6 정산 (교재 단위 조정)

**적용 단가** — 이 교재에 적용된 단가가 배지로 표시된다.

| 표시 | 의미 |
|---|---|
| **등록 시 단가 ({날짜})** | 저장 시점의 고객사 단가로 **고정**된 상태. 이후 고객사 단가가 바뀌어도 이 교재는 그대로 |
| **고객사 단가 / 기본 단가** | 아직 고정 전(신규 교재 또는 단가 이력이 없는 구 교재). **저장하면 현재 단가로 고정**된다 |
| **⚠ 고객사 단가 변경됨 — 현재 단가로 갱신** | 고정된 단가와 현재 고객사 단가가 다를 때만 표시. 누르면 확인 후 **이 교재만** 현재 단가로 다시 계산한다 |

| 항목 | 규칙 |
|---|---|
| 할인율(%) | 합계(할인 전) 기준 |
| 추가 할인액(₩) | 정액 차감 |
| 할인 사유 | 예) 재작업분 · 물량 협의 |
| [할인 초기화] | 할인 없음으로 되돌린다 |

산출 근거가 **적용비(페이지) → 편집·기능비 → 합계(할인 전) → 할인율 → 추가 할인액 → 청구액** 순으로 표시된다.

#### 4.7 부가 정보 · 업무 메모

**교원구몬/KEP · 산출물** 묶음은 **접혀 있고** 제목을 눌러 펼친다. KEP 관련 값이 있거나 **등록 모드일 때만** 나타난다.

| 항목 | 내용 |
|---|---|
| 세트 개수 | |
| 세부내역 · 출력용파일 · APP 데이터 | 링크(URL)와 설명을 함께 등록. 새 탭으로 열 수 있다 |

**업무요청 메모** — 종류(요청 / 처리 / 메모)를 고르고 내용을 입력한다. **Enter 기록 · Shift+Enter 줄바꿈**. (S10)

| 요소 | 동작 |
|---|---|
| 기록 목록 | 번호 · 종류 배지 · 작성일시(수정하면 **(수정됨)**) · 작성자(본인이면 **(나)**) |
| 본인 글 | 클릭하면 위 입력칸에서 수정 — 버튼이 **[수정 저장] · [취소]** 로 바뀐다. **[삭제]** 가능 |
| 타인 글 | **🔒 타인 글** 로 표시되고 수정·삭제할 수 없다 |
| 기록 없음 | **기록된 업무 메모가 없습니다.** |

#### 4.8 교재 삭제

**[교재 삭제]** — 확인 후 해당 교재 행이 `PRJ-03` 목록에서 제거된다. **수정 모드이면서 완료로 잠기지 않았을 때만** 버튼이 나온다. (S9)

#### 4.9 하단 액션

| 버튼 | 조건 | 동작 |
|---|---|---|
| **[교재 삭제]** | 수정 모드 · 완료 잠금이 아닐 때만 | 확인 후 목록에서 제거하고 `PRJ-03` 으로 |
| **[목록]** | 공통 | `PRJ-03` 교재 목록으로. **입력값은 저장되지 않는다** |
| **[추가]** | 등록 모드 | 저장 후 목록으로 이동 |
| **[저장]** | 수정 모드 | 저장 후 목록으로 이동 |

### 연결 화면

| 트리거 | 이동 | 설명 |
|---|---|---|
| [저장] · [취소] · [교재 삭제] | `PRJ-03` | 목록·집계·청구액 갱신 후 복귀 |
| 저장(자동) | `LOG-01` | 등록 = **교재 추가**, 수정 = **교재 작업** |
| 저장(자동, 공유 코드) | `SOB-01` | 지도에 사용 고객사·편집 상태 반영 |
| 단가를 바꿔야 할 때 | `MEM-01` → `MEM-02` | 고객사 단가 지정 |
| 사용 가능한 Book이 없을 때 | `SOB-01` → `SOB-02` | 코드 추가 할당 |

### 상태 구성

이 화면은 **10개 상태**로 나뉜다. 구분: 기본 · 변형 · 오류 · 분기 · 확인창

| 상태 | Figma 프레임 | 제목 | 구분 |
|---|---|---|---|
| `S1` | `PRJ-04:S01` | 등록 모드 · 교재(책) 추가 | 기본 |
| `S2` | `PRJ-04:S02` | 수정 모드 · 교재(책) 편집 수정 | 기본 |
| `S3` | `PRJ-04:S03` | 완료 상태 · 내용 잠금 | 변형 |
| `S4` | `PRJ-04:S04` | 완료 조건 미충족 | 오류 |
| `S5` | `PRJ-04:S05` | 완료 해제 이력 | 변형 |
| `S6` | `PRJ-04:S06` | 공유 코드 · 사용 고객사 필수 | 오류 |
| `S7` | `PRJ-04:S07` | 단가 고정 · 현재 단가로 갱신 | 분기 |
| `S8` | `PRJ-04:S08` | 필기펜 항목 · 할인 없음 | 변형 |
| `S9` | `PRJ-04:S09` | 교재 삭제 확인창 | 확인창 |
| `S10` | `PRJ-04:S10` | 업무 메모 · 남의 메모 수정 시도 | 오류 |

---

## 3. 상태별 변화

### PRJ-04: S01 등록 모드 · 교재(책) 추가

*화면 정의서 상태 `S1`*

**구분** — 기본

`PRJ-03`의 **[＋ 교재(책) 추가]**로 진입. 화면 제목 **교재(책) 추가**. **등록 모드에서는 S/O를 선택**할 수 있다. 여기서 입력한 **페이지 수와 심볼·기능 수량이 곧 청구액**이 되므로, 편집팀 작업 기록이자 정산 입력 화면이다. 대상 단위 = 교재(책) 1권 = S/O/B 좌표 1개.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 진행 상태 | 선택 | 진행중 / 완료 / 보류 | 기본값 **진행중** |
| 할당된 S / O | 선택 (필수) | — | 할당된 S/O가 여러 개면 선택 |
| Book | 선택 (필수) | — | **사용 가능한 번호**에서만 `P-01`. 한 번에 **100개**만 그리고 **[＋ 100개 더 보기]** 로 100씩 늘린다 `PC-046` |
| Total Page | 입력 | 적용비 계산 | **적용비 계산 기준(페이지 수)** |
| 심볼·기능 수량 | 입력 | 편집·기능비 계산 | 각 항목 **수량 × 단가** |
| [추가] | 클릭 | `PRJ-03` 갱신 | 상단 알림 **추가됨** |
| 저장(자동) | 기록 | `LOG-01` | 등록 = **교재 추가** |
| 저장(자동, 공유 코드) | 반영 | `SOB-01` | 지도에 사용 고객사·편집 상태 반영 |
| 사용 가능한 Book이 없을 때 | 이동 | `SOB-01` → `SOB-02` | 코드 추가 할당 |
| [목록] | 클릭 | `PRJ-03` | 교재 목록으로 · 입력값은 저장되지 않는다 |

### PRJ-04: S02 수정 모드 · 교재(책) 편집 수정

*화면 정의서 상태 `S2`*

**구분** — 기본

`PRJ-03`의 **교재 행 클릭**으로 진입. 화면 제목 **교재(책) 편집 수정**. S/O는 **(수정 불가)**로 표기된다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| S / O | — | **수정 불가** | 수정 모드에서는 변경할 수 없다 |
| Book | 선택 | — | 사용 가능한 번호에서 |
| 펜 모델 · 편집방식 | 추가/삭제 | — | 여러 개 선택 가능 |
| [저장] | 클릭 | `PRJ-03` 갱신 | 상단 알림 **수정됨** · 목록·집계·청구액 갱신 |
| 저장(자동) | 기록 | `LOG-01` | 수정 = **교재 작업** |
| [교재 삭제] | 클릭 | S9 확인창 | — |

### PRJ-04: S03 완료 상태 · 내용 잠금

*화면 정의서 상태 `S3`*

**구분** — 변형

PRD §4.1 · §5 — **완료 처리하려면 ncp2 최종수정 날짜가 필요**하고, 완료 상태에서는 **내용이 잠긴다**.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 안내 | 표시 | — | **🔒 완료 처리되어 내용이 잠겼습니다. 수정하려면 진행중으로 변경하세요.** |
| 입력 항목 | — | **잠금** | — |
| [진행중] | 클릭 | 잠금 해제 | 완료 해제 |
| 완료 처리 후 정산 확정 | — | **미결** | ⚠ §7 — 청구 확정 개념 도입 시 재정의 |

### PRJ-04: S04 완료 조건 미충족

*화면 정의서 상태 `S4`*

**구분** — 오류

PRD §4.1 · §5 — ncp2 최종수정 날짜가 비어 있으면 완료로 바꿀 수 없다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| [완료] | 클릭 | **차단** | **ncp2 최종수정 날짜가 비어 있어 완료할 수 없습니다. 날짜를 입력한 뒤 완료로 변경하세요.** |
| 인라인 안내 | 표시 | — | **※ 완료하려면 ncp2 최종수정 날짜가 필요합니다.** |
| ncp2 최종수정 | 입력 | 완료 가능 | — |

### PRJ-04: S05 완료 해제 이력

*화면 정의서 상태 `S5`*

**구분** — 변형

PRD §4.1 — 완료를 해제하면 그때의 ncp2 최종수정 날짜가 **이력으로 보관**되고(해제 시각·해제자 포함) 화면에서 확인할 수 있다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 완료 해제 이력 | 표시 | — | **· {날짜} (완료 해제 {해제일시} · {해제자})** |
| [완료] | 클릭 | 재완료 | ncp2 최종수정 날짜가 있으면 가능 |

### PRJ-04: S06 공유 코드 · 사용 고객사 필수

*화면 정의서 상태 `S6`*

**구분** — 오류

`P-12` — 공유(커먼) 코드 Owner의 교재는 **사용 고객사 입력이 필수**다. 비우고 저장하면 확인창으로 막는다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 사용 고객사 | 선택 (필수) | — | 공유 코드 Owner일 때만 표시 |
| [저장] | 클릭 | **차단** | **S{n}/O{n} 는 공유 코드입니다. 사용 고객사를 입력하세요.** |
| 저장 후 | 반영 | `SOB-01` | 지도에 사용 고객사·편집 상태 반영 |

### PRJ-04: S07 단가 고정 · 현재 단가로 갱신

*화면 정의서 상태 `S7`*

**구분** — 분기

PRD §4.5 — **등록 시 단가 ({날짜})**는 저장 시점의 고객사 단가로 고정된 상태다. 고정된 단가와 **현재 고객사 단가가 다를 때만⚠ 고객사 단가 변경됨 — 현재 단가로 갱신** 버튼이 나타나며, 누르면 확인 후 **이 교재만** 현재 단가로 다시 계산한다 `PC-027`.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 등록 시 단가 배지 | 표시 | — | **등록 시 단가 ({날짜})** — 고정 상태 |
| 고객사 단가 / 기본 단가 배지 | 표시 | — | 아직 고정 전 — **저장하면 현재 단가로 고정** |
| [현재 단가로 갱신] | 클릭 | 확인창 | **이 교재를 현재 고객사 단가로 다시 계산할까요? (등록 시 단가가 대체됩니다)** |
| 단가를 바꿔야 할 때 | 이동 | `MEM-01` → `MEM-02` | 고객사 단가 지정 |

### PRJ-04: S08 필기펜 항목 · 할인 없음

*화면 정의서 상태 `S8`*

**구분** — 변형

PRD §4.4 — 펜 종류에 따라 입력 항목이 갈린다. **필기펜**은 none 편집비용 · Custom · action 변경 편집 · 노트서버 업로드 · 교원구몬/KEP. ⚠ **Custom · 교원구몬/KEP 단가는 미확정**(`PC-018` 확인 대기).

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 타입 = 필기펜 | 선택 | 항목 전환 | 소리펜 ⇄ 필기펜 |
| Custom · 교원구몬/KEP | — | **미확정** | ⚠ §7 — 단가 확정 대기 |
| 할인율 · 추가 할인액 | 입력 | 청구액 조정 | 0이면 할인 행이 산출 근거에 나오지 않는다 |
| [할인 초기화] | 클릭 | 할인 없음으로 | — |

### PRJ-04: S09 교재 삭제 확인창

*화면 정의서 상태 `S9`*

**구분** — 확인창

PRD §4.7 · §5 — 확인 후 해당 교재 행이 `PRJ-03` 목록에서 제거된다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| [교재 삭제] | 클릭 | 확인창 | **이 책(교재) 편집 행을 삭제할까요?** |
| [확인] | 클릭 | `PRJ-03` 목록에서 제거 | 집계·청구액 갱신 |
| [취소] · [✕] | 클릭 | S2 복귀 | — |

### PRJ-04: S10 업무 메모 · 남의 메모 수정 시도

*화면 정의서 상태 `S10`*

**구분** — 오류

PRD §4.6 · §5 — 업무 메모는 **Enter 기록 · Shift+Enter 줄바꿈**이며 **본인이 작성한 메모만** 수정·삭제할 수 있다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 메모 종류 | 선택 | 요청 / 처리 / 메모 | — |
| 내용 입력 후 Enter | Enter | 기록 | Shift+Enter 줄바꿈 |
| 남의 메모 [수정] | 클릭 | **차단** | **{작성자} 님이 작성한 메모입니다. 본인 글만 수정할 수 있습니다.** |
| 남의 메모 [삭제] | 클릭 | **차단** | **{작성자} 님이 작성한 메모입니다. 본인 글만 삭제할 수 있습니다.** |
| 세부내역·출력용파일·APP 데이터 | 입력 | 링크(URL) + 설명 | 새 탭으로 열 수 있다 · ⚠ §7 — 파일 업로드 전환 여부 미결 |
| 저장 공간 부족 | 상단 알림 | — | **⚠ 브라우저 저장 공간이 가득 차 저장하지 못했습니다. 다른 사이트 데이터를 정리한 뒤 다시 저장하세요.** |
