# DSH-01 · 대시보드

**화면 ID**: DSH-01

> 원본 — 화면 정의서 `docs/screens/` · PRD `docs/prd/DSH-01_대시보드.md`
> 이 파일은 화면 정의서와 **같은 소스에서 자동 생성**된다. 직접 고치지 말 것.

---

## 2. 화면 구성

### 개요

로그인 후 처음 만나는 화면. **코드가 얼마나 남아 있고 · 편집이 얼마나 진행됐고 · 받을 돈이 얼마이고 · 지금 처리할 일이 무엇인지**를 한 화면에서 파악한다. 분야별 **4개 구역**(코드 · 편집 · 정산 · 운영)으로 나뉘며 **조회 전용**이다 — 상세 확인·수정은 각 화면코드로 이동해서 수행한다. 집계는 화면을 열 때 원장에서 다시 계산하며, 계산 규칙과 예시는 PRD §4.2 에 있다.

### 상태 구성

이 화면은 **7개 상태**로 나뉜다. 구분: 기본 · 빈 상태 · 변형 · 분기 · 권한

| 상태 | Figma 프레임 | 제목 | 구분 |
|---|---|---|---|
| `S1` | `DSH-01:S01` | 기본 · 데이터 정상 | 기본 |
| `S2` | `DSH-01:S02` | 처리할 알림 없음 | 빈 상태 |
| `S3` | `DSH-01:S03` | Section 정원 경고 | 변형 |
| `S4` | `DSH-01:S04` | 편집 상태 미지정 — 전량 진행중 | 빈 상태 |
| `S5` | `DSH-01:S05` | 미정산 추정 불가 | 분기 |
| `S6` | `DSH-01:S06` | 최근 활동 없음 | 빈 상태 |
| `S7` | `DSH-01:S07` | STAFF 진입 — 화면은 동일 | 권한 |

---

## 3. 상태별 변화

### DSH-01: S01 기본 · 데이터 정상

*화면 정의서 상태 `S1`*

**구분** — 기본

로그인 성공 직후 열리는 기본 진입 화면. 분야별 **4개 구역**을 위에서 아래로 배치한다 — ① 코드(SOBP) ② 편집(CasterN) ③ 정산 ④ 운영. 각 구역은 **요약 3장 + 상세 카드**로 이루어지고, 구역 제목 우측 링크로 해당 관리 화면에 들어간다. **조회 전용**이며 사용자 입력이 없어 검증 오류가 없다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| ① 코드 요약 | 조회 | — | 코드 할당율 3.5% · 발급 owner 475 · 미발급 owner 13,093 |
| ② 편집 요약 | 조회 | — | CasterN 고객사 69 · 편집 완료 교재 1,240 · 전용 단가 고객사 7 |
| ③ 정산 요약 | 조회 | — | 편집 청구액 ₩1,284,900,000 · 미정산 추정 ~₩11,800,000 · Key 발급 정산 ₩8,300,000 |
| ④ 운영 알림 | 조회 | — | 처리 필요 항목만 건수로 표시 — 0건은 흐리게 |
| 요약 카드 | 클릭 | 해당 관리 화면 | 카드마다 이동 대상이 다르다 — PRD §6 |
| 최근 활동 행 | 조회 | — | 시각 · 활동 종류 · 내용 · 담당자 — **최근 8건** |
| ① 구역 제목 [SOBP 맵] | 클릭 | `SOB-01` | 코드 여력 확인 |
| ② 구역 제목 [편집 프로젝트] | 클릭 | `PRJ-02` | 편집량·정산 |
| ③ 구역 제목 [Key 관리] | 클릭 | `TKT-01` | 발급·정산 목록 |
| ④ 구역 제목 [전체 로그] | 클릭 | `LOG-01` | 활동 로그 전체 조회 |
| 좌측 메뉴 [SOBP 맵] | 클릭 | `SOB-01` | Section·Owner·Book·Page 드릴다운 |
| 좌측 메뉴 [고객사 관리] | 클릭 | `MEM-01` | 업체 상세·계약·단가 |
| 좌측 메뉴 [편집 프로젝트] | 클릭 | `PRJ-02` | 편집량·정산 |
| 좌측 메뉴 [Key 관리] | 클릭 | `TKT-01` | 발급·정산 목록 |

### DSH-01: S02 처리할 알림 없음

*화면 정의서 상태 `S2`*

**구분** — 빈 상태

PRD §5 — 운영 알림 6항목이 모두 **0건**인 상태. 건수 배지가 **흐리게** 표시되어 "할 일 없음"이 한눈에 읽힌다. **이동은 그대로 가능**하며 나머지 구역은 정상 표시된다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 운영 알림 6항목 | 표시 | — | 모두 0건 — 흐리게 |
| 알림 항목 | 클릭 | 해당 화면 | 0건이어도 **이동 가능** |
| ①②③ 구역 | 조회 | — | 정상 표시 |

### DSH-01: S03 Section 정원 경고

*화면 정의서 상태 `S3`*

**구분** — 변형

사용률이 **50% 이상**인 Section 은 막대를 강조하고 **비율(%)**을 함께 적는다. 운영 알림에도 해당 Section 목록이 올라온다. 코드 여력은 늦게 알면 손쓸 수 없으므로 미발급 요약 카드에도 **사용률이 가장 높은 Section** 을 표기한다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| Section 5 | 표시 | — | **163 / 256 · 64%** — 경고 대상 |
| Section 3 | 표시 | — | 212 / 4,096 · 5% — 해당 없음 |
| 미발급 요약 카드 | 조회 | — | 부가 표기 **Section 5 사용률 63.7%** |
| 운영 알림 [정원 50% 이상] | 클릭 | `SOB-01` | 여력 확인 |
| ⚠ 참고 | — | `SOB-01` 과 기준이 다르다 | SOB-01 의 **추천제외**는 이 배지와 별개 값이다 |

### DSH-01: S04 편집 상태 미지정 — 전량 진행중

*화면 정의서 상태 `S4`*

**구분** — 빈 상태

교재 진행 상태를 한 번도 지정하지 않으면 **전량이 진행중**으로 집계된다. 이때만 분포 카드 아래에 안내가 붙는다. 편집 완료 교재 요약은 **0** 이 된다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 편집 진행 상태 분포 | 표시 | — | 진행중 8,483 (100%) · 완료 0 · 보류 0 |
| 안내 문구 | 표시 | — | **시드 데이터에는 진행 상태 값이 없어 전부 진행중으로 집계됩니다. 편집 상세에서 교재 상태를 바꾸면 여기에 반영됩니다.** |
| 편집 완료 교재 | 조회 | — | **0** · 전체 8,483권 중 0% |
| 교재 상태 변경 | — | `PRJ-04` | 교재 편집 화면에서 바꾸면 이 분포에 반영 |

### DSH-01: S05 미정산 추정 불가

*화면 정의서 상태 `S5`*

**구분** — 분기

유료 티켓 이력이 **전혀 없으면** 추정 근거가 없어 금액을 **—** 로 표시한다. 미정 건수는 그대로 보여주고, 운영 알림의 [정산 미등록]도 건수만 남는다. 계산 규칙은 PRD §4.2(마).

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 미정산 금액 추정 | 표시 | — | **—** · 미정 3건 · 유료 이력 없어 추정 불가 |
| 운영 알림 [정산 미등록] | 표시 | — | 건수 3 — 추정액 문구 없음 |
| 추정 근거 | — | — | 1순위 같은 고객사 최근 유료 금액 · 2순위 전체 유료 평균 |

### DSH-01: S06 최근 활동 없음

*화면 정의서 상태 `S6`*

**구분** — 빈 상태

PRD §5 — 활동 목록 영역에 기록이 없다는 안내만 표시되고, **구역 제목의 [전체 로그] 이동은 그대로 가능**하다. 나머지는 정상 표시된다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 최근 활동 | 표시 | — | **기록된 활동이 없습니다. (고객사 수정·코드 할당·프로젝트 등록·티켓 발급·교재 작업·로그인 시 기록)** |
| [전체 로그 →] | 클릭 | `LOG-01` | 기록이 없어도 **이동 가능** |
| 운영 알림 | 조회 | — | 정상 표시 |

### DSH-01: S07 STAFF 진입 — 화면은 동일

*화면 정의서 상태 `S7`*

**구분** — 권한

⚠ **이 화면에는 권한 분기가 없다.** STAFF도 ADMIN과 **똑같은 4개 구역**을 본다. 차이는 **좌측 메뉴**뿐으로, **활동 로그** 메뉴에 **ADMIN** 배지가 붙는다. 구역 제목 **[전체 로그]** 로 `LOG-01` 에 들어가면 그 화면에서 **🔒 접근 권한이 없습니다** 로 막힌다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 4개 구역 전체 | 조회 | 동일 | STAFF도 그대로 본다 |
| 좌측 메뉴 [활동 로그] | 표시 | **ADMIN** 배지 | 메뉴를 숨기지 않는다 |
| [전체 로그 →] | 클릭 | `LOG-01` | STAFF 는 그 화면에서 차단된다 — **🔒 접근 권한이 없습니다** |
| 최근 활동 내용 | 조회 | — | 활동 로그 화면은 막히지만 **대시보드의 최근 8건은 보인다** |
| 좌측 메뉴 [SOBP 맵] | 클릭 | `SOB-01` | Section·Owner·Book·Page 드릴다운 |
| 좌측 메뉴 [고객사 관리] | 클릭 | `MEM-01` | 업체 상세·계약·단가 |
| 좌측 메뉴 [편집 프로젝트] | 클릭 | `PRJ-02` | 편집량·정산 |
| 좌측 메뉴 [Key 관리] | 클릭 | `TKT-01` | 발급·정산 목록 |
