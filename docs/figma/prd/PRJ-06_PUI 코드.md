# PRJ-06 · PUI 코드 (페이퍼)

**화면 ID**: PRJ-06

> 원본 — 화면 정의서 `docs/screens/` · PRD `docs/prd/PRJ-06_PUI 코드.md`
> 화면 구성·상태는 화면 정의서에서, 기능 개요·흐름·정책·사용법·연결 화면·문서 이력은 PRD 에서 **자동으로 모아** 만든다. 이 파일을 직접 고치지 말 것.

---

## 2. 화면 구성

### 개요

**PUI(Paper UI) = 종이 위 컨트롤러**에 쓰이는 코드. 재생·정지·볼륨 같은 **종이 위 조작 기능이 어떤 Book/Page에 매핑돼 있는지**를 확인하는 참조 화면. 구성은 **좌 280px 할당 목록 / 우 상세**이며, 상세는 **프로젝트·할당 코드** + 접었다 펼치는 **🖼 컨트롤러 이미지 · ⚙ 기능 매핑 · 📄 원본 시트** 3개 섹션이다. **이 화면에서는 코드를 발급하지 않는다**(발급은 `SOB-02`). · 원장 데이터

### 기능 개요

**PUI = Paper UI(종이 UI)** 의 약자다 `PC-043`. **종이 위 컨트롤러**에 쓰이는 코드로, 재생·정지·볼륨 같은 **종이 위 조작 기능이 어떤 Book/Page에 매핑돼 있는지**를 확인하는 참조 화면이다. 일반 콘텐츠 코드와 달리 기능 자체가 코드에 고정돼 있어 **조회·참조 중심**으로 사용한다.

| 항목 | 내용 |
|---|---|
| 권한 | STAFF / ADMIN |
| 진입 | 좌측 메뉴 [서비스 관리 ▸ CasterN 서비스 관리 ▸ PUI 코드 (페이퍼)] |
| 성격 | **참조 전용** (기능표·할당 코드 조회) + 참고 이미지 보관 |
| 구성 | 좌: PUI 할당(Section/Owner) 목록 · 우: 선택 할당 상세 |

### 화면 흐름 (Flow)

```
[서비스 관리 ▸ CasterN ▸ PUI 코드 (페이퍼)]
   └─▶ PRJ-06
         ├─ 좌측에서 PUI 할당(S/O) 선택 ─▶ 우측 상세
         │     ├─ 프로젝트 / 할당 코드 표
         │     ├─ 이미지(펼치기/접기)
         │     ├─ 기능표(펼치기/접기)
         │     └─ 원본 데이터(펼치기/접기)
         └─ 코드 좌표를 지도에서 확인하려면 ─▶ SOB-01
```

### 정책

| 정책 | 이 화면에서의 적용 |
|---|---|
| `P-01` SOBP | PUI도 동일하게 Section/Owner/Book/Page 좌표로 관리한다 |
| `P-02` 코드 종류 N/G | 할당별 코드 종류(PDS)를 함께 표기 |
| `P-05` 무겹침 | PUI 할당 코드도 일반 코드와 같은 원장에서 관리된다(별도 코드 체계가 아님) |
| 참조 성격 | 이 화면에서는 **코드를 발급하지 않는다.** 발급은 `SOB-02` |

### 사용법

#### 4.1 상단 요약 (S1)

맨 위 한 줄에 화면 이름과 규모가 나온다 — **PUI 코드 관리** · *종이 위 컨트롤러(Paper UI) 프로젝트 할당 코드* · `할당 {n}` · `프로젝트 {n}` · `기능 {n}`

#### 4.2 좌측 — PUI 할당 목록 (S1)

제목은 **PUI 할당 (Section/Owner)**. 들어오면 **첫 할당이 자동 선택**된다.

| 표시 | 내용 |
|---|---|
| S / O | 할당된 Section·Owner (`S{n}/O{n}`) + **코드 종류 배지** |
| 라벨 | 할당 이름(있을 때만) |
| 건수 | `프로젝트 {n} · 기능 {n}` |

#### 4.3 우측 — 상세

머리에 `S{n} / O{n} · {라벨}` 과 `{코드 종류} · 시트 {시트명}` 이 표시되고, 그 아래 **프로젝트 / 할당 코드 ({n})** 표가 온다. 이어서 **접었다 펼치는 3개 묶음**이 나온다.

| 묶음 | 기본 | 내용 | 화면 |
|---|---|---|---|
| **프로젝트 / 할당 코드** | 항상 | 프로젝트 · Book · Page · 제품 · 고객사 · 메모/포함기능 · 부서/발급인 · 비고 | S2 |
| **🖼 컨트롤러 이미지** | **펼침** | 참고 이미지 등록·확인 · *디자인 시안·인쇄물 이미지를 올려 보면서 작업* | S3 |
| **⚙ 기능 매핑 (Book/Page/params)** | 접힘 | 대구분 · 소구분 · 기능명 · 요약 · Book · Page · params | S2 |
| **📄 원본 시트** | 접힘 | 엑셀 원본 그대로 · `{n}행` 배지 · 영역 안에서 스크롤 | S4 |

각 묶음 제목 옆에 **건수 배지**가, 오른쪽 끝에 **펼치기 ▼ / 접기 ▲** 가 있다.

| 표가 빌 때 | 문구 | 화면 |
|---|---|---|
| 프로젝트 표 없음 | **정규화된 프로젝트 표가 없습니다. 아래 원본 시트에서 확인하세요.** | S1 |
| 기능 매핑 없음 | **이 할당에는 기능 매핑 표가 없습니다.** | S1 |

#### 4.4 컨트롤러 이미지 (S3 · S5 · S6)

| 요소 | 동작 |
|---|---|
| [＋ 이미지 업로드] | 여러 장을 한 번에 고를 수 있고 **한 번에 최대 8장**까지 받는다 |
| 저장 위치 | **브라우저에 저장**된다 — 서버로 올라가지 않는다. 안내: *브라우저에 저장 · 클릭하면 크게 보기* |
| 이미지 클릭 | **화면 전체 위에 크게** 열린다. 파일명과 **[닫기 ✕]**, 바깥 어두운 영역을 눌러도 닫힌다 (S6) |
| [삭제] | 그 이미지만 제거 |
| 이미지 없음 | 점선 박스 **업로드된 이미지가 없습니다. PUI 컨트롤러 디자인/인쇄 시안을 올려보세요.** |

#### 4.5 사용 절차

1. 좌측에서 확인할 PUI 할당(S/O)을 고른다.
2. **프로젝트 / 할당 코드** 표에서 어떤 프로젝트가 어떤 Book/Page를 쓰는지 확인한다.
3. **기능 매핑**에서 기능별 Book·Page·params를 확인한다(종이 위 버튼 매핑 근거).
4. 필요하면 참고 이미지를 등록해 둔다.
5. 정규화되지 않은 항목은 **원본 시트**에서 확인한다.

### 연결 화면

| 트리거 | 이동 | 설명 |
|---|---|---|
| 코드 좌표 확인 | `SOB-01` | 해당 S/O/B 위치·상태 |
| 신규 PUI 코드 발급 | `SOB-02` | 이 화면에서는 발급하지 않는다 |
| 고객사 확인 | `MEM-01` | 업체 상세 |

### 문서 이력

| 버전 | 날짜 | 수정자 | 변경 내용 |
|---|---|---|---|
| v1.0 | 2026-08-25 | 서비스기획팀 | 최초 작성 |
| v1.1 | 2026-08-27 | 서비스기획팀 | 구현 화면 기준 정정 — 상단 요약, **첫 할당 자동 선택**, 묶음별 **기본 펼침/접힘**과 건수 배지, **이미지 확대**(S6)·업로드 8장 제한·브라우저 저장, 빈 표 문구 2종, 화면 상태 번호(S1~S6) 연결 |
| v1.2 | 2026-08-31 | 서비스기획팀 | **메뉴명 `PUI 코드 (피지컬)` → `PUI 코드 (페이퍼)`** `PC-043` — **PUI = Paper UI**(종이 UI)의 약자임을 확인해 "피지컬" 표기를 모두 "페이퍼/종이 위"로 바로잡음 |

### 상태 구성

이 화면은 **6개 상태**로 나뉜다. 구분: 기본 · 변형 · 분기 · 오류

| 상태 | Figma 프레임 | 제목 | 구분 |
|---|---|---|---|
| `S1` | `PRJ-06:S01` | 기본 · 첫 할당 자동 선택 | 기본 |
| `S2` | `PRJ-06:S02` | 프로젝트 · 기능 매핑이 있는 할당 | 기본 |
| `S3` | `PRJ-06:S03` | 컨트롤러 이미지 등록됨 | 변형 |
| `S4` | `PRJ-06:S04` | 원본 시트 펼침 | 분기 |
| `S5` | `PRJ-06:S05` | 이미지 저장 실패 (용량 초과) | 오류 |
| `S6` | `PRJ-06:S06` | 컨트롤러 이미지 확대 | 분기 |

---

## 3. 상태별 변화

### PRJ-06: S01 기본 · 첫 할당 자동 선택

*화면 정의서 상태 `S1`*

**구분** — 기본

좌측 메뉴 [서비스 관리 ▸ CasterN ▸ PUI 코드 (페이퍼)]로 진입. 구현은 **첫 할당을 자동 선택**한다(`useState(D.allocations[0].sheet)`) — 따라서 **"할당 미선택" 상태는 실제로 나타나지 않는다**. 기본 펼침 상태는 **이미지만 펼침**, 기능 매핑·원본 시트는 접힘이다. 첫 할당(S3/O52 아이글 출석부)은 정규화된 프로젝트·기능 표가 없어 안내 문구가 보인다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 상단 요약 | 조회 | — | 할당 **6** · 프로젝트 **41** · 기능 **100** |
| 좌측 할당 항목 | 클릭 | 우측 상세 교체 | S{n}/O{n} + PDS 배지 + 라벨 + 프로젝트/기능 건수 |
| 프로젝트 표 없음 | 표시 | — | **정규화된 프로젝트 표가 없습니다. 아래 원본 시트에서 확인하세요.** |
| [펼치기 ▼] · [접기 ▲] | 클릭 | 섹션 토글 | 이미지 · 기능 매핑 · 원본 시트 |
| ⚠ PRD와 다름 | 참고 | — | PRD §5는 "할당 미선택 → **할당을 선택하세요.**"를 상태로 두지만 구현은 항상 첫 항목이 선택돼 있어 **도달할 수 없다** |
| 코드 좌표 확인 | 이동 | `SOB-01` | 해당 S/O/B 위치·상태 |
| 신규 PUI 코드 발급 | 이동 | `SOB-02` | **이 화면에서는 발급하지 않는다** |
| 고객사 확인 | 이동 | `MEM-01` | 업체 상세 |

### PRJ-06: S02 프로젝트 · 기능 매핑이 있는 할당

*화면 정의서 상태 `S2`*

**구분** — 기본

S3/O1011을 선택한 상태. **프로젝트 2건 · 기능 100건**이 있어 표가 채워진다. 프로젝트 표는 최소 폭 700px, 기능 매핑 표는 820px로 **가로 스크롤**된다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 프로젝트 / 할당 코드 | 조회 | — | 프로젝트 · Book · Page · 제품 · 고객사 · 메모/포함기능 · 부서/발급인 · 비고 |
| 기능 매핑 | 조회 | — | 대구분 · 소구분 · 기능명 · 요약 · Book · Page · **params**(파랑 고정폭) |
| 빈 셀 | 표시 | — | 값이 없으면 **-** |
| 가로 스크롤 | 드래그 | — | 프로젝트 700px · 기능 820px 최소 폭 |
| 코드 좌표 확인 | 이동 | `SOB-01` | 해당 S/O/B 위치·상태 |
| 신규 PUI 코드 발급 | 이동 | `SOB-02` | **이 화면에서는 발급하지 않는다** |
| 고객사 확인 | 이동 | `MEM-01` | 업체 상세 |

### PRJ-06: S03 컨트롤러 이미지 등록됨

*화면 정의서 상태 `S3`*

**구분** — 변형

PRD §4.2 — 참고 이미지를 등록·확인한다. 카드 격자는 `repeat(auto-fill, minmax(150px, 1fr))`이며 클릭하면 확대된다. ⚠ §7 미결 — 보관 위치(현재 브라우저 저장, 용량 제한).

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| [＋ 이미지 업로드] | 클릭 | 파일 선택 | 한 번에 최대 **8장** |
| 이미지 | 클릭 | 확대 보기 | **브라우저에 저장 · 클릭하면 크게 보기** |
| [삭제] | 클릭 | 이미지 제거 | — |
| 개수 배지 | 표시 | — | 0이면 회색, 1개 이상이면 파랑 |
| 보관 위치 | — | **미결** | ⚠ §7 — 서버 보관 전환 여부 |

### PRJ-06: S04 원본 시트 펼침

*화면 정의서 상태 `S4`*

**구분** — 분기

PRD §4.2 — 가공 전 엑셀 원본 행을 그대로 확인한다(최대 높이 460px 스크롤). 첫 열은 회색으로 구분된다. ⚠ §7 미결 — 원본 갱신 주기·담당.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 📄 원본 시트 | 클릭 | 펼침/접힘 | **{n}행** 배지 + **· 엑셀 원본 그대로** |
| 원본 표 | 스크롤 | — | 최대 높이 460px |
| 갱신 주기·담당 | — | **미결** | ⚠ §7 — 현재 빌드 시점 데이터 |
| PUI 기능 추가 시 | — | **미결** | ⚠ §7 — 기존 Book/Page 확장 vs 신규 발급 |

### PRJ-06: S05 이미지 저장 실패 (용량 초과)

*화면 정의서 상태 `S5`*

**구분** — 오류

PRD §5 메시지 — 브라우저 저장 실패 시 상단에 **빨간 토스트**로 노출되고 4초 뒤 사라진다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 상단 알림 | 표시 | — | **저장 용량 초과 — 이미지 수를 줄여주세요.** |
| 자동 사라짐 | 4초 후 | — | — |
| 이미지 삭제 | 클릭 | 용량 확보 | — |

### PRJ-06: S06 컨트롤러 이미지 확대

*화면 정의서 상태 `S6`*

**구분** — 분기

이미지를 클릭하면 **화면 전체 위에 크게** 열린다. 파일명과 **[닫기 ✕]** 가 함께 나오고, **바깥 어두운 영역을 클릭해도 닫힌다**.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 이미지 | 클릭 | 확대 | 커서가 **돋보기(＋)** 로 바뀐다 |
| [닫기 ✕] · 바깥 영역 | 클릭 | 닫힘 | — |
| 이미지 저장 위치 | 참고 | — | **브라우저에 저장**된다 — 서버에 올라가지 않는다 |
| 업로드 개수 | 참고 | — | 한 번에 **최대 8장** |
