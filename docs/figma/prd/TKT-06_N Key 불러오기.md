# TKT-06 · N Key 불러오기

**화면 ID**: TKT-06

> 원본 — 화면 정의서 `docs/screens/` · PRD `docs/prd/TKT-06_N Key 불러오기.md`
> 이 파일은 화면 정의서와 **같은 소스에서 자동 생성**된다. 직접 고치지 말 것.

---

## 2. 화면 구성

### 개요

내려받은 **키 파일(.json)** 을 열어 그 안에 무엇이 들어 있는지 확인하는 창이다. `TKT-03` Key 관리 도구 막대의 **[📂 N Key 불러오기]** 로 연다. **발급 원장에 없는 외부 파일도 확인할 수 있고**, 조회 전용이라 파일을 고쳐 저장하지 않는다. 원장에 있는 건의 항목은 `TKT-05` 발급 상세에서 본다 — 이 창은 **파일** 을 본다.

### 상태 구성

이 화면은 **4개 상태**로 나뉜다. 구분: 기본 · 필터 · 빈 상태 · 검증

| 상태 | Figma 프레임 | 제목 | 구분 |
|---|---|---|---|
| `S1` | `TKT-06:S01` | 불러오기 완료 — 항목 표시 | 기본 |
| `S2` | `TKT-06:S02` | 항목 검색 | 필터 |
| `S3` | `TKT-06:S03` | 검색 결과 없음 | 빈 상태 |
| `S4` | `TKT-06:S04` | 형식 오류 | 검증 |

---

## 3. 상태별 변화

### TKT-06: S01 불러오기 완료 — 항목 표시

*화면 정의서 상태 `S1`*

**구분** — 기본

`TKT-03` 도구 막대의 **[📂 N Key 불러오기]** 로 파일을 고르면 내용을 읽어 **Key · Value 2열**로 보여준다. 어떤 파일을 보고 있는지 **파일명이 위에 고정**된다. **발급 원장에 없는 외부 파일도 열 수 있다.** 조회 전용이며 파일을 고쳐 저장하지 않는다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| [📂 N Key 불러오기] | 클릭 | 파일 선택 창 | `.json` 대상 |
| 파일명 | 표시 | — | Ticket_웅진씽크빅_S3O17_B400-499.json |
| 항목 수 | 표시 | — | **13개 항목 (불러온 파일)** |
| 표 | 조회 | Key · Value | 파일에 든 항목을 **그대로** 나열 |
| 중첩 값 | 표시 | 펼침 | 한 줄씩 풀어서 보여준다 |
| 같은 파일 재선택 | 가능 | — | 파일을 고친 뒤 다시 확인할 수 있다 |
| [표 복사] | 클릭 | 복사 | **현재 표시 중인** 항목 |
| [닫기] · ✕ | 클릭 | `TKT-03` | 목록의 필터·페이지 상태는 유지된다 |
| 원장 건의 항목 | 조회 | `TKT-05` | 발급 상세의 Key 정보 — 이 창과 별개 |
| 파일 만들기 | — | `TKT-04` | N Key 발급에서 내려받는다 |

### TKT-06: S02 항목 검색

*화면 정의서 상태 `S2`*

**구분** — 필터

검색어로 표를 거른다. **항목명과 값을 함께** 찾는다. 항목 수도 걸러진 개수로 함께 줄어들고, **[표 복사]** 는 걸러진 것만 복사한다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 항목 검색 | 입력 | 표 필터 | 항목명 + 값 |
| 항목 수 | 자동 | 재계산 | **3개 항목 (불러온 파일)** |
| [표 복사] | 클릭 | 걸러진 것만 | 표시 중인 항목 기준 |
| 검색어 지움 | — | 전체 복귀 | 13개 항목으로 돌아온다 |

### TKT-06: S03 검색 결과 없음

*화면 정의서 상태 `S3`*

**구분** — 빈 상태

검색어에 맞는 항목이 없으면 표에 안내만 남는다. 검색어를 지우면 전체가 돌아온다. 불러온 파일 자체는 그대로 유지된다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 표 | 표시 | — | **"owner999" 에 맞는 항목이 없습니다.** |
| 파일명 | 표시 | 유지 | 불러온 파일은 그대로 |
| 검색어 지움 | — | 전체 복귀 | — |

### TKT-06: S04 형식 오류

*화면 정의서 상태 `S4`*

**구분** — 검증

JSON 으로 읽을 수 없는 파일을 고르면 오류 안내와 함께 **빈 표**가 남는다. **창은 닫히지 않아** 바로 다른 파일을 고를 수 있다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 형식 오류 | 표시 | — | **⚠ JSON 형식의 티켓 파일이 아닙니다. Key 생성으로 내려받은 .json 파일을 선택하세요.** |
| 표 | 표시 | — | **표시할 항목이 없습니다.** |
| 창 | 유지 | — | 닫히지 않는다 — 다른 파일 재선택 가능 |
| [닫기] · ✕ | 클릭 | `TKT-03` | 목록으로 |
