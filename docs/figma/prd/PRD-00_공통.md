# PRD-00 · 공통 (로그인 · 셸 · 공통 패턴)

**화면 ID**: PRD-00

> 원본 — 화면 정의서 `docs/screens/` · PRD `docs/prd/PRD-00_공통.md`
> 화면 구성·상태는 화면 정의서에서, 기능 개요·흐름·정책·사용법·연결 화면은 PRD 에서 **자동으로 모아** 만든다. 이 파일을 직접 고치지 말 것.

---

## 2. 화면 구성

### 개요

모든 화면이 공유하는 **로그인 · 권한 · 셸 · 공통 패턴 · 메시지 규칙**. 각 화면 정의서는 여기서 정의한 규칙을 반복하지 않고 참조한다. 역할은 **ADMIN / STAFF** 2종이며 `LOG-01`만 ADMIN 전용 메뉴다. 프레임 1장 = 1440×900 · Figma 프레임 1개.

### 기능 개요

모든 화면이 공유하는 **로그인·권한·역할·공통 목록/필터/검색 패턴·메시지 규칙**을 정의한다. 각 화면 PRD는 여기서 정의한 규칙을 반복 기술하지 않고 참조한다.

| 항목 | 내용 |
|---|---|
| 대상 사용자 | 네오랩 **내부 직원 전용** (외부 고객 접근 없음 — `P-10`) |
| 역할 | **ADMIN** / **STAFF** 2종 |
| 포함 화면 | 로그인, 개인정보 수정(헤더 모달), 좌측 메뉴·상단바 |
| 적용 범위 | 전 화면 |

### 화면 흐름 (Flow)

```
[비로그인] ──▶ 로그인 화면
                 │  회사 계정(Google) 로그인 성공
                 ▼
              DSH-01 대시보드 (기본 진입 화면)
                 │
                 ├─ 좌측 메뉴 ─▶ TKT-04 / SOB-01 / PRJ-01 / PRJ-02 / PRJ-06
                 │              MEM-01 / LOG-01(ADMIN) / INF-01
                 ├─ 상단 계정 ─▶ 개인정보 수정(모달) → 저장 → 원래 화면 유지
                 └─ 로그아웃 ──▶ 로그인 화면
```

- 미로그인 상태로 화면 경로에 접근하면 **로그인 화면으로 이동**한다.
- 로그아웃 진행 중에는 화면 셸 대신 전환 화면을 보여준다(비로그인 잔상 방지).

### 정책

| 정책 | 이 문서에서의 적용 |
|---|---|
| `P-10` 내부 직원 전용 | 외부 고객 가입·요청 절차 없음. 계정은 회사 도메인 계정만 |
| `PC-002` 행위자 모델 | Customer 역할 폐기. 역할은 ADMIN/STAFF 2종 |
| `P-04` 코드 상태 | 전 화면에서 코드 상태는 **할당됨(발급) / 미발급** 2가지로만 표기. "예약/사용중" 용어 사용 금지 |
| 용어 기준 | 코드 좌표는 **SOBP**(Section–Owner–Book–Page). 한글 "부분" 등 대체어 사용 금지 |

#### 3.1 역할·권한

| 역할 | 부여 방식 | 권한 |
|---|---|---|
| **ADMIN** | 직원 명단에 ADMIN으로 등록된 계정 | 전 화면 |
| **STAFF** | 회사 도메인 계정으로 로그인하면 자동 부여(명단에 없는 경우 포함) | `LOG-01` 을 제외한 전 화면 |

> `LOG-01`(활동 로그)은 **메뉴를 숨기지 않는다.** 메뉴에 **ADMIN** 배지가 붙고, STAFF 가 들어가면 그 화면에서 **🔒 접근 권한이 없습니다 / 활동 로그는 관리자(ADMIN) 계정만 열람할 수 있습니다.** 로 막힌다. (S10)

- 회사 도메인(`@neolab.net`) 계정만 로그인할 수 있다. 도메인 검증은 로그인 시 서버에서 재확인한다.
- 명단에 없는 회사 계정은 **STAFF로 자동 등록**되어 바로 사용할 수 있다(별도 승인 절차 없음).

### 사용법

#### 4.1 로그인

로그인 화면은 **환경 설정에 따라 둘 중 하나만** 나온다.

| 방식 | 조건 | 절차 | 화면 |
|---|---|---|---|
| **회사 Google 계정(SSO)** | OAuth 자격증명이 설정된 환경 | **[회사 Google 계정으로 로그인]** → 회사 계정 선택 → 성공 시 `DSH-01` | S2 |
| **회사 등록 계정(로컬)** | SSO 미설정 환경 | 이메일 + 비밀번호 입력 후 Enter 또는 [로그인] | S1 · S5 |

> SSO 환경에서는 **세션당 1회** 자동 로그인이 시도된다(취소 시 반복하지 않는다).
> 안내 문구도 갈린다 — SSO: *회사 **Google 계정**으로 로그인하세요. **@neolab.net** 계정이면 승인 없이 바로 이용할 수 있습니다.* / 로컬: *회사에 **등록된 계정**으로 로그인하세요. (내부 직원 전용 · **@neolab.net**)*

입력 항목

| 항목 | 필수 | 규칙 |
|---|---|---|
| 이메일 | ✔ | 회사 도메인 계정 (`name@neolab.net`) |
| 비밀번호 | ✔(로컬 로그인만) | 초기값 = 이메일. Enter로 제출 |

#### 4.2 개인정보 수정 (상단 계정 → 모달) (S7 · S8)

| 항목 | 필수 | 규칙 |
|---|---|---|
| 이름 | ✔ | 표시 이름 |
| 이메일 | — | **읽기 전용** (변경 불가) |
| 새 비밀번호 | — | **변경할 때만 입력**. 비워두면 기존 비밀번호 유지 |

저장하면 모달이 닫히고 **직전 화면이 그대로 유지**된다(화면 이동 없음).

#### 4.3 앱 셸 (S6 · S9 · S10 · S11)

로그인하면 **좌측 메뉴 + 상단 바** 구조의 앱 셸 안에서 모든 화면이 열린다.

| 요소 | 동작 | 화면 |
|---|---|---|
| 좌측 메뉴 | 그룹별로 접었다 펼 수 있다. **현재 화면이 속한 그룹은 항상 펼쳐진다** | S6 |
| 메뉴 접기 **«** | 사이드바가 **아이콘 레일**로 줄어든다 | S9 |
| 레일 펼치기 **»** · 로고 | 사이드바를 다시 편다 | S9 |
| 상단 계정 | 누르면 **개인정보수정 / 로그아웃** 메뉴가 열린다 | S7 |
| 로그아웃 | **로그아웃 중…** 전환 화면을 거쳐 로그인 화면으로 | S11 |
| STAFF 로그인 | 메뉴 구성은 같고 **활동 로그**에 **ADMIN** 배지가 붙는다 | S10 |

#### 4.4 좌측 메뉴 ↔ 화면코드

| 그룹 | 메뉴 | 화면code |
|---|---|---|
| — | 대시보드 | `DSH-01` |
| 코드 | SOBP 맵 | `SOB-01` |
| 코드 | 코드 프로젝트 (전 서비스 공통) | `PRJ-01` |
| 티켓 발급 | 계정 발급 (목록) | `TKT-01` (하위 `TKT-02` 등록·수정) |
| 티켓 발급 | N Key 발급 | `TKT-04` |
| 티켓 발급 | Key 발급 정산 | `TKT-03` |
| 서비스 관리 ▸ CasterN 서비스 관리 | 편집 프로젝트 | `PRJ-02` |
| 서비스 관리 ▸ CasterN 서비스 관리 | PUI 코드 (피지컬) | `PRJ-06` |
| 서비스 관리 | 폼솔루션 (**예정**) | `SVC-01` |
| 멤버 관리 | 고객사 관리 | `MEM-01` |
| 멤버 관리 | 활동 로그 ★ADMIN | `LOG-01` |
| 정보 | 코드 관리 정보 | `INF-01` (하위 탭 `INF-04` OID 관리대장) |
| 정보 | 브랜드(CI) · DB 구조 | (내부 참조 · PRD 없음) |

사이드바 전체는 접기/펼치기가 가능하며, 접은 상태에서도 각 메뉴로 이동할 수 있다.

**하위 메뉴가 있는 항목**(예: CasterN 서비스 관리)은 **항목을 누르면 하위 메뉴가 접혔다 펼쳐진다.**

| 동작 | 결과 |
|---|---|
| 하위가 있는 항목 클릭 | 하위 메뉴 접기 ⇄ 펼치기 (화면 이동 없음). 오른쪽 표식 `▾`(펼침) / `▸`(접힘) |
| 기본 상태 | 펼침 |
| 하위 화면에 머무는 동안 | 접어도 **펼친 상태가 유지**된다(현재 위치를 잃지 않도록) |
| 하위 메뉴 클릭 | 해당 화면으로 이동 |

**서비스 관리 그룹의 확장 규칙** — NcodeCenter는 사내에서 **Ncode를 쓰는 모든 서비스**의 프로젝트 현황(집계·상태)을 관리한다.
서비스별로 관리 메뉴를 두며, 화면이 아직 없는 서비스는 **예정** 표시와 함께 안내 화면(`/services/{key}`)으로 연결된다.
서비스 추가는 `SOB-02`의 **사용 서비스** 항목과 짝을 이룬다 — 그 서비스로 지정된 코드가 해당 관리 메뉴의 집계 대상이 된다.

#### 4.5 공통 목록·필터·검색 패턴 (S14)

전 화면이 아래 패턴을 공유한다. 각 화면 PRD는 **이 화면에만 있는 필터**만 기술한다.

| 패턴 | 동작 |
|---|---|
| **검색 입력** | 입력 즉시 목록이 걸러진다. 입력값이 있으면 `×`(지우기) 버튼이 나타나고, 누르면 검색어가 비워지며 전체 목록으로 복귀 |
| **칩 필터** | 클릭 = 단일 선택(토글 아님). 다시 전체를 보려면 `전체` 칩을 선택 |
| **좌·우 2단 구성** | 좌측 목록에서 항목을 선택하면 우측에 상세가 표시된다. 미선택 시 우측에 안내 문구 표시 |
| **페이지네이션** | `«`(처음) `‹`(이전) `›`(다음) `»`(마지막). 첫/마지막 페이지에서 해당 버튼 비활성 |
| **필터·검색 변경 시** | 목록은 1페이지로 되돌아간다 |
| **초기화 버튼** | 화면 데이터를 시드 상태로 되돌린다. 실행 전 확인창을 띄운다 |

#### 4.6 활동 로그 자동 기록

아래 행위는 사용자가 별도 조작하지 않아도 **`LOG-01`에 자동 기록**된다.

| 활동 종류 | 발생 화면 |
|---|---|
| 고객사 관리 | `MEM-02` |
| 코드 할당(등록) | `SOB-02` |
| 프로젝트 등록 | `PRJ-01` |
| 티켓 발급 | `TKT-04` · `TKT-02` · `TKT-05` |
| 교재 추가 | `PRJ-04` (신규) |
| 교재 작업 | `PRJ-04` (수정) |
| 로그인 · 로그아웃 | 로그인 화면 |

### 연결 화면

| 트리거 | 이동 | 설명 |
|---|---|---|
| 로그인 성공 | `DSH-01` | 기본 진입 화면 |
| 좌측 메뉴 선택 | 각 화면코드 | 4.3 표 참조 |
| 상단 계정 → 개인정보 수정 | (모달) | 저장 후 현재 화면 유지 |
| 로그아웃 | 로그인 화면 | |
| 비로그인 상태로 화면 접근 | 로그인 화면 | 로그인 후 요청 화면으로 이동 |

### 상태 구성

이 화면은 **14개 상태**로 나뉜다. 구분: 진입 · 분기 · 오류 · 성공 · 기본 · 모달 · 변형 · 권한 · 전환 · 확인창 · 패턴

| 상태 | Figma 프레임 | 제목 | 구분 |
|---|---|---|---|
| `S1` | `PRD-00:S01` | 로그인 · 기본 | 진입 |
| `S2` | `PRD-00:S02` | 로그인 · 회사 Google 계정(SSO) | 분기 |
| `S3` | `PRD-00:S03` | 로그인 · 회사 도메인 아님 | 오류 |
| `S4` | `PRD-00:S04` | 로그인 · Google 로그인 실패 | 오류 |
| `S5` | `PRD-00:S05` | 로그인 · 로컬 로그인 성공 | 성공 |
| `S6` | `PRD-00:S06` | 앱 셸 · 기본 (ADMIN) | 기본 |
| `S7` | `PRD-00:S07` | 앱 셸 · 계정 메뉴 열림 | 분기 |
| `S8` | `PRD-00:S08` | 개인정보 수정 모달 | 모달 |
| `S9` | `PRD-00:S09` | 앱 셸 · 사이드바 접힘(레일) | 변형 |
| `S10` | `PRD-00:S10` | 앱 셸 · STAFF 로그인 | 권한 |
| `S11` | `PRD-00:S11` | 로그아웃 전환 화면 | 전환 |
| `S12` | `PRD-00:S12` | 공통 저장 실패 · 상단 알림 | 오류 |
| `S13` | `PRD-00:S13` | 삭제 확인창 · 이름 입력 (공통 패턴) | 확인창 |
| `S14` | `PRD-00:S14` | 공통 목록 · 필터 · 검색 패턴 | 패턴 |

---

## 3. 상태별 변화

### PRD-00: S01 로그인 · 기본

*화면 정의서 상태 `S1`*

**구분** — 진입

미로그인 상태로 화면 경로에 접근하면 이 화면으로 이동한다. PRD §4.1 · §2. SSO 미설정 환경의 **개발용 로컬 로그인** 형태이며, SSO 설정 시 S2로 대체된다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 이메일 입력 | 입력 · Enter | 제출 | 회사 도메인 계정 **name@neolab.net** 형식 |
| 비밀번호 입력 | 입력 · Enter | 제출 | 초기값 = 이메일. Enter로 제출 |
| 로그인 계정 기억 | 체크 토글 | — | 체크 시 다음 진입에 이메일 자동 채움 |
| [로그인] | 클릭 | `DSH-01` | 성공 시 **{이름}({역할})으로 로그인** 인라인 표시 후 이동 |
| (비로그인 경로 접근) | 자동 | 로그인 화면 | 로그인 후 요청 화면으로 이동 |

### PRD-00: S02 로그인 · 회사 Google 계정(SSO)

*화면 정의서 상태 `S2`*

**구분** — 분기

SSO 자격증명이 설정된 환경. PRD §4.1 — 회사 Google Workspace 계정으로만 로그인한다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| [회사 Google 계정으로 로그인] | 클릭 | Google 인증 → `DSH-01` | @neolab.net 계정만 허용. 명단에 없어도 **STAFF로 자동 등록**(승인 불필요) |
| (인증 취소) | 자동 | S1 유지 | 세션당 1회만 자동 로그인 시도 — 취소 루프 방지 |

### PRD-00: S03 로그인 · 회사 도메인 아님

*화면 정의서 상태 `S3`*

**구분** — 오류

PRD §5.1 — 회사 도메인이 아닌 계정으로 인증한 경우. 인라인 배너로 노출한다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 오류 배너 | 표시 | — | **회사 계정(@neolab.net)만 로그인할 수 있습니다.** |
| [회사 Google 계정으로 로그인] | 클릭 | 재시도 | 다른 계정 선택 가능 |

### PRD-00: S04 로그인 · Google 로그인 실패

*화면 정의서 상태 `S4`*

**구분** — 오류

PRD §5.1 — 인증 과정에서 오류가 발생한 경우.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 오류 배너 | 표시 | — | **Google 로그인 중 오류가 발생했습니다. 다시 시도하세요.** |
| [회사 Google 계정으로 로그인] | 클릭 | 재시도 | — |

### PRD-00: S05 로그인 · 로컬 로그인 성공

*화면 정의서 상태 `S5`*

**구분** — 성공

PRD §5.1 — 로컬 로그인 성공 시 인라인 메시지를 표시하고 곧바로 `DSH-01`로 이동한다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 인라인 메시지 | 표시 | `DSH-01` | **{이름}({역할})으로 로그인** |
| 활동 로그 | 자동 기록 | `LOG-01` | PRD §4.5 — 로그인 · 로그아웃은 자동 기록 |

### PRD-00: S06 앱 셸 · 기본 (ADMIN)

*화면 정의서 상태 `S6`*

**구분** — 기본

PRD §4.3 — 좌측 메뉴 · 상단바. ADMIN은 **활동 로그(LOG-01)** 메뉴가 보인다. CasterN 서비스 관리는 기본 **펼침(▾)**. 본문은 예시로 `DSH-01`을 실었다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 좌측 메뉴 항목 | 클릭 | 각 화면코드 | PRD §4.4 표 — 대시보드/SOBP 맵/코드 프로젝트/N Key 발급·계정 발급·발급 목록/편집 프로젝트/PUI/고객사/활동 로그/Ncode 정보 |
| [CasterN 서비스 관리] | 클릭 | 하위 접기 ⇄ 펼치기 | **화면 이동 없음**. 표식 ▾(펼침) / ▸(접힘) |
| «(메뉴 접기) | 클릭 | S9 레일 상태 | 접은 상태에서도 각 메뉴로 이동 가능 |
| 🔔 알림 | 클릭 | (미정) | §7 미결 |
| 계정 영역 | 클릭 | S7 계정 메뉴 | — |

### PRD-00: S07 앱 셸 · 계정 메뉴 열림

*화면 정의서 상태 `S7`*

**구분** — 분기

PRD §4.2 · §6 — 상단 계정을 누르면 개인정보수정 / 로그아웃 메뉴가 열린다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| [개인정보수정] | 클릭 | S8 모달 | 현재 화면 위에 모달로 열린다 |
| [로그아웃] | 클릭 | S11 전환 → 로그인 화면 | 활동 로그에 **로그아웃** 자동 기록 |
| 바깥 영역 | 클릭 | S6 복귀 | 메뉴 닫힘 |

### PRD-00: S08 개인정보 수정 모달

*화면 정의서 상태 `S8`*

**구분** — 모달

PRD §4.2 — 이메일은 **읽기 전용**. 새 비밀번호는 **변경할 때만** 입력하며 비워두면 기존 비밀번호가 유지된다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 이름 | 입력 (필수) | — | 표시 이름 |
| 이메일 | 읽기 전용 | — | 변경 불가 |
| 새 비밀번호 | 입력 (선택) | — | 비워두면 기존 비밀번호 유지 |
| [저장] | 클릭 | 모달 닫힘 · **직전 화면 유지** | 화면 이동 없음 (PRD §4.2 · §6) |
| [취소] · [✕] · 바깥 클릭 | 클릭 | 모달 닫힘 | 변경 폐기 |

### PRD-00: S09 앱 셸 · 사이드바 접힘(레일)

*화면 정의서 상태 `S9`*

**구분** — 변형

PRD §4.3 — 사이드바 전체 접기. 접은 상태에서도 각 메뉴로 이동할 수 있다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| »(메뉴 펼치기) | 클릭 | S6 복귀 | — |
| 레일 아이콘 | 클릭 | 각 화면코드 | 접힌 상태에서도 이동 가능 |

### PRD-00: S10 앱 셸 · STAFF 로그인

*화면 정의서 상태 `S10`*

**구분** — 권한

PRD §3.1 — STAFF는 **활동 로그(LOG-01)**가 메뉴에 노출되지 않는다. CasterN 하위를 접은 상태(▸)도 함께 보인다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 활동 로그 | — | 메뉴 없음 | ADMIN만 노출 (PRD §3.1) |
| [CasterN 서비스 관리] | 클릭 | 하위 펼침 | 접힘 표식 ▸ → 펼치면 ▾ |
| 그 외 메뉴 | 클릭 | 각 화면코드 | LOG-01 제외 전 화면 접근 가능 |

### PRD-00: S11 로그아웃 전환 화면

*화면 정의서 상태 `S11`*

**구분** — 전환

PRD §2 — 로그아웃 진행 중에는 화면 셸 대신 전환 화면을 보여준다(**비로그인 잔상 방지**).

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| (자동) | 완료 | 로그인 화면 | 활동 로그에 **로그아웃** 기록 |

### PRD-00: S12 공통 저장 실패 · 상단 알림

*화면 정의서 상태 `S12`*

**구분** — 오류

PRD §5.2 — 브라우저 저장 공간 부족. 전 화면 공통으로 상단 알림(토스트)으로 노출한다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 상단 알림 | 표시 | — | **⚠ 브라우저 저장 공간이 가득 차 저장하지 못했습니다. 다른 사이트 데이터를 정리한 뒤 다시 저장하세요.** |
| (저장 재시도) | 클릭 | 재시도 | 정리 후 동일 동작 재실행 |

### PRD-00: S13 삭제 확인창 · 이름 입력 (공통 패턴)

*화면 정의서 상태 `S13`*

**구분** — 확인창

PRD §5.3 — **고객사·프로젝트 삭제**는 대상 이름을 직접 입력해야 [삭제 확정]이 활성화된다. `MEM-01`·`PRJ-01`이 이 패턴을 공유한다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 이름 입력 | 입력 | [삭제 확정] 활성 | 대상 이름과 **정확히 일치**해야 활성 |
| [삭제 확정] | 클릭 (활성 시) | 삭제 · 목록 복귀 | 기본 상태는 **비활성** |
| [취소] · [✕] | 클릭 | 확인창 닫힘 | 삭제하지 않음 |

### PRD-00: S14 공통 목록 · 필터 · 검색 패턴

*화면 정의서 상태 `S14`*

**구분** — 패턴

PRD §4.4 — 전 화면이 공유하는 패턴. 각 화면 PRD는 **그 화면에만 있는 필터**만 기술한다. 이 보드는 화면이 아니라 **컴포넌트 규격**이다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 검색 입력 | 입력 | 즉시 필터링 | 입력값이 있으면 **✕**(지우기) 노출 |
| ✕ (지우기) | 클릭 | 전체 목록 복귀 | 검색어 비움 |
| 칩 필터 | 클릭 | 단일 선택 | **토글 아님**. 전체로 되돌리려면 [전체] 칩 선택 |
| 좌측 목록 항목 | 클릭 | 우측 상세 표시 | 미선택 시 우측에 안내 문구 |
| « ‹ › » | 클릭 | 페이지 이동 | 첫/마지막 페이지에서 해당 버튼 **비활성** |
| 필터·검색 변경 | 자동 | 1페이지로 복귀 | — |
| [초기화] | 클릭 | 시드 상태로 복귀 | 실행 전 **확인창** |
