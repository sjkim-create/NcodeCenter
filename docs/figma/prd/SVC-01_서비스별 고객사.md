# SVC-01 · 서비스별 고객사 (폼솔루션 서비스 관리)

**화면 ID**: SVC-01

> 원본 — 화면 정의서 `docs/screens/` · PRD `docs/prd/SVC-01_서비스별 고객사.md`
> 화면 구성·상태는 화면 정의서에서, 기능 개요·흐름·정책·사용법·연결 화면·문서 이력은 PRD 에서 **자동으로 모아** 만든다. 이 파일을 직접 고치지 말 것.

---

## 2. 화면 구성

### 개요

**고객사 관리에서 지정한 사용 서비스**를 기준으로, 그 서비스가 쓰는 고객사를 모아 보는 **조회 전용** 목록 `PC-076`. casterN 은 `PRJ-02` 편집 프로젝트가 그 역할을 하므로, 이 화면은 **폼솔루션 등 나머지 서비스**를 맡는다. 폼솔루션은 서비스 개발 전이라 현재 **0곳**이며 화면은 빈 상태(S2)로 열린다. 고객사를 추가하거나 서비스를 바꾸는 일은 **여기서 하지 않는다** — `MEM-02` 고객사 등록·수정에서 한다.

### 기능 개요

**고객사 관리에서 지정한 사용 서비스**를 기준으로, 그 서비스가 쓰는 고객사를 모아 보는 **조회 전용** 목록이다 `PC-076`.

| 항목 | 내용 |
|---|---|
| 목적 | 서비스 담당자가 **자기 서비스의 고객사만** 골라 본다 |
| 대상 | **casterN 을 제외한** 서비스 — 현재는 **폼솔루션** |
| 기준 | `MEM-02` 고객사 등록·수정 ▸ **사용 서비스** `PC-076` |
| 성격 | **조회 전용** — 고객사 추가·서비스 변경을 여기서 하지 않는다 |

**casterN 은 이 화면을 쓰지 않는다.** 편집량·정산까지 함께 봐야 해서 `PRJ-02` 편집 프로젝트가 그 역할을 한다.

| 사용 서비스 | 그 고객사가 나타나는 화면 | 현재 |
|---|---|---|
| casterN | `PRJ-02` 편집 프로젝트 | 70곳 |
| 폼솔루션 | **이 화면** | 0곳 (서비스 개발 전) |
| *(지정 없음)* | 없음 — SDK 연동(코드만 할당) | 나머지 |

### 화면 흐름 (Flow)

```
사이드바 [폼솔루션 서비스 관리] ─▶ SVC-01 (/services/formsolution)
   │
   ├─ [코드 프로젝트] ─▶ PRJ-01 (그 고객사로 검색)
   ├─ [SOBP 맵] ──────▶ SOB-01
   └─ 지정된 고객사 없음 ─▶ 안내 → MEM-02 에서 사용 서비스 체크
```

### 정책

| 정책 | 이 화면에서의 적용 |
|---|---|
| `PC-076` 사용 서비스 = 고객사 속성 | 목록 기준은 **고객사의 사용 서비스**다. 좌표(SOBP)별 지정이 아니다 — 옛 기준 `PC-057` 은 좌표에 붙어 있어 실제 고객사와 어긋났다 |
| `PC-076` 지정 위치 | 바꾸려면 **`MEM-02` ▸ 사용 서비스** 로 간다. 이 화면과 SOBP 맵은 **읽기만** 한다 |
| 조회 전용 | 고객사를 새로 만들거나 코드를 할당하지 않는다 (`MEM-02` · `SOB-02` 담당) |
| casterN 분리 | casterN 고객사는 `PRJ-02` 가 맡는다 — 이 화면에 중복해서 싣지 않는다 |

### 사용법

#### 4.1 목록 (S1)

| 열 | 표시 |
|---|---|
| 고객사 | 사용 서비스에 이 서비스가 체크된 고객사 · **가나다순** |
| 할당 좌표 (S/O) | 코드 종류 배지 + **S · O** 칩 · **최대 6건**, 넘으면 `외 {n}건` |
| 코드 프로젝트 | 그 고객사의 코드 프로젝트 건수 |
| 발급 규모 | 발급 코드 수 합계 |
| (작업) | **[코드 프로젝트]** → `PRJ-01` · **[SOBP 맵]** → `SOB-01` |

머리말에 **{n}곳** 배지와 *"고객사 관리에서 사용 서비스 = {서비스} 로 지정한 고객사입니다"* 안내가 붙는다.

#### 4.2 지정된 고객사 없음 (S2)

폼솔루션은 서비스 개발 전이라 **0곳**이다. 빈 카드에서 어디서 지정하는지 알려 준다.

> **아직 이 서비스로 지정된 고객사가 없습니다.**
> **고객사 관리 ▸ 사용 서비스** 에서 **폼솔루션** 를 체크하면 여기에 나옵니다 `PC-076`.

#### 4.3 casterN 고객사는 여기 나오지 않는다 (S3)

casterN 으로 지정된 고객사는 이 목록이 아니라 **[편집 프로젝트](`PRJ-02`)** 에서 다룬다 — 편집량·정산까지 함께 봐야 하기 때문이다.

| 고객사의 사용 서비스 | 나타나는 화면 |
|---|---|
| casterN | `PRJ-02` 편집 프로젝트 (70곳) |
| 폼솔루션 | **이 화면** (0곳) |
| *(지정 없음)* | 어느 화면에도 나오지 않는다 — SDK 연동(코드만 할당) |

### 연결 화면

| 화면 | 관계 |
|---|---|
| `MEM-02` 고객사 등록·수정 | **사용 서비스를 지정하는 곳** — 이 목록의 기준 `PC-076` |
| `PRJ-02` 편집 프로젝트 | casterN 고객사를 맡는 짝 화면 |
| `PRJ-01` 코드 프로젝트 | [코드 프로젝트] 이동 |
| `SOB-01` SOBP 맵 | [SOBP 맵] 이동 |

### 문서 이력

| 버전 | 날짜 | 수정자 | 변경 내용 |
|---|---|---|---|
| v1.0 | 2026-09-02 | 서비스기획팀 | 최초 작성 — 2026-09-01 신설된 화면(`PC-057`)의 정의서를 뒤늦게 채운다 `PC-079`. 고객사 기준은 이미 `PC-076` 으로 바뀐 상태를 반영 |

### 상태 구성

이 화면은 **3개 상태**로 나뉜다. 구분: 기본 · 빈 상태 · 분기

| 상태 | Figma 프레임 | 제목 | 구분 |
|---|---|---|---|
| `S1` | `SVC-01:S01` | 기본 — 서비스별 고객사 목록 | 기본 |
| `S2` | `SVC-01:S02` | 지정된 고객사 없음 | 빈 상태 |
| `S3` | `SVC-01:S03` | casterN 은 이 화면을 쓰지 않는다 | 분기 |

---

## 3. 상태별 변화

### SVC-01: S01 기본 — 서비스별 고객사 목록

*화면 정의서 상태 `S1`*

**구분** — 기본

좌측 메뉴 **[폼솔루션 서비스 관리]** 로 진입한다(`/services/{key}`). **[고객사 관리]에서 사용 서비스로 지정한 고객사**만 모아 보여 준다 `PC-076` — 이전에는 SOBP 맵의 좌표별 지정을 기준으로 삼았다 `PC-057`. **조회 전용**이며 여기서 고객사를 추가하거나 서비스를 바꾸지 않는다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 고객사 | — | — | 사용 서비스에 이 서비스가 체크된 고객사 · **가나다순** |
| 할당 좌표 (S/O) | — | — | 그 고객사가 가진 좌표 · 코드 종류 배지 + S · O 칩 · **최대 6건** 뒤는 **외 {n}건** |
| 코드 프로젝트 | — | — | 그 고객사의 코드 프로젝트 건수 |
| 발급 규모 | — | — | 발급 코드 수 합계 |
| [코드 프로젝트] | 클릭 | `PRJ-01` | 그 고객사로 검색된 목록으로 이동 |
| [SOBP 맵] | 클릭 | `SOB-01` | 좌표 지도로 이동 |
| 사용 서비스 변경 | — | `MEM-02` | 이 화면에서는 바꾸지 않는다 `PC-076` |

### SVC-01: S02 지정된 고객사 없음

*화면 정의서 상태 `S2`*

**구분** — 빈 상태

폼솔루션은 **서비스 개발 전**이라 현재 지정된 고객사가 **0곳**이다. 어디서 지정하는지 그 자리에서 알려 준다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| 빈 카드 | 표시 | — | **아직 이 서비스로 지정된 고객사가 없습니다.** |
| 보조 안내 | 표시 | `MEM-02` | **고객사 관리 ▸ 사용 서비스** 에서 **폼솔루션** 을 체크하면 여기에 나옵니다 |
| 건수 | 자동 | **0곳** | — |

### SVC-01: S03 casterN 은 이 화면을 쓰지 않는다

*화면 정의서 상태 `S3`*

**구분** — 분기

casterN 으로 지정된 고객사는 이 목록이 아니라 **[편집 프로젝트](`PRJ-02`)** 에서 다룬다 — 편집량·정산까지 함께 봐야 하기 때문이다. 이 화면은 **편집 외 서비스**(폼솔루션 등)를 맡는다.

| 요소 | 액션 | 결과 · 이동 | 메시지 · 비고 |
|---|---|---|---|
| casterN 고객사 | — | `PRJ-02` | 편집 프로젝트 목록에서 본다 (70곳) |
| 폼솔루션 고객사 | — | 이 화면 | 현재 0곳 |
| 아무 서비스도 없음 | — | **어느 화면에도 안 나온다** | SDK 연동(코드만 할당) — 코드만 받아 직접 연동하는 고객사 `PC-076` |
