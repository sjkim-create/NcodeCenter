# TKT-02 · 계정 등록·수정

> 문서번호 `NcodeCenter_001_PRD_TKT02` · 공통 규칙은 [PRD-00](PRD-00_공통.md) · 상위 화면 [TKT-01](TKT-01_계정%20발급.md)
> 화면 정의서 [TKT-02_계정 등록·수정.html](../screens/TKT-02_계정%20등록·수정.html) — 아래 표의 **화면** 열이 그 파일의 상태 번호다.

## 0. 문서 이력

| 버전 | 날짜 | 수정자 | 변경 내용 |
|---|---|---|---|
| v1.0 | 2026-08-28 | 서비스기획팀 | 최초 작성 `PC-031` — 목록에서 분리된 등록(3단계)·상세수정. CasterN 권한 7종, App Key 발급 선택화 |
| v2.0 | 2026-08-29 | 서비스기획팀 | **사용처 중복 선택**으로 변경 — 서비스별 조건을 **탭**으로 분리. **App Key 발급을 CasterN 탭 안으로** 이동(③단계 폐기 · 등록 3단계 → 2단계). 사용처에서 CasterN 을 빼면 **App Key 연동 끊김** 규칙 신설. 화면코드 `TKT-06` → `TKT-02` |
| v1.4 | 2026-08-31 | 서비스기획팀 | **App Key 규칙 정리** `PC-050` — 계정당 **1개**, 그 계정의 **사용처 전체에 공통**(CasterN 전용·연동 끊김 폐지). 발급 시 **Book Start · Book Volume(권수)** 를 지정하고, App Key 단계를 사용처 탭 밖 **③ 단계**로 뺀다 |
| v1.5 | 2026-08-31 | 서비스기획팀 | **영역 번호 삭제 · 묶음 구성** `PC-052` — ①②③ 원형 번호를 없애고 카드 묶음으로 정리(`TKT-05` 와 같은 결). App Key 는 **SOBP 맵에서 발급된 S/O** 를 골라 **Start Book · Book 볼륨** 을 지정하는 순서로 재구성 |
| v1.6 | 2026-08-31 | 서비스기획팀 | **사용처 자동 연동** `PC-057` — 고객사를 고르면 SOBP 맵에서 지정한 사용 서비스대로 사용처가 체크되고 그 서비스의 조건만 입력한다 |
| v1.7 | 2026-08-31 | 서비스기획팀 | **CasterN 권한에서 [App 페이지 설정] 삭제** `PC-058` — 권한 7종 → **6종** |
| v1.8 | 2026-09-01 | 서비스기획팀 | **계정 : 키 1:1 매핑** `PC-059` — 계정 상세에 그 계정의 **Key 정보(App Key · N Key)** 를 항목 표로 보여 준다. App Key 발급에 **Start Page · Page 볼륨**을 더해 **고객사 S/O 안에서 B·P 영역을 계정마다 다르게** 잡는다 |
| v1.9 | 2026-09-01 | 서비스기획팀 | **App Key 발급의 Key 정보 항목을 N Key 와 맞춤** `PC-060` — 자동·고정 항목(Section·Owner / PatternType / TicketType / TicketVersion)을 화면에 보여 주고, 파라미터 이름도 `IssuedTime`·`ValidUntilTime` 로 통일 |
| v2.1 | 2026-09-01 | 서비스기획팀 | **탭 3개로 구성** `PC-062` — **계정 정보 · 사용 서비스 및 권한 · App Key 발급**. 「사용처」 표기를 **사용 서비스**로 통일 |
| v2.2 | 2026-09-01 | 서비스기획팀 | **App Key 발급에 Code Type 직접 선택** `PC-064` (PDS2·PDS3·PDS4·OID) · 등록 화면 **[목록] 버튼을 아래로** · Key 정보 항목명 띄어쓰기 통일 |
| v2.3 | 2026-09-01 | 서비스기획팀 | **App Key 생성 규칙** `PC-066` — `ncc_live_…` 형식을 폐기하고 **영문·숫자 29자 난수**로 발급한다 |
| v2.4 | 2026-09-01 | 서비스기획팀 | **Key 정보를 스크롤 없이 전부 표시** `PC-067` · 목록에서 `?tab=` 으로 첫 탭 지정 |
| v2.5 | 2026-09-01 | 서비스기획팀 | **계정 상세에서 N Key 영역 삭제** `PC-068` — 이 화면은 App Key 만 다룬다. N Key 는 `TKT-03` [N Key 관리] 에서 발급·조회 |
| v2.6 | 2026-09-01 | 서비스기획팀 | **화면명·머리말·버튼 정리** `PC-069` — 화면명 **계정 및 App Key 등록 / 계정 및 App Key 상세 수정**, 제목은 상단 한 곳만 두고 카드에는 **고객사명을 크게**, [목록]·[계정 삭제]·[저장]을 **화면 맨 아래**로 |
| v2.7 | 2026-09-01 | 서비스기획팀 | **App Key 발급 내역을 [Key 정보] 위로** · 설명글을 **제목 툴팁**으로 정리 `PC-070` — 「고객사 별 계정 개수 제한 없음 · App Key 는 계정당 1개 발급」 |
| v2.8 | 2026-09-01 | 서비스기획팀 | **App Key 발급 폼 정리** `PC-071` — [함께 발급] 체크박스 폐지(탭을 열면 바로 입력·범위를 고르면 발급), **Code Type·만료일을 한 줄**로, **Section·Owner·Ticket Type·Ticket Version 은 한 줄 요약**으로. 「이미 발급됨」 안내도 제목 툴팁으로 |
| v2.9 | 2026-09-01 | 서비스기획팀 | **사용 서비스 및 권한 설명도 제목 툴팁으로** `PC-073` — 제목 옆에는 선택 개수만 남긴다 |

---

## 1. 기능 개요

`TKT-01` 계정 목록에서 열리는 **두 화면**이다.

| 화면 | 주소 | 하는 일 |
|---|---|---|
| **계정 등록** | `/tickets/account/new` | ① 계정 정보 → ② 사용처·권한 을 입력하고 **[계정 추가]** |
| **계정 상세·수정** | `/tickets/account/{email}` | ①② 를 고쳐 **[저장]**, 아래에서 이 계정의 **App Key 발급 내역**을 본다 |

| 항목 | 내용 |
|---|---|
| 권한 | STAFF / ADMIN |
| 진입 | `TKT-01` 의 **[＋ 계정 추가]** / **ID(email)·[상세]** |
| 선행 조건 | 고객사가 `MEM-01`에 등록. **App Key까지 발급하려면** `SOB-02`로 SOBP를 할당받았을 것 |

> **한 계정으로 여러 서비스를 쓸 수 있다.** 사용처는 중복 선택이며, 서비스마다 지정할 조건이 다르다.
> 화면은 **번호 없는 영역 묶음**(계정 정보 · App Key 발급 · 사용처·권한)으로 구성한다 `PC-052` — `TKT-05` 발급 상세와 같은 결이다.
>
> **App Key 는 계정당 1개**이고 그 계정의 **사용처 전체(CasterN·폼솔루션·SDK)에 공통**으로 쓰인다 `PC-050`. 발급은 선택 사항이라 계정만 등록해 두고 나중에 붙일 수 있다. 발급할 때 **Book Start · Book Volume(권수)** 를 지정한다.

---

## 2. 화면 흐름 (Flow)

```
TKT-01 계정 목록
   ├─ [＋ 계정 추가] ─▶ TKT-02 등록
   │      ① 계정 정보(고객사·NAME·ID·PWD·ADDR·HOMEPAGE)
   │      ② 사용처 · 권한  ─ 탭: CasterN / 폼솔루션 / SDK 연동
   │            └ CasterN 탭 ─ 사용자 권한 6종
   │      └ ③ App Key 발급(선택) ─▶ 할당 SOBP 범위 + Book Start · Book Volume + 만료일 `PC-050`
   │                                              └─(범위 없음)─▶ SOB-02 로 이동
   │      └─ [계정 추가] ─▶ TKT-01 목록에 노출 (+체크 시 App Key 동시 발급)
   │
   └─ ID(email)·[상세] ─▶ TKT-02 상세·수정
          ├─ [저장] ────────────────────▶ 반영 (App Key 는 사용처와 무관하게 유지)
          ├─ ③ [App Key 발급] ─────────▶ 키 생성 → TKT-03 · LOG-01 기록 (계정당 1개)
          ├─ 발급 내역 [키 삭제] ────────▶ 해당 App Key만 삭제
          └─ [계정 삭제] ─(확인창)───────▶ 계정 + 연동 App Key 삭제 → TKT-01
```

---

## 3. 정책

| 정책 | 이 화면에서의 적용 |
|---|---|
| `PC-031` 계정 화면 분리 · 권한 | 등록·수정을 목록에서 분리. 서비스별 조건을 계정마다 지정 |
| `PC-029` 고객사당 계정 수 | 제한 없음. ID(email)만 전체에서 **유일** |
| `PC-002` 내부 직원 전용 | 고객이 직접 만들지 않고 **직원이 대신 등록**한다 |
| `P-13` 사용 서비스 | 계정은 **선택한 사용처에서만 로그인**된다. 여러 서비스를 고르면 **한 계정으로 각 서비스에 로그인**하며, 각 서비스는 자기 계정만 관리·인증한다 |
| `P-05` 무겹침 | App Key는 **이미 할당된 SOBP 범위**에만 연결된다. 좌표(S/O)는 직접 타이핑하지 않고, 그 안에서 **Book Start · Book Volume** 만 지정한다 `PC-050` |
| `PC-050` App Key 단위 | **계정당 1개** · **사용처 전체 공통**. 사용처를 바꿔도 키는 유지된다(옛 「연동 끊김」 규칙 폐지). 범위를 바꾸려면 **키를 삭제한 뒤 다시 발급**한다 |
| `P-02` 코드 종류 N/G | PatternType은 선택한 범위에서 자동 결정 |
| 서비스별 조건 | 서비스마다 지정할 항목이 다르다. **현재 조건이 정의된 서비스는 CasterN 뿐**이고 나머지는 준비중이다 |

---

## 4. 사용법

### 4.1 ① 계정 정보

| 항목 | 필수 | 규칙 | 수정 화면 |
|---|---|---|---|
| 회사정보(고객사) | ✔ | `MEM-01` 등록 고객사. 고르면 **ADDR이 자동 입력**된다 | **잠금**(변경 불가) |
| NAME(담당자/사용자명) | — | | 수정 가능 |
| ID (EMAIL) | ✔ | **이메일 형식** · 이미 등록된 ID는 쓸 수 없다 | **잠금**(키 연동 기준값) |
| PWD | ✔ | 비밀번호를 요청하지 않는 고객사(SDK)는 **[임의 생성]** 으로 10자리 자동 생성 | 수정 가능 |
| ADDR(주소) | — | 고객사 주소 자동 입력 · 수정 가능 | 수정 가능 |
| HOMEPAGE | — | | 수정 가능 |

### 4.2 ② 사용처 · 권한 — 탭 구조

서비스마다 조건이 달라 **한 번에 한 서비스만** 보여준다. 선택한 서비스의 설정이 아래로 쌓이지 않는다.

**탭 바** — 서비스 3종이 항상 탭으로 있고, **사용처로 선택된 서비스에는 선택 표시**가 붙는다. 준비되지 않은 서비스에는 **준비중** 표기가 붙는다.

| 서비스 | 사용처 선택 | 조건 |
|---|---|---|
| **CasterN** | 가능 (기본 선택) | 사용자 권한 6종 `PC-058` |
| **폼솔루션** | 가능 | **준비중** — 조건 항목 없음 |
| **SDK 연동** | 가능 | **준비중** — 조건 항목 없음 |

**탭 패널** — 맨 위에 그 서비스의 **사용 여부** 체크가 있고, 켜져 있을 때만 아래 조건이 나온다.

| 상황 | 패널 내용 |
|---|---|
| 사용처로 선택 안 함 | *사용처로 선택하면 이 서비스의 조건을 설정할 수 있습니다.* |
| CasterN 선택 | 사용자 권한 6종 |
| 폼솔루션·SDK 선택 | **준비중** — *이 서비스의 권한·설정 항목은 아직 정의되지 않았습니다. 사용처 연동만 등록됩니다.* |

**CasterN 사용자 권한 6종** `PC-031` `PC-058`

| # | 권한 | 설명 |
|---|---|---|
| 1 | 프로젝트 생성 | 편집 프로젝트를 새로 만든다 |
| 2 | 심볼 편집 | 심볼(코드 영역) 편집 |
| 3 | 리소스 편집 | 음원·이미지 등 리소스 편집 |
| 4 | Ncode PDF 내보내기 | Ncode가 입혀진 PDF 출력 |
| 5 | NCP2 내보내기 | NCP2 파일 내보내기 |
| 6 | App용 패키지 내보내기 | App에서 쓰는 패키지 내보내기 |

| 동작 | 결과 |
|---|---|
| 권한 항목 선택 | 개별 on/off |
| [모두 선택] / [모두 해제] | 7개 일괄 · 선택 수에 따라 라벨이 바뀐다 |
| 선택 수 표시 | **선택 {n} / 6** |
| 권한 0개 저장 | 허용. 목록에는 **미지정**으로 표시된다 |
| CasterN 을 새로 켬 | 권한이 **7종 모두 선택**된 상태로 시작한다 |

> 사용처에서 뺀 서비스의 조건은 **저장하지 않는다.**

### 4.3 App Key 발급 — CasterN 탭 안 (선택)

App Key는 SOBP 범위를 편집툴에 연동하는 키라 **CasterN 사용처에만 해당**한다. 그래서 별도 단계가 아니라 **CasterN 탭의 권한 아래**에 있다. 폼솔루션·SDK 탭에는 나오지 않는다.

| 화면 | 동작 |
|---|---|
| **등록** | **[이 계정에 App Key도 함께 발급합니다]** 체크 → 할당된 SOBP 범위 + 만료일 |
| **상세·수정** | 범위·만료일을 고르고 **[App Key 발급]**. 저장된 사용처에 CasterN 이 있을 때만 발급할 수 있다 |

| 항목 | 규칙 |
|---|---|
| 할당된 SOBP 범위 | 접힘/펼침 목록에서 하나 선택. 순번 + S · O · B · P 값. 범위 없으면 `SOB-02`에서 할당 |
| 만료일(기간) | 기본 **무제한**(날짜 잠김). 체크를 풀면 달력으로 지정 |
| 미체크 시 | **계정만** 등록된다. 키는 상세에서 나중에 발급 |
| CasterN 미선택 | *App Key 는 CasterN 사용처에만 발급됩니다.* 안내만 나온다 |

### 4.4 상세 · 수정

| 영역 | 내용 |
|---|---|
| 머리말 | **계정 상세 · 수정** + ID(email) + 고객사 |
| ① 계정 정보 | ID·고객사는 잠금, 나머지는 수정 |
| ② 사용처 · 권한 | 탭 구조 동일. CasterN 탭에 App Key 발급이 붙는다 |
| ③ App Key 발급 내역 | 이 계정의 키 목록 — 키 앞부분 · 코드 범위 · 사용처 · 유효 기한 · 생성 일시 · **[키 삭제]**. **조회·삭제만** 하고 발급은 ② CasterN 탭에서 한다 |
| [저장] | 계정 정보·사용처·조건을 함께 저장 |
| [계정 삭제] | 확인창 → 계정 + 연동 App Key 삭제 → `TKT-01` |

**App Key 발급 결과** — 발급하면 **계정 ID · PWD · App Key** 가 나오고 **[전체 복사]** 로 세 값을 한 번에 복사한다. 키는 아래 내역에 줄로 추가된다.

### 4.5 사용처 변경과 App Key 연동

사용처에서 **CasterN 을 빼면** 그 계정의 App Key는 **연동이 끊긴다.** 키를 지우지는 않는다.

| 상황 | 처리 |
|---|---|
| 저장 시 | 끊기는 키가 있으면 확인을 받는다 |
| 끊긴 키 | 발급 내역에 **연동 끊김** 으로 표시된다. 로그인에는 쓸 수 없다 |
| 다시 CasterN 선택 | 연동이 되살아난다 — 키를 다시 발급할 필요가 없다 |
| 키 삭제 | 담당자가 발급 내역에서 직접 판단해 지운다 |

### 4.6 로그인 허용 판정

```
그 서비스로 로그인 가능 = 계정 사용처에 그 서비스가 있다
                        AND 그 서비스용 App Key 가 있다

예) 계정 사용처 = CasterN · 폼솔루션
    보유 App Key = CasterN 용 1개

    CasterN  로그인 → 사용처 있음 + 키 있음      → 가능
    폼솔루션  로그인 → 사용처 있음 + 키 없음      → 불가
    SDK      로그인 → 사용처 없음               → 불가

예) 위 계정에서 사용처 CasterN 을 제거하면
    CasterN  로그인 → 사용처 없음 → 불가 (키는 남아 있으나 '연동 끊김')
```

---

## 5. 상태별 변화 · 메시지

### 5.1 상태

| 상태 | 화면 | 화면 번호 |
|---|---|---|
| 등록 진입 | 고객사 미선택 · 사용처 **CasterN** 기본 · 권한 7/7 · App Key 체크 해제 | S1 |
| 사용처 중복 선택 | 여러 서비스에 선택 표시. 탭을 바꿔도 아래로 쌓이지 않는다 | S2 |
| CasterN 탭 — 권한 개별 | 예) **선택 3 / 7** · 목록에는 **3 / 7** | S3 |
| 폼솔루션 · SDK 탭 | **준비중** 안내. 사용처 연동만 등록된다 | S4 |
| 사용처로 선택 안 한 탭 | *사용처로 선택하면 이 서비스의 조건을 설정할 수 있습니다.* | S4 |
| App Key 함께 발급 | CasterN 탭 안에 SOBP 범위 + 만료일 | S5 |
| CasterN 미선택 — App Key | *App Key 는 CasterN 사용처에만 발급됩니다.* | S6 |
| 등록 검증 실패 | 버튼 아래 한 줄 | S7 |
| 상세·수정 | ID·고객사 잠금 · App Key 발급 내역 표시 | S8 |
| 상세 — 키 발급 완료 | 발급 결과 + 내역에 추가 | S9 |
| CasterN 제거 저장 | 연동 끊김 확인창 → 내역에 **연동 끊김** 표시 | S10 |
| 계정 삭제 확인창 | 계정 + 연동 키 삭제 | S11 |

### 5.2 메시지 — 인라인

검사는 아래 순서로 진행하고 하나라도 걸리면 그 자리에서 멈춘다.

| 순서 | 상황 | 문구 |
|---|---|---|
| ① | 회사 미선택 | **회사(고객사)를 선택하세요.** |
| ② | ID 형식 오류 | **계정 ID는 이메일 형식이어야 합니다.** |
| ③ | 비밀번호 없음 | **비밀번호가 필요합니다. (요청 없으면 [임의 생성])** |
| ④ | 사용처 미선택 | **사용처(연동 서비스)를 1개 이상 선택하세요.** |
| ⑤ | 범위 미선택 | **할당된 SOBP 범위를 선택하세요.** (App Key 함께 발급일 때만) |
| ⑥ | ID 중복 | **이미 등록된 ID(email)입니다.** |

| 성공 | 문구 | 화면 |
|---|---|---|
| 저장 | **계정 정보가 저장되었습니다.** | S8 |
| App Key 발급 | **App Key 발급 완료 — 계정과 연동되어 서비스 DB에 등록되었습니다.** | S9 |
| 전체 복사 | **계정·키 정보가 복사되었습니다.** | S9 |

### 5.3 확인창

| 상황 | 문구 | 화면 |
|---|---|---|
| CasterN 제거 저장 | **사용처에서 빠지는 서비스로 발급된 App Key {n}개는 연동이 끊깁니다.**(키는 지워지지 않고 「연동 끊김」으로 남습니다) **저장할까요?** | S10 |
| 계정 삭제 | **이 계정과 연동 App Key를 삭제할까요?** | S11 |

---

## 6. 연결 화면

| 트리거 | 이동 | 설명 |
|---|---|---|
| [목록] · [취소] · 등록 완료 | `TKT-01` | 계정 목록 |
| 할당 범위 없음 | `SOB-02` | 코드 할당 후 재시도 |
| 미등록 고객사 | `MEM-02` | 고객사 등록 후 재시도 |
| App Key 발급(자동) | `TKT-03` · `LOG-01` | 발급 이력·활동 로그 자동 기록 |

---

## 7. 미결 · 협의

- [ ] **권한 기본값** — 현재 CasterN 을 켜면 7종 모두 선택. 최소 권한으로 시작할지 확정 필요
- [ ] **권한 연동 시점** — CasterN이 계정 권한을 조회하는 규격·갱신 주기 (개발팀 협의)
- [ ] **비밀번호 전달 방식** — 현재 화면 표시·복사
- [ ] **폼솔루션 · SDK 조건 정의** — 두 서비스의 권한·설정 항목이 정해지면 각 탭에 채운다
- [ ] 계정 1개에 App Key 여러 개를 허용하는 현재 방식의 **상한** 필요 여부
- [ ] 연동이 끊긴 App Key를 **일정 기간 뒤 자동 정리**할지
