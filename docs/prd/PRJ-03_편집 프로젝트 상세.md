# PRJ-03 · 편집 프로젝트 상세 (편집 프로젝트 수정)

> 문서번호 `NcodeCenter_001_PRD_PRJ03` · 공통 규칙은 [PRD-00](PRD-00_공통.md) · 부모 화면 [PRJ-02](PRJ-02_편집%20프로젝트%20목록.md)
> 화면 정의서 [PRJ-03_편집 프로젝트 상세.html](../screens/PRJ-03_편집%20프로젝트%20상세.html) — 아래 표의 **화면** 열이 그 파일의 상태 번호다.

## 0. 문서 이력

| 버전 | 날짜 | 수정자 | 변경 내용 |
|---|---|---|---|
| v1.0 | 2026-08-25 | 서비스기획팀 | 최초 작성 |
| v2.1 | 2026-09-03 | 서비스기획팀 | **정산 공식 정정** `PC-085` `PC-091` — 적용비는 **`Total Page` 가 아니라 [Ncode 적용] 수량**으로 계산한다(타입이 정하는 **활성 펜** 기준). `Total Page` 는 **목록의 페이지 항목** 표시용 `PC-089` |
| v2.0 | 2026-08-29 | 서비스기획팀 | **교재 편집이 모달 → 별도 페이지**로 바뀜 (`PRJ-04`) — [＋ 교재(책) 추가]·교재 행 클릭이 페이지 이동이 된다. 페이지네이션 건수 선택에 **25건** 추가 |
| v1.1 | 2026-08-27 | 서비스기획팀 | 구현 화면 기준 정정 — **정렬 3단**(교재명·발급일), **코드 필터 잠금**, **필터 해제** 버튼, 페이지네이션 **건수 선택**, **메모 열 = 업무 원장 건수**, 정산 열 할인 표기, **결과 없음** 상태 추가, 화면 상태 번호(S1~S12) 연결 | 계산식 팝오버 **4블록·스크롤** 명세, 전사 기본 단가 **500/1,000** 명시.
| v1.2 | 2026-08-28 | 서비스기획팀 | 교재 표의 **코드 종류 열·필터를 PDS3·PDS2·PDS4·OID 4종**으로 표시 `PC-037` (기존 N/G 원시값 표기 폐지) |
| v1.3 | 2026-08-28 | 서비스기획팀 | **전체 고객사 보기** 지원 `PC-040` — 모든 고객사의 교재를 한 표로 보여줄 때 **고객사 열·고객사 필터**가 추가되고 **조회 전용**(추가·수정·초기화 숨김)으로 동작한다 |
| v1.4 | 2026-08-31 | 서비스기획팀 | **S/O/B 열에 Book 필터 추가** `PC-052` — B 전체 · **B 낮은순 · B 높은순 · B 직접 입력**(번호로 찾기) |
| v1.5 | 2026-08-31 | 서비스기획팀 | **B 직접 입력을 셀렉트 자리에서** `PC-054` — [B 직접 입력] 을 고르면 그 자리가 번호 입력칸으로 바뀐다(✕ 로 전체 복귀) |
| v1.6 | 2026-08-31 | 서비스기획팀 | 상단 이동 버튼 표기 정리 `PC-055` — **[← 편집 프로젝트] → [목록]** |
| v1.7 | 2026-08-31 | 서비스기획팀 | 교재 목록 화면의 상단 이동 버튼은 **[편집 프로젝트]** 로 둔다 `PC-056` — [목록] 은 교재 등록·상세에서 그 고객사 교재 목록으로 돌아가는 버튼이다 |
| v1.8 | 2026-08-31 | 서비스기획팀 | **B 필터를 직접 입력 콤보박스로** `PC-057` — 입력칸에 번호를 바로 치고, 목록에서 낮은순·높은순을 고른다(별도 [직접 입력] 선택 없음) |

---

## 1. 기능 개요

선택한 편집 고객사(Owner)의 **교재(책) 편집 내역과 정산(청구액)** 을 관리하는 화면. 편집량(페이지·심볼)이 곧 청구 근거이므로, **편집팀의 작업 관리 화면이자 재무팀의 정산 근거 화면**이다.

| 항목 | 내용 |
|---|---|
| 권한 | STAFF / ADMIN |
| 진입 | `PRJ-02`에서 고객사 선택 → **같은 화면 우측에 표시**(목록과 동시 노출) |
| 대상 단위 | 고객사 + Owner |
| 편집 단위 | 교재(책) 1권 = 목록 1행 → 등록·수정은 `PRJ-04` |

> 좌측 목록과 함께 보이므로 **다른 고객사로 바로 전환**할 수 있고, `PRJ-02`의 코드 종류 필터가 이 상세에도 그대로 적용된다.

---

## 2. 화면 흐름 (Flow)

```
PRJ-02 좌측 고객사 선택 ─▶ PRJ-03 (우측 상세)
   ├─ [＋ 교재(책) 추가] ───▶ PRJ-04 (등록 모드)
   ├─ 교재 행 클릭 ─────────▶ PRJ-04 (수정 모드)
   ├─ [계산식 · 단가 근거] ─▶ 청구액 산출 근거 펼침 → [닫기]
   ├─ [초기화] ─ 확인 ─────▶ 엑셀 시드 상태로 복원
   └─ 단가를 바꾸려면 ──────▶ MEM-01 → MEM-02 (고객사 단가)
```

---

## 3. 정책

| 정책 | 이 화면에서의 적용 |
|---|---|
| `P-16` 정산 = 항목별 단가표 | 청구액 = **적용비([Ncode 적용] 수량 × 적용단가) + 편집·기능비(항목별 수량 × 단가) − 할인** `PC-085`. 적용비는 **타입이 정하는 활성 펜**의 [Ncode 적용] 만 센다 `PC-091` — `Total Page` 는 계산에 쓰지 않는다 `PC-089` |
| 단가 3단 구조 | **기본 단가 → 고객사 단가(`MEM-02`) → 교재별 단가·할인(`PRJ-04`)** 순으로 우선 적용된다 |
| `PC-027` 단가 적용 시점 | 각 교재는 **등록 시점의 고객사 단가로 고정**되어 정산된다. 고객사 단가가 바뀌어도 **기존 교재의 청구액은 변하지 않는다** |
| `P-14` 편집 = casterN | 이 화면의 데이터가 casterN 서비스의 편집 실적이다 |
| `P-12` 공통(커먼) 코드 | 공유 코드 Owner의 교재는 **사용 고객사**를 반드시 지정해야 한다(`PRJ-04`) |
| `P-01` SOBP | 교재는 S/O/B 좌표에 매인다. 상세에서 좌표를 임의로 바꾸지 않는다 |

---

## 4. 사용법

### 4.1 상단 (S1)

고객사명 · **owner {번호}** · 보유 코드 종류(N/G) 배지가 표시되고, 우측에 **[초기화]**, **[＋ 교재(책) 추가]** 가 있다.

### 4.2 요약 지표 (S1)

두 묶음으로 나뉜다 — 왼쪽 **편집량**(수량), 오른쪽 **정산**(금액).

**편집량**

| 지표 | 의미 |
|---|---|
| 교재(책) | 교재 수 (필터 적용 시 **교재(책)·필터**로 표기) |
| TOTAL PAGE | PDF 편집 대상 페이지 합 (교재의 `Total Page` 합계 · **정산과 무관** `PC-089`) |
| 소리펜 심볼 합 / 필기펜 심볼 합 | 펜별 심볼 수 |
| 심볼 합계 | 소리펜 + 필기펜 |

**정산**

| 지표 | 의미 |
|---|---|
| 적용비 (Ncode 적용) | **[Ncode 적용] 수량 × 적용 단가** `PC-085` — 아래에 `{타입} {n}p × 단가` |
| 편집·기능비 | 심볼·기능 항목별 단가 합 — 아래에 `심볼·기능 {n} (항목별 단가)` |
| 청구액 | 기준가 − 할인 — 아래에 `할인 −{n}` 또는 `할인 없음` |

### 4.3 단가 기준 표시

지표 아래 한 줄에 **신규 교재에 적용될 현재 고객사 단가**가 배지로 표시된다 — *{고객사 단가|기본 단가} (신규 교재 기준) : 적용 - {n}원/페이지 · 편집(기본) - {n}원/심볼*. 고객사 전용 단가가 없으면 전사 기본 단가를 쓴다.

**목록의 청구액은 교재마다 고정된 단가로 계산된다** `PC-027`. 즉 고객사 단가를 올려도 이전에 등록한 교재의 금액은 그대로이고, 그 뒤에 등록하는 교재부터 새 단가가 반영된다.

| 표시 | 조건 | 화면 |
|---|---|---|
| **고객사 단가 (신규 교재 기준)** 배지 | 전용 단가 있음 · 옆에 `항목별 단가는 고객사 관리 참조` | S2 |
| **기본 단가 (신규 교재 기준)** 배지 | 전용 단가 없음 (툴팁: 고객사 전용 단가가 없어 전사 기본 단가로 계산합니다.) | S3 |
| **프로젝트 단가·할인 {n}건** 배지 | 고객사 단가와 다른 단가·할인이 걸린 교재가 있을 때 | S2 |
| **할인 −{금액} ({n}%)** 배지 | 할인 합계가 있을 때 (툴팁에 적용 교재 수·기준가) | S2 |
| **청구액 {금액}** | 항상 | S1 |

같은 줄 끝의 **`?`** 버튼을 누르면 **계산식 · 단가 근거**가 왼쪽 아래로 펼쳐진다. 내용이 길어 **세로로 스크롤**되며(최대 화면 높이의 70%), **4개 블록**으로 구성된다. (S4)

맨 위에 안내가 있다 — *단가는 3단계로 적용됩니다 — 기본 단가 → 고객사 단가(고객사 관리) → 프로젝트 단가·할인(교재 편집 수정).*

| 블록 | 내용 |
|---|---|
| **① 현재 집계 계산식** | `적용비용(페이지 {n} × {단가})` → `편집·기능비(심볼·기능 {n}, 항목별 단가 합)` → `기준가` → `할인({n}%, 적용 교재 {n}건)` → **청구액** 표. 아래에 **적용 단가 / 편집 단가**와 전용·기본 구분. 필터 중이면 제목 옆에 **(필터 {n}건 기준)**. 고객사 관리에 없는 고객사면 **※ 고객사 관리에 등록되지 않아 기본 단가로 계산됩니다.** |
| **② 소리펜 변경후 단가 (전사 기본값)** | 적용 **₩500**/페이지(변경전 300) · 편집 **₩1,000**/심볼(변경전 900). 전 고객사 공통이며 예외(잉글리시에그 편집 변경전 600, 트윈클 보드 단가)를 함께 적는다 |
| **③ 개별 견적서 (참고)** | 실제 견적 2건 — *2026-07-16 양지사 플래너 4종* / *2026-05-26 블루래빗 생생자연관찰*. 각 항목 단가와 합계(VAT 구분) |
| **④ 정의** | 적용 = PDF 1페이지 당 · 편집(기본) = **음원 재생 영역 1개 = 심볼**(mp3 개수와 다름) · 프롬프트 = 대화(포코로) 세션 1개 당 |

> **전사 기본 단가는 적용 ₩500/페이지 · 편집(기본) ₩1,000/심볼**이다.

### 4.4 목록 — 검색 · 필터 · 정렬

| 요소 | 동작 | 화면 |
|---|---|---|
| 검색 | **교재명 검색** (공유 코드 Owner면 **교재명·사용고객사 검색**). `×` 로 해제 | S8 |
| 머리글 아래 필터 | **상태 · (사용 고객사) · 코드 · 타입 · 편집방식** 5종을 각 열 아래 드롭다운으로 고른다 | S6 |
| 코드 필터 잠금 | `PRJ-02` 에서 코드 종류를 고르면 이 필터가 **그 값으로 고정·잠김** — *목록의 PDS 필터로 고정됨* | S6 |
| 정렬 | **교재명 · 발급일** 머리글 클릭 — **오름 → 내림 → 해제** 3단. 정렬 중인 열과 아닌 열을 구분해 표시한다 | S7 |
| 필터 해제 | 필터가 하나라도 걸리면 **필터 해제 ({보이는}/{전체})** 버튼이 나타난다 | S9 |
| 페이지네이션 | `전체 {n}건 중 {a}~{b} 표시` + **건수 선택**(25 / 50 / 100 / 200 / 500 / 전체 보기) + `«` `‹` 번호 `›` `»` + `{현재}/{전체}` | S10 |

### 4.5 목록 항목

No · 상태 · (사용 고객사) · 교재명 · 코드 · 타입 · S/O/B · 페이지 · 심볼 개수 · 편집방식 · 발급일 · 최종 수정일 · 메모 · ncp2 크기(byte) · 정산(청구액)

| 열 | 표시 |
|---|---|
| 교재명 | 교재명 아래에 **원본 파일명**이 아래 줄에 붙는다 |
| 코드 | **N** / **G** |
| S/O/B | S · O · B 칩 |
| 페이지 | `{시작}~{끝}` + 아래에 `{n}p` |
| 편집방식 | 쉼표로 이어진 편집 방식 목록 |
| **메모** | 그 교재의 **업무 원장(요청·처리·메모) 건수** 배지. 없으면 `0` |
| 정산 (청구액) | 청구액. 할인이 있으면 아래에 **~~기준가~~ −{n}%** |

**행을 클릭하면 `PRJ-04` 수정 모드**로 열린다. 폭이 좁으면 가로 스크롤된다.

---

### 4.9 S/O/B 열의 Book 필터 `PC-052` `PC-057`

교재가 많은 고객사에서 **Book 번호로 바로 찾기** 위한 필터다. 열 머리의 **콤보박스 하나**로 처리한다.

| 조작 | 결과 |
|---|---|
| 입력칸에 번호를 **바로 친다** | 그 Book 번호로 거른다 — **[직접 입력] 을 먼저 고를 필요가 없다** `PC-057` |
| 목록에서 **B 낮은순** | Book 오름차순 정렬 |
| 목록에서 **B 높은순** | Book 내림차순 정렬 |
| 비우면 | **B 전체** 로 돌아간다 |

> 입력과 선택을 한 칸에서 하므로(콤보박스), 번호를 아는 담당자는 **치고**, 훑어보는 담당자는 **고른다**.

---

## 5. 상태별 변화 · 메시지

### 5.1 상태

| 상태 | 화면 | 화면 번호 |
|---|---|---|
| 기본 | 헤더 + 지표 2묶음 + 단가 줄 + 목록 | S1 |
| 고객사 전용 단가 있음 | **고객사 단가 (신규 교재 기준)** 배지 · 프로젝트 단가·할인 건수 · 할인 합계 | S2 |
| 전용 단가 없음 | **기본 단가** 배지 (툴팁: 고객사 전용 단가가 없어 전사 기본 단가로 계산합니다.) | S3 |
| 계산식 펼침 | `?` 버튼 → 산출 근거 팝오버 | S4 |
| 공유 코드 Owner | 교재명 왼쪽에 **사용 고객사** 열이 추가되고 그 열의 필터도 함께 나타난다 | S5 |
| 공유 코드인데 사용 고객사 미입력 | 해당 행에 **미입력** — 툴팁: 공유 코드인데 사용 고객사가 비어 있습니다. 교재를 열어 입력하세요. | S5 |
| 필터 적용 | 교재 지표가 **교재(책)·필터**로 바뀌고 집계가 필터 기준으로 계산된다 | S6 |
| 결과 없음 | 표 자리에 **결과 없음** · 페이지네이션이 사라진다 | S11 |
| 고객사 미선택(부모 화면) | `PRJ-02` 우측에 **좌측에서 고객사를 선택하세요.** | — |

### 5.2 메시지

| 상황 | 노출 방식 | 문구 | 화면 |
|---|---|---|---|
| 초기화 | 확인창 | **엑셀 시드로 초기화할까요?** → 완료 시 상단 알림 **초기화됨** | S12 |
| 저장 공간 부족 | 상단 알림 | **⚠ 브라우저 저장 공간이 가득 차 저장하지 못했습니다. 다른 사이트 데이터를 정리한 뒤 다시 저장하세요.** | S12 |

---

## 6. 연결 화면

| 트리거 | 이동 | 설명 |
|---|---|---|
| [＋ 교재(책) 추가] | `PRJ-04` | 등록 모드 |
| 교재 행 클릭 | `PRJ-04` | 수정 모드 |
| 좌측 고객사 전환 | `PRJ-03`(다른 고객사) | 목록과 동시 노출이므로 즉시 전환 |
| 고객사 단가 변경 | `MEM-01` → `MEM-02` | 변경 단가는 **이후 등록하는 교재**부터 반영된다(기존 교재 금액은 유지) |
| 코드 좌표 확인 | `SOB-01` | 해당 S/O/B 위치 확인 |
| 편집 실적 기록 확인 | `LOG-01` | 교재 추가·교재 작업 기록 |

---

## 7. 미결 · 협의

- [ ] 편집 실적의 **casterN 자동 반영**(현재 수동 입력) — 연동 방식·주기
- [ ] 고객사별 **월 마감/청구 확정** 개념 도입 여부(현재는 상시 집계)
- [ ] 목록 **엑셀 내보내기** 필요 여부(재무팀 요청 시)
