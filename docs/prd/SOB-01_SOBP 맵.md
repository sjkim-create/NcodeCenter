# SOB-01 · SOBP 맵

> 문서번호 `NcodeCenter_001_PRD_SOB01` · 공통 규칙은 [PRD-00](PRD-00_공통.md)
> 화면 정의서 [SOB-01_SOBP 맵.html](../screens/SOB-01_SOBP%20맵.html) — 아래 표의 **화면** 열이 그 파일의 상태 번호다.

## 0. 문서 이력

| 버전 | 날짜 | 수정자 | 변경 내용 |
|---|---|---|---|
| v1.0 | 2026-08-25 | 서비스기획팀 | 최초 작성 |
| v2.1 | 2026-08-28 | 서비스기획팀 | **OID 를 좌표 관리에서 분리** `PC-033` — 종류 칩은 **PDS3·PDS2·PDS4·IDS** 4종, 툴바에 **[OID는 별도 대장 →]**(`INF-04`) 링크, S19를 「OID 는 좌표 관리 대상 아님」으로 교체 |
| v2.0 | 2026-08-28 | 서비스기획팀 | **좌표(SOBP) 우선 모델** 반영 `PC-032` — 코드 종류를 **좌표의 속성**으로 재정의(**PDS3 · PDS2 · PDS4(S-code, Section 44)** + IDS), **펜 구분(소리펜·필기펜) 필터** 추가, 배타 규칙을 **종류 기준**으로 정정, 상태 **S18(PDS4)** 신설 |
| v1.1 | 2026-08-27 | 서비스기획팀 | 구현 화면 기준 재작성 — **PAGE 용량 맵**(§4.4) 신설, 상태 필터 표기 정정(`코드 발급`→`발급 전체`, `미사용`→`코드 미발급`), Section 목록 명시, 화면 상태 번호(S1~S17) 연결 |
| v2.2 | 2026-08-28 | 서비스기획팀 | **IDS = OID 통합** · 종류 칩을 **PDS3·PDS2·PDS4·OID** 4종으로 정리하고 OID 필터 복귀 `PC-035`. **[OID는 별도 관리 →] 링크 삭제**, S19를 「OID 좌표 보기」로 교체 |
| v2.3 | 2026-08-28 | 서비스기획팀 | **book 미분할 OID 노출** `PC-036` — 북코드가 없어 지도에서 빠지던 OID 업체(한솔교육 S3/O25 등)를 **OWNER 로 표시**하고 BOOK 열에 **미분할 안내·항목 목록** 추가. Page 용량 패널의 종류 표기도 현재 필터 기준으로 수정 |
| v2.4 | 2026-08-28 | 서비스기획팀 | **펜 구분 필터 삭제** `PC-037` — 펜 값은 Book 카드 배지로만 표시한다(필터 불필요) |
| v2.5 | 2026-08-28 | 서비스기획팀 | 종류 칩을 고르면 **그 종류의 첫 좌표(S-O-B)로 자동 이동** `PC-038` — 데이터가 있는 좌표부터 보여준다(없으면 원장 첫 owner → 첫 Section) |
| v2.6 | 2026-08-28 | 서비스기획팀 | **종류 배타 폐지** `PC-039` — 「같은 S/O 는 한 종류만」 규칙과 🚫 영역 할당됨 차단을 없애고 **종류는 용도 표시(배지)** 로 전환. 상단을 **「코드」(전체·PDS2·PDS3·PDS4·OID) · 「상태」 필터 그룹**으로 재배치하고 **전체 보기** 신설 |
| v2.7 | 2026-08-28 | 서비스기획팀 | OWNER·BOOK 카드의 **칩 순서 통일** `PC-040` — [번호] → [상태] → [종류] → (펜) |
| v2.8 | 2026-08-28 | 서비스기획팀 | 종류 배지 정확도 개선 `PC-041` — 원장 보강(코드 종류 미정) 레코드는 배지에서 제외. 신규 발급의 S/O 종류 단일 규칙은 `SOB-02` 에서 검사 |
| v2.9 | 2026-08-28 | 서비스기획팀 | **PDS4(Section 44) 정식 범위 반영** `PC-042` — owner 0~4095 · book 0~255 · page 0~255 · xy 0~255. 지도 owner 정원을 관측치 1024 → 4096 으로 정정하고 §7 미결 항목을 닫는다 |
| v2.10 | 2026-08-31 | 서비스기획팀 | **미발급 좌표의 잔여 페이지를 종류별로 표기** `PC-049` — PDS2 · PDS3 의 Section 정원이 달라 한 숫자로는 발급 가능량을 알 수 없다. 발급된 좌표는 종전대로 하나만 |

---

## 1. 기능 개요

**Section → Owner → Book → Page** 를 단계별로 좁혀 들어가며 **어떤 코드가 누구에게 발급되어 있고, 어디가 비어 있는지**를 확인하는 코드 지도다. 신규 발급 대상 좌표를 여기서 고른 뒤 `SOB-02`로 할당한다. 코드 발급 업무의 출발점 화면이다.

| 항목 | 내용 |
|---|---|
| 권한 | STAFF / ADMIN |
| 진입 | 좌측 메뉴 [SOBP 맵] |
| 성격 | **조회 전용**. 이 화면 자체에는 저장 동작이 없다 |
| 발급 | **[＋ 직접 코드 할당]** 을 누르면 `SOB-02` 가 **이 화면 위에 창으로** 열린다 |

---

## 2. 화면 흐름 (Flow)

```
좌측 메뉴 [SOBP 맵]
   └─▶ SOB-01
         ① 코드 필터 (전체 / PDS2 / PDS3 / PDS4(S-code) / OID)
         ② 상태 필터 · 고객사 검색으로 범위 좁히기
         ③ SECTION ─▶ OWNER ─▶ BOOK ─▶ PAGE 드릴다운
         └─ [＋ 직접 코드 할당] ─▶ SOB-02 (현재 선택한 Section·Owner가 발급 대상)
                                    └─ 할당 완료 ─▶ SOB-01 (지도 갱신 · 선택 Book 초기화)
```

고객사명을 검색창에 정확히 입력하거나 목록에서 고르면 **그 고객사가 보유한 첫 S/O/B 위치로 지도가 자동 이동**한다.

---

## 3. 정책

| 정책 | 이 화면에서의 적용 |
|---|---|
| `P-05` 무겹침 | 이미 발급된 좌표는 상태·고객사명으로 드러나며, 그 위에 겹쳐 발급할 수 없다(`SOB-02`에서 차단) |
| `P-07` SO 공유 불가 | Section·Owner는 고객사 식별 축. 다른 고객사가 쓰는 S/O에는 발급할 수 없고, **공유(커먼) 코드로 지정된 Owner만 예외** |
| `PC-049` 잔여 페이지 표기 | **아직 발급되지 않은 좌표**는 코드 종류가 정해지지 않아 잔여(발급 가능) 페이지를 **PDS2 · PDS3 로 나눠** 보여 준다(Section 정원이 종류마다 다르다 — 예 S0: PDS2 1,024p · PDS3 4,096p). **이미 발급된 좌표**(계정 발급 · N Key 발급 · 편집 프로젝트의 Book 사용)는 종류가 정해져 있어 **잔여 페이지 하나**만 보여 준다 |
| `P-01` 좌표 = SOBP | **좌표가 먼저**다. Section→Owner→Book→Page 로 짚고, 좌표가 유일하므로 중복은 성립하지 않는다 `PC-032` |
| `P-02` 코드 종류 = 좌표 속성 | **PDS3 · PDS2 · PDS4(S-code = Section 44) · OID** 중 무엇인지만 가린다. 종류 칩으로 전환한다. 옛 **IDS(A코드) = OID 동일 용어** `PC-035` |
| `P-02-1` S/O 내 종류 단일 | 지도에서는 **차단하지 않고 종류 배지(용도)** 로 보여준다 `PC-039`. **신규 발급은 한 S/O 에 한 종류만**이며 그 검사는 `SOB-02` 할당 시점에 한다 `PC-041`. **과거 혼용 이력**(S3/O42 · S3/O44 · S3/O1020)은 배지 두 개로 그대로 보인다 |
| 코드 종류 미정 | **원장 보강 프로젝트**(할당 원장에만 있는 S/O)는 종류가 없어 **배지를 표시하지 않는다** `PC-041`. **직접 코드 할당으로 새로 점유한 좌표도 종류가 미정**이라 배지가 없다 — 종류는 **N Key 발급 · App Key 발급 · 편집 프로젝트**에서 정해진다 `PC-051` |
| `P-02-2` OID | **index 전용 코드**(외부 코드 판독용)도 이 지도에서 **종류 [OID]** 로 필터해 본다 `PC-035`. 같은 S/O 를 다른 종류와 함께 쓸 수 있어 **OID로 잡은 Book 만** 발급으로 표시된다. 업체별 index 목록은 `INF-04`(코드 관리 정보 탭) |
| `P-03` 펜 구분 | **소리펜(NSP)·필기펜(NWP)** 은 좌표 속성으로 **배지로만 표시**한다. 지도에 **펜 필터는 두지 않는다** `PC-037` |
| `P-04` 코드 상태 | 상태는 **코드 발급 / 코드 미발급** 기준. 발급된 코드 안에서 **편집 / 공유 / 사용가능**을 세분해 보여준다 |
| `P-12` 공통(커먼) 코드 | 공유 Owner는 발급 여부와 무관하게 항상 **공유**로 표기하고, 사용 고객사를 함께 보여준다 |
| 테스트/개발 섹션 | **PDS2 S0·S14** 는 상용 미출시 → **추천제외** 배지. 직접 선택은 가능. **PDS3에는 추천제외 Section이 없다** |

---

## 4. 사용법

### 4.1 상단 도구

| 요소 | 동작 | 화면 |
|---|---|---|
| 코드 (필터 그룹) | `전체` / `PDS2` / `PDS3` / `PDS4` / `OID` — 상단 **「코드」 묶음** `PC-039`. 고르면 Section 목록이 다시 그려지고 **그 종류의 첫 좌표(S→O→B)로 이동**한다 `PC-038`. 번호 점프·더 보기·상태 필터는 초기화된다 | S1 · S2 · S18 · S19 |
| 상태 (필터 그룹) | `전체` / `발급 전체` / `코드 미발급` / `편집` / `공유` / `사용가능` — 상단 **「상태」 묶음**. 오른쪽에 **고객사 검색** | S1 · S3~S7 |
| 상태 필터 | `전체` / `발급 전체` / `코드 미발급` / `편집` / `공유` / `사용가능` | S1 · S3 · S4 · S5 · S6 · S7 |
| 고객사 검색 | 비우면 전체. 목록에서 고르거나 정확히 입력하면 **해당 고객사 좌표로 이동**, 부분 입력이면 필터로만 동작 | S8 |
| 범례 | 코드발급 · 편집 · 사용가능 · 공유 · 코드 미발급 | S1 |

상태 필터의 의미 (표기는 화면 그대로)

| 화면 표기 | 의미 | 화면 |
|---|---|---|
| 전체 | 필터 없음 | S1 |
| 발급 전체 | 발급(할당)된 코드 전체 (편집·공유·사용가능 포함) | S3 |
| 코드 미발급 | 아직 발급되지 않은 빈 코드 | S4 |
| 편집 | 발급된 코드 중 실제 편집에 쓰인 코드 | S5 |
| 공유 | 여러 고객사가 함께 쓰도록 지정된 OWNER | S6 |
| 사용가능 | 공유(커먼) 코드 중 아직 편집하지 않은 것 | S7 |

> 필터는 Section·코드 종류를 바꿔도 유지된다. 결과가 비면 §4.5의 안내가 나온다.

### 4.2 발급 대상 선택 (S1)

도구 아래에 **[＋ 직접 코드 할당]** 버튼과 **발급 대상(현재 선택한 S/O)** 이 함께 표시된다 — *위에서 고른 Section·Owner에 코드를 발급합니다.*

### 4.3 드릴다운 4단

| 단계 | 표시 정보 | 조작 | 화면 |
|---|---|---|---|
| **SECTION** | Section 번호, `사용 owner {n} / {정원}`, 추천제외 배지 | 클릭 → 하위 선택 초기화 | S1 · S2 |
| **OWNER** | Owner 번호, 상태 배지(공유 / 🚫 영역 할당됨 / 코드 상태), 보유 고객사명, 언어 슬롯(🌐) | 클릭 → Book 목록 갱신 | S12 |
| **BOOK** | Book 번호, 상태, 사용 고객사·교재명. **다른 코드 종류가 발급한 Book은 선택할 수 없다**. OID 중 **book 미분할** 업체는 목록 맨 위에 **『book 미분할 · {n}건』 안내 + 항목 목록** `PC-036` | 클릭 → Page 용량 갱신 | S13 · S19 |
| **PAGE** | **Page 용량 맵** — §4.4 | 조회 | S14 · S15 · S17 |

Section 목록

| 코드 종류 | Section |
|---|---|
| PDS3(Ncode) | 0 · 3 · 5 · 10 · 11 · 14 · 15 |
| PDS2(Gcode) | 0 · 3 · 14 — **S0 · S14 = 추천제외** |
| PDS4(S-code) | **44** (원장 21 owner · **owner 0~4095 · book 0~255 · page 0~255 · xy 0~255** — Code Info 정식 범위 `PC-042`) |
| OID | **3 · 4** — 데이터가 있는 Section만. Section 4 는 옛 **IDS(A코드)** 이력이며 OID 와 같은 것으로 본다. **book 을 나누지 않은 업체**(한솔교육 S3/O25 등)도 OWNER 로 표시된다 `PC-036` |

언어 슬롯(🌐)은 **PDS2의 S3** 에서만 표시된다. 값은 db에서 직접 관리한다.

### 4.4 PAGE — 용량 맵

선택한 Book의 페이지를 **얼마나 쓰고 있는지** 보여주는 조회 전용 영역이다.

요약 4종

| 항목 | 의미 |
|---|---|
| 전체 | 이 Section이 Book 하나에 줄 수 있는 최대 페이지 |
| 권장 | 운영 권장 사용량 **1,000p** (전체 용량이 더 작으면 전체가 상한) |
| 실 사용 | 실제 편집·발급에 쓰인 페이지 |
| 잔여 (발급 가능) | 전체 − 실 사용 |

막대 3종

- **전체** — 100% 기준
- **권장** — 전체 대비 권장 비율
- **실 사용** — **두 줄**로 나눠 표시한다(권장 기준 / 실제 사용). 실제 사용이 권장을 넘으면 **넘은 구간이 빗금**으로 그려지고 범례에 **⚠ 초과 +{n}p** 가 붙는다

각 막대에 마우스를 올리면 수치 안내가 뜬다 (S17).

| 상황 | 화면 | 화면 번호 |
|---|---|---|
| 사용 중 | 고객사 · 프로젝트 · 교재 · **사용 구간** 을 함께 표시 | S1 |
| 사용 중 아님 | **미사용 (발급 가능) — 이 S/O/B에는 사용 중인 프로젝트가 없습니다.** · 실 사용 0p · 사용 구간 줄 없음 | S14 |
| 공유 코드 | 라벨이 **고객사 → 공유 고객사** 로 바뀐다. 실사용 고객사가 없으면 **사용 고객사 없음** | S15 |
| 편집 중인 코드 | **편집** 배지 + **[✏️ 편집으로 이동 →]** (`PRJ-02`) | S15 |

### 4.5 목록 조작

| 기능 | 동작 | 화면 |
|---|---|---|
| 번호 점프(`O`·`B` 입력) | 입력한 번호부터 이후 목록만 표시. `✕`로 해제. 정원을 넘겨 입력하면 **정원-1로 보정**, 숫자만 받는다 | S9 |
| [＋ 더 보기] | 다음 **80개**를 이어서 표시(남은 개수 함께 표시). 목록을 끝까지 스크롤해도 자동으로 이어 로드된다 | S16 |
| 필터로 결과가 없을 때 | **S{n} 에는 {걸린 필터} 에 해당하는 Owner가 없습니다.** + [필터 해제] | S10 |
| 필터 없이 결과가 없을 때 | **결과 없음** 만 표시 | S11 |
| 상위 선택 변경 | 현재 선택이 목록에서 사라지면 첫 항목으로 자동 이동 | S1 |

> **[필터 해제]** 는 상태·고객사·검색어·번호 점프·더 보기를 **한 번에** 되돌린다.
> 안내 문구에 들어가는 조건은 **상태 · 고객사 · 검색어 · O 번호 이후** 를 이어 붙인 것이다.

---

## 5. 상태별 변화 · 메시지

| 표시 | 의미 | 화면 |
|---|---|---|
| **공유** | 공유(커먼) 코드로 지정된 Owner. **발급 여부와 무관하게 항상 이 배지가 우선**한다. 안내 아이콘에 공유 사유·사용 고객사 | S6 · S12 · S15 |
| 종류 배지 | OWNER·BOOK 카드에 그 좌표의 **용도(PDS2·PDS3·PDS4·OID)** 를 표시. 한 owner 가 두 종류를 함께 쓰면 배지도 둘 다 붙는다 `PC-039` | S12 · S13 |
| **코드발급 / 편집 / 사용가능 / 코드 미발급** | 코드 상태 배지 | S3 · S4 · S5 · S7 |
| **추천제외** | 테스트/개발 전용 Section (자동 추천 제외, 직접 선택은 가능) — **PDS2 S0·S14 만** | S2 |
| 🌐 언어 슬롯 | PDS2 S3 의 언어 확장 Owner | S12 |
| 사용 고객사 표기 | 공유 코드는 **실사용 고객사**만 표시한다(대표 오너명으로 대체 표기하지 않음) | S12 · S15 |
| 결과 없음 | **결과 없음** 또는 필터 안내 문구 + [필터 해제] | S10 · S11 |

**OWNER·BOOK 카드의 칩 순서는 같다** `PC-040` — **[번호] → [상태(공유 포함)] → [종류(용도)] → (펜)**. 칩이 넘치면 두 카드 모두 같은 규칙으로 줄바꿈한다.

이 화면 자체에는 저장 동작이 없어 검증 메시지가 없다. 할당 관련 메시지는 `SOB-02`.

---

## 6. 연결 화면

| 트리거 | 이동 | 설명 |
|---|---|---|
| [＋ 직접 코드 할당] | `SOB-02` | 현재 선택한 Section·Owner가 발급 대상 · **같은 화면 위 창** |
| 고객사 검색으로 이동 | (동일 화면) | 해당 고객사 좌표로 지도 이동 |
| 발급된 코드의 고객사 확인 | `MEM-01` | 업체 상세 |
| 발급 코드의 프로젝트 확인 | `PRJ-01` | 고객사별 코드 프로젝트 |
| [✏️ 편집으로 이동 →] | `PRJ-02` → `PRJ-03` | 그 Owner의 편집 프로젝트 |

---

## 7. 미결 · 협의

- [ ] 상태 5종(발급 전체·코드 미발급·편집·공유·사용가능) **API 필터 지원** — 개발팀 Admin/NDP 측 대응 필요
- [ ] **권장 사용량 1,000p** 의 근거·조정 주체 — 현재 전 Section 공통 고정값
- [ ] 다른 종류 선점(🚫) 규칙의 **해제 절차** — 잘못 선점된 Owner를 되돌리는 방법
- [ ] OID 관련 미결은 `INF-04` §7 참고 (좌표 관리 대상이 아님 `PC-033`)

> ~~Page 단계의 부분 사용 표시 기준~~ — §4.4 **Page 용량 맵**으로 해소(v1.1).
