# MEM-02 · 고객사 등록·수정

> 문서번호 `NcodeCenter_001_PRD_MEM02` · 공통 규칙은 [PRD-00](PRD-00_공통.md) · 부모 화면 [MEM-01](MEM-01_고객사%20관리.md)
> 화면 정의서 [MEM-02_고객사 등록·수정.html](../screens/MEM-02_고객사%20등록·수정.html) — 아래 표의 **화면** 열이 그 파일의 상태 번호다.
> 포함: **업무요청 메모 고객사 수정 — 업무요청 메모**(§4.7)

## 0. 문서 이력

| 버전 | 날짜 | 수정자 | 변경 내용 |
|---|---|---|---|
| v1.0 | 2026-08-25 | 서비스기획팀 | 최초 작성 — 등록·수정 공용 화면 |
| v2.5 | 2026-09-03 | 서비스기획팀 | **화면정의서 정정** `PC-093` — 프로젝트 상태는 **토글 스위치**(칩 아님), 공통코드 사용 고객사는 **2열 5건 카드**(1열 3건 아님) |
| v2.4 | 2026-09-03 | 서비스기획팀 | **필기펜 단가 항목 정리** `PC-084` — 6→**4항목**. `none 편집비용` → **`기본 편집`**, `Custom` 1,000 → **1,500**원/심볼, `action 변경 편집`·`교원구몬/KEP` **삭제** |
| v2.3 | 2026-09-02 | 서비스기획팀 | **공통코드 목록에서 `Common 추가 언어 슬롯-964`(PDS2 · S3/O964) 제외** `PC-082` — 사용 고객사 0곳 · 다국어 편집용 레퍼런스라 하위 고객사를 두지 않는다(6→5건). SOBP 맵의 **공유 OWNER 판정은 그대로** |
| v2.2 | 2026-09-02 | 서비스기획팀 | **사용 서비스를 탭으로** `PC-077` — casterN · 폼솔루션 **탭 구성**(계정의 인증 서비스 탭과 같은 결). **프로젝트 상태 · 공통코드 사용 고객사는 casterN 탭 안**으로 들어간다(편집에 딸린 설정이므로). 폼솔루션은 **지정 항목 없이 선택만** 하고, 고르면 [폼솔루션 서비스 관리]에 나타난다고 안내한다 |
| v2.1 | 2026-09-02 | 서비스기획팀 | **사용 서비스 항목 신설** `PC-076` — 서비스는 **고객사 속성**이므로 여기서 지정한다(§4.2). SOBP 맵의 선택은 폐지되고 읽기 표시가 된다. 고르는 값은 **casterN · 폼솔루션** 둘뿐이고 **아무것도 고르지 않으면 SDK 연동(코드만 할당)**. 계정의 **인증 서비스**(`TKT-02`)와는 다른 값이다 |
| v2.0 | 2026-08-29 | 서비스기획팀 | **모달 → 별도 페이지 2개** — 등록·상세수정을 `MEM-01` 목록에서 분리. 좌측 입력을 **3탭**(기본 정보 / 편집 단가 / 관련 서류)으로 나누고, 수정 화면의 **업무요청 메모를 탭과 무관하게 우측 고정**. 편집 단가의 소리펜·필기펜을 **그룹 카드**로 분리하고 **그룹별 기본값 복원** 추가. 업무요청 메모 코드 폐기(이 문서에 통합) |
| v1.1 | 2026-08-27 | 서비스기획팀 | 구현 화면 기준 정정 — 편집 단가 **소리펜 16 / 필기펜 6항목**(양쪽에 Ncode 적용 포함)과 **전사 기본 단가표**(적용 500 · 편집 1,000) 명시, 전용 항목 **강조 표시**, 프로젝트 상태 **토글(진행중/사업 종료)**, 업무 메모 **대상(공통/프로젝트) 선택**, 모드별 버튼 라벨, 화면 상태 번호(S1~S9) 연결 |
| v1.4 | 2026-08-31 | 서비스기획팀 | **공통코드 목록 정리** `PC-044` — 실제로 공유되지 않는 **NLJ NTT-940 · 딥스원테크-303 · 구몬D-37** 3건을 목록에서 제외. 등록 기준(“여러 고객사가 함께 쓰는 코드만”)을 §4.4 에 명시 |

---

## 1. 기능 개요

`MEM-01` 고객사 목록에서 열리는 **두 화면**이다. 모달이 아니라 각각 독립된 페이지다.

| 화면 | 주소 | 하는 일 |
|---|---|---|
| **고객사 등록** | `/companies/new` | 기본 정보·공통코드 귀속·단가·서류를 입력하고 **[등록]** |
| **고객사 상세·수정** | `/companies/{id}` | 같은 항목을 고쳐 **[저장]**, 우측에서 **업무요청 메모**를 남긴다 |

| 항목 | 내용 |
|---|---|
| 권한 | STAFF / ADMIN |
| 진입 | `MEM-01` 의 **[＋ 고객사 등록]** / 목록 행 클릭 |
| 좌측 구성 | **탭 3개** — 기본 정보 / 편집 단가 / 관련 서류 |
| 우측 구성 | **업무요청 메모** — 수정 화면에서만 · 탭과 무관하게 항상 보인다 |
| 저장 | **[등록]/[저장] 한 번으로 세 탭 내용이 함께 기록된다** |

> 등록 화면에는 업무요청 메모가 없다. 고객사가 만들어진 뒤부터 기록한다.

---

## 2. 화면 흐름 (Flow)

```
MEM-01 고객사 목록
   ├─ [＋ 고객사 등록] ─▶ MEM-02 등록  (/companies/new)
   │      탭: 기본 정보 · 편집 단가 · 관련 서류
   │      └─ [등록] ─▶ MEM-01 목록으로
   │
   └─ 행 클릭 ─────────▶ MEM-02 상세·수정  (/companies/{id})
          ├─ 좌: 탭 3개
          ├─ 우: 업무요청 메모 (탭과 무관하게 고정)
          ├─ [저장] ─▶ 이 화면에 머무름
          ├─ [목록] ─▶ MEM-01
          └─ [고객사 삭제] ─(업체명 재입력 확인)─▶ MEM-01
```

---

## 3. 정책

| 정책 | 이 화면에서의 적용 |
|---|---|
| `P-11` 고객사 계층 | 공통코드를 **사용**하도록 체크하면 그 고객사는 **하위 고객사**가 된다. **대표(상위) 회사는 자기 코드의 하위가 될 수 없어** 해당 항목이 비활성으로 표시된다 (`PC-015`) |
| `P-12` 공통(커먼) 코드 중앙 모델 | 하위 등록은 **이미 발급된 공통코드를 함께 쓰는 것**이므로 **코드 할당이 발생하지 않는다** |
| `P-16` 정산 단가 | 편집 단가는 **2026 항목별 단가표** 기준. **기본값에서 바꾼 항목만 이 고객사 전용 단가**가 된다 |
| `PC-027` 단가 적용 시점 | 단가는 **고객사마다 등록·수정**할 수 있고, 변경은 **이후 등록되는 교재(책)부터 적용**된다. **기존 교재는 등록 당시 단가를 그대로 유지**한다(소급 적용 없음) |
| 단가 3단 구조 | 기본 단가 → **이 화면의 고객사 단가** → 교재별 할인(`PRJ-04`) |
| 프로젝트 종료 | 사업이 끝난 고객사는 **종료**로 표시하고 사유·이관 메모를 남긴다(코드 발급 이력은 보존) |

---

## 4. 사용법

### 4.0 탭 구성

입력 항목이 길어 좌측을 탭으로 나눈다. **한 번에 한 탭만** 보여주고 아래로 쌓지 않는다.

| 탭 | 내용 | 탭 머리 표기 |
|---|---|---|
| **기본 정보** | 업체·담당자·사업자·계좌·주소 + **사용 서비스 탭**(casterN ▸ 프로젝트 상태·공통코드 귀속 / 폼솔루션) `PC-077` | — |
| **편집 단가** | 소리펜·필기펜 항목별 단가 | 전용 단가 개수 |
| **관련 서류** | 서류 항목·파일 | 등록된 서류 수 |

탭을 열지 않아도 **전용 단가 개수와 서류 수**가 탭 머리에 보인다.

수정 화면에서는 **업무요청 메모가 우측에 고정**되어 어느 탭을 보든 함께 보인다. 좌측을 스크롤해도 따라온다 — 단가를 조정하면서 요청 내용을 확인하는 동선을 위해서다.

### 4.1 기본 정보

| 항목 | 필수 | 규칙 |
|---|---|---|
| 업체명 | ✔ | 코드·프로젝트가 이 이름으로 연결된다 |
| 담당자 | — | |
| 연락처 | — | |
| 사업자등록증번호 | — | 형식 예: `000-00-00000` |
| 주소 | — | `TKT-01` 계정 등록 시 기본 주소로 사용된다 |
| 은행명 / 계좌번호 | — | 정산 지급용 |
| 세금계산서 발행용 이메일 | — | 예: `tax@company.com` |

### 4.2 사용 서비스 `PC-076`

**우리가 이 고객사를 어느 서비스로 다루는가**를 정한다. 이 값 하나로 **편집 프로젝트·폼솔루션 서비스 관리**가 고객사를 불러온다.

| 값 | 의미 | 준비 상태 |
|---|---|---|
| **casterN (편집툴)** | 우리가 이 고객사 자료를 편집한다 → **편집 프로젝트(`PRJ-02`)의 대상**이 된다 | 사용 중 (초기값 70곳) |
| **폼솔루션** | 폼솔루션 서비스로 관리한다 | **서비스 준비중** — 개발 전이라 지정된 고객사가 없다(0곳) |
| *(아무것도 고르지 않음)* | **SDK 연동 (코드만 할당)** — 우리 서비스를 거치지 않고 코드만 받아 직접 연동한다 | 기본값 |

**탭으로 나눈다** `PC-077` — 서비스마다 딸린 설정이 다르므로 **한 번에 한 서비스만** 보여준다. 계정의 [인증 서비스 및 권한](`TKT-02` §4.2) 과 같은 결이다.

| 탭 | 켰을 때 나오는 것 |
|---|---|
| **casterN (편집툴)** | **프로젝트 상태**(§4.3) · **공통코드 사용 고객사(하위) 등록**(§4.4) — 둘 다 **편집에 딸린 설정**이라 이 탭 안에 있다 |
| **폼솔루션** `준비중` | **지정할 항목이 없다.** 선택하면 이 고객사가 **[폼솔루션 서비스 관리]** 화면에 나타난다는 안내만 나온다 |

- 탭 머리의 서비스명 앞에 **✓** 가 붙어 어느 서비스가 켜져 있는지 한눈에 보인다.
- casterN 을 끄면 그 자리에 *casterN 으로 선택하면 프로젝트 상태·공통코드 사용 고객사를 지정할 수 있습니다.* 가 나온다.
- **복수 선택**할 수 있다. 한 고객사가 편집도 하고 폼솔루션도 쓸 수 있다.
- **초기값**은 편집팀 대장에서 채웠다 — 교재(책) 행이 있는 **70곳이 casterN**. 이전 방식(SOBP 맵 좌표별 지정 `PC-057`)은 43곳뿐이라 **실제 편집 고객사 27곳이 빠져 있었다**.
- **계정의 [인증 서비스](`TKT-02`)와 다른 값이다.** 이쪽은 *우리가 이 고객사를 어떻게 다루나*, 저쪽은 *외부 고객사가 우리 서비스 어디에 로그인하나* 이다. 우리가 고객사 자료를 편집하는 데는 계정·App Key 가 필요 없다.

### 4.3 프로젝트 상태 (S8) — casterN 탭 안 `PC-077`

**토글 스위치**로 전환한다 `PC-093` — 칩 버튼이 아니다. 켜면 붉은색으로 바뀌고, 스위치 오른쪽에 **현재 상태**(`진행중` / `사업 종료`)와 설명이 붙는다.

| 값 | 의미 |
|---|---|
| **진행중** | 정상 거래 중 (기본값) |
| **사업 종료** | 사업 정리. 아래에 **종료 사유 / 코드 이관 메모** 입력란이 나타난다 (예: 엠베스트-28로 코드 이관) |

> 안내: *사업 종료 시 코드 발급 이력만 유지되고, 목록·코드 프로젝트에서 비활성으로 표시됩니다.*

### 4.4 공통코드 사용 고객사(하위) 등록 — casterN 탭 안 `PC-077`

*대표 회사 아래로 귀속 · 코드 할당 불필요*

- 사용할 공통코드를 체크하면 이 고객사가 그 **공통코드의 사용 고객사(하위)** 로 등록된다.
- 목록에 오르는 것은 **여러 고객사가 함께 쓰는 코드**뿐이다 `PC-044`. 보유 고객사가 **한 곳뿐(자기 자신·자사)** 이거나 사용 이력이 **없는 코드**는 일반 코드이므로 이 목록에 나오지 않는다.
- 안내: 이미 발급된 공통코드를 쓰므로 **코드 할당은 없고**, 편집 프로젝트·티켓 발급에서 하위 고객사로 관리된다.
- **자기 회사가 보유한 코드는 선택할 수 없다**(비활성).

**2열 카드**로 늘어놓는다 `PC-093` — 한 칸에 **공통코드명**(굵게)과 **좌표**(`PDS2 · S3/O21`)가 2줄로 들어가고, 선택하면 파란 테두리·연파랑 배경이 된다.

**현재 목록 (5건)** — 레지스트리는 `lib/commonCodes.ts` 가 정본이고, 정본 데이터는 `build_all_sources.py` 가 만든다.

| 공통코드 | 좌표 | 사용 고객사 |
|---|---|---|
| Common-21 | PDS2 · S3/O21 | 50곳 |
| 네오노트-0-27 | PDS3 · S0/O27 | 6곳 |
| 네오노트-3-27 | PDS3 · S3/O27 | 154곳 |
| 네오노트-1012 | PDS3 · S3/O1012 | 31곳 |
| 스마트클래스키트-1013 | PDS3 · S3/O1013 | 4곳 |

**목록에서 뺀 코드**

| 코드 | 뺀 이유 |
|---|---|
| NLJ NTT-940 · 딥스원테크-303 · 구몬D-37 | 보유 고객사 1곳(자사)이거나 사용 이력 0곳 `PC-044` |
| **Common 추가 언어 슬롯-964** (PDS2 · S3/O964) | **사용 고객사 0곳 · 다국어 편집용 레퍼런스**라 하위 고객사를 두지 않는다 `PC-082` |
| 네오노트-IDS-27 (OID · S4/O27) | **이력전용** — 검색·이력에만 쓰고 이 목록에는 올리지 않는다 |

> `Common 추가 언어 슬롯-964` 를 목록에서 빼도 **SOBP 맵의 공유 OWNER 판정은 그대로다** `PC-082` — `O964~983` 은 NSP 커먼 코드 범위(`sharedOwners.ts`)라 계속 **공유**로 표시되고 Book 단위 배타만 적용된다.

### 4.5 편집 단가 (2026 항목별)

소리펜과 필기펜은 **정산 단위가 다른 별개 묶음**이라 각각 카드로 나눠 보여준다.

| 그룹 | 성격 | 그룹 머리 표기 |
|---|---|---|
| **🔊 소리펜** | ncp2 산출 · mp3 심볼 | 항목 수 · 그 그룹의 전용 단가 개수 |
| **✍ 필기펜** | 필기 인식 · APP 연동 | 항목 수 · 그 그룹의 전용 단가 개수 |

전용 단가가 있는 그룹에만 **[기본값]** 이 나타나며 **그 그룹 항목만** 되돌린다. 상단의 **[전체 기본값]** 은 두 그룹을 모두 되돌린다.

*기본값에서 바꾼 항목만 전용 단가*

**🔊 소리펜 16항목 · ✍ 필기펜 4항목** `PC-084` 을 각각 **2열**로 늘어놓고 값을 직접 입력한다. 두 그룹 모두 맨 위에 **Ncode 적용(페이지당)** 이 들어간다. (S5)

**필기펜 4항목** `PC-084`

| 항목 | 단위 | 기본 단가 |
|---|---|---|
| Ncode 적용 | 페이지당 | 500 |
| **기본 편집** (구 `none 편집비용`) | 심볼당 | 1,000 |
| Custom | 심볼당 | **1,500** (구 1,000) |
| 노트서버 업로드 | 건당 | 10,000 |

> **삭제한 항목** `PC-084` — `action 변경 편집` · `교원구몬/KEP` 는 쓰지 않아 뺐다. 같은 항목이 [편집 프로젝트]의 **필기펜 심볼 입력**(`PRJ-04`)에도 쓰이므로 **두 화면이 함께** 바뀐다.
> 교재의 **타입** 셀렉트(소리펜 / 필기펜 / 교원구몬·KEP)는 단가 항목이 아니라 **그대로 둔다**.

| 요소 | 동작 |
|---|---|
| 항목별 단가 입력 | 항목마다 **단위**(페이지당 / 심볼당 / 건당)가 함께 표시된다 |
| 기본값과 다른 항목 | 항목명과 입력칸이 **기본값과 다르다는 표시**로 강조된다 |
| 전용 항목 수 | 기본값과 다른 항목이 있으면 **전용 {n}항목** 배지 |
| [전체 기본값] | 모든 항목을 기본 단가로 되돌린다 |

전사 기본 단가(원)

| 🔊 소리펜 | 단가 | ✍ 필기펜 | 단가 |
|---|---|---|---|
| Ncode 적용 (페이지당) | 500 | Ncode 적용 (페이지당) | 500 |
| Ncode 편집(기본) (심볼당) | 1,000 | none 편집비용 (심볼당) | 1,000 |
| Compound 2 / 3 / 4언어 | 1,300 / 1,600 / 1,900 | Custom (심볼당) | 1,000 ⚠ |
| Compound 5 / 6 / 7 / 8언어 | 2,200 / 2,500 / 2,800 / 3,100 | action 변경 편집 (심볼당) | 1,500 |
| 슬롯전환 / 그룹재생 (건당) | 3,000 / 5,000 | 노트서버 업로드 (건당) | 10,000 |
| 게임 / 프롬프트 편집 / RAG 데이터 업로드 (건당) | 각 50,000 | 교원구몬/KEP (건당) | 1,000 ⚠ |
| 4도 Ncode 출력 (페이지당) | 1,000 | | |

> ⚠ **Custom · 교원구몬/KEP** 단가는 미확정(`PC-018` 확인 대기).

안내: **편집 프로젝트의 청구액이 이 단가로 계산됩니다(항목 수량 × 단가). 단가를 바꾸면 이후 등록되는 교재(책)부터 적용되고, 기존 교재는 등록 당시 단가가 그대로 유지됩니다. 교재별 할인은 편집 프로젝트 > 교재 수정에서.**

**단가 변경의 영향 범위**

| 대상 | 적용 |
|---|---|
| 이 화면에서 단가 저장 후 **새로 등록하는 교재** | **새 단가**로 계산 |
| 이미 등록된 교재 | **등록 당시 단가 유지** — 청구액이 바뀌지 않는다 |
| 기존 교재를 새 단가로 바꾸고 싶을 때 | `PRJ-04` 교재 수정에서 **[현재 단가로 갱신]** 을 눌러 개별 적용 |

### 4.6 관련 서류

| 요소 | 동작 |
|---|---|
| [＋ 서류 추가] | 행을 하나 추가한다 |
| 항목명 | 서류 이름을 직접 입력(항목명 자체를 바꿀 수 있다) |
| 파일 | 파일 등록. 고르기 전에는 **선택 없음** |
| [삭제] | 해당 서류 행 제거 |
| 서류 없음 | **등록된 서류가 없습니다.** |

### 4.7 업무요청 메모 (수정 화면 전용)

고객사와 주고받은 요청·처리 내역을 남기는 **업무 원장**. **수정 모드에서만** 우측에 표시된다(등록 시에는 없음).

| 요소 | 동작 | 화면 |
|---|---|---|
| 대상 | **고객사 공통** 또는 그 고객사의 **프로젝트**를 골라 기록한다 | S7 |
| 종류 | **요청 / 처리 / 메모** | S7 |
| 입력 | **Enter 기록 · Shift+Enter 줄바꿈** | S7 |
| 기록 목록 | 번호 · **대상 배지**(고객사 공통 / 프로젝트) · 종류 배지 · 작성일시(수정하면 **(수정됨)**) · 작성자(본인이면 **(나)**) | S2 |
| 본인 글 | 클릭하면 위 입력칸에서 수정 — 버튼이 **[수정 저장] · [취소]** 로 바뀐다. **[삭제]** 가능 | S7 |
| 타인 글 | **🔒 타인 글** 로 표시되고 수정·삭제할 수 없다 | S8 |
| 기록 없음 | **기록된 업무가 없습니다.** | S1 |

---

### 4.8 하단 액션

| 버튼 | 화면 | 동작 |
|---|---|---|
| **[고객사 삭제]** | 상세·수정만 | 업체명을 다시 입력해야 삭제된다. 프로젝트·발급 코드가 회수된다 |
| **[목록]** | 공통 | `MEM-01` 로 이동. 입력값은 저장되지 않는다 |
| **[등록]** | 등록 | 저장 후 **목록으로 이동** |
| **[저장]** | 상세·수정 | 저장 후 **이 화면에 머무른다** |

세 탭의 내용은 **한 번의 저장으로 함께** 기록된다. 탭은 저장 단위가 아니다.

---

## 5. 상태별 변화 · 메시지

| 상태 | 화면 | 화면 번호 |
|---|---|---|
| 등록 — 기본 정보 탭 | 빈 입력. 업무요청 메모 없음 | S1 |
| 상세·수정 — 기본 정보 탭 | 값이 채워지고 우측에 업무요청 메모 | S2 |
| 편집 단가 탭 | 소리펜·필기펜 그룹 카드. 그룹별 전용 개수와 [기본값] | S3 |
| 관련 서류 탭 | 서류 목록. 탭 머리에 건수 | S4 |
| 업체명 미입력 | 저장이 막히고 안내가 나온다 | S5 |
| 대표(상위) 회사 | 자기 공통코드는 하위로 고를 수 없다 | S6 |
| 전용 단가 지정됨 | 바꾼 항목이 강조되고 개수가 표기된다 | S7 |
| 프로젝트 종료 선택 | 종료 사유·이관 메모 입력칸이 열린다 | S8 |
| 업무요청 메모 입력 중 | 종류를 고르고 내용을 적는다 | S9 |
| 남의 메모 수정 시도 | 차단 안내 | S10 |
| 메모 삭제 확인창 | 확인 후 제거 | S11 |

### 메시지

| 상황 | 노출 방식 | 문구 | 화면 |
|---|---|---|---|
| 업체명 미입력 | 상단 알림 | **업체명은 필수입니다.** | S3 |
| 등록 저장 | 상단 알림 | **등록됨 · {업체명}** | S1 |
| 수정 저장 | 상단 알림 | **수정됨 · {업체명}** | S2 |
| 남의 메모 수정 시도 | 상단 알림 | **{작성자} 님이 작성한 메모입니다. 본인 글만 수정할 수 있습니다.** | S8 |
| 남의 메모 삭제 시도 | 상단 알림 | **{작성자} 님이 작성한 메모입니다. 본인 글만 삭제할 수 있습니다.** | S8 |
| 본인 글 확인 실패 | 상단 알림 | **본인이 작성한 메모만 수정할 수 있습니다.** | S8 |
| 메모 삭제 | 확인창 | **메모 {No}번을 삭제할까요?** | S9 |

---

## 6. 연결 화면

| 트리거 | 이동 | 설명 |
|---|---|---|
| [저장] · [취소] | `MEM-01` | 목록 갱신 후 복귀 |
| 저장(자동) | `LOG-01` | **고객사 관리** 활동 기록 |
| 등록 후 코드 할당 | `SOB-01` → `SOB-02` | 등록된 고객사만 발급 대상 |
| 단가 변경 후 확인 | `PRJ-02` → `PRJ-03` | 청구액 재계산 |
| 티켓 발급 | `TKT-04` / `TKT-01` | |
| 하위 등록 결과 확인 | `PRJ-01` | 공유 코드 프로젝트의 **사용 고객사(하위)** 목록 |

---

## 7. 미결 · 협의

- [ ] **Custom · 교원구몬/KEP 단가** 확정 (`PC-018` 확인 대기)
- [ ] 서류 파일의 **보관 위치·용량 정책**(현재 브라우저 보관)
- [ ] 업무 원장 메모의 **타 직원 수정 권한**(현재 본인만) — ADMIN 예외 허용 여부
