# NcodeCenter — 코드 발급·할당 운영 정책 (참조)

> 출처: 코드 발급 담당(한윤정) 협의. 현재 코드를 **Excel로 관리하는 실무**에서 합의된 원칙.
> 성격: **개발 반영 기준 문서(참조)**. 일부는 개발팀/편집파트 협의 필요(§맨아래).
> ★ **신규 화면/자동 할당 로직 개발 시 반드시 이 문서의 관련 항목을 반영·확인**한다.
> 관련: [정책 정의서](NcodeCenter-정책정의서.md)(확정 정책 정의) · [구조](NcodeCenter-Structure.md) · [DB](NcodeCenter-DB.md) · [SOBP 할당(NDP)](NcodeCenter-SOBP-Allocation.md) · [메뉴](menus/)

---

## 0. 정책 변경 이력 (Changelog)

> **운영 규칙**: Excel 등 raw 데이터를 정리하거나 정책이 바뀔 때마다 **이 표에 1행을 추가**한다.
> 목적 — 과거에 무엇을 어떻게 바꿨는지(변경 전 → 후·사유)를 잃지 않고 추적. 본문 규칙을 고칠 때는 **반드시 여기에도 기록**.
> ID 규칙: `PC-NNN`(Policy Change). 상태: ✅ 확정 · 🟡 확인 대기.

| ID | 날짜 | 항목 | 변경 전 → 변경 후 | 사유 · 근거 | 반영 위치 | 상태 |
|----|------|------|------------------|-------------|-----------|------|
| PC-001 | 2026-07-15 | 취급 코드 종류 | S·N·G → **N/G만** (S코드 제외) | 코드 종류 단순화 결정 | §4, 구조MD | ✅ |
| PC-002 | 2026-07-15 | 행위자 모델 | 외부 고객 가입/요청 → **내부 직원 전용 운영** (가입승인·Customer 역할 폐기) | 실무 = 내부 직원이 직접 등록·할당 | 구조MD §0/§2/§5, 메뉴 #2 | ✅ |
| PC-003 | 2026-08-03 | "예약(Reservation)" 메뉴 | Admin식 **별도 "Ncode 예약" 메뉴/절차** → **미채택**(NcodeCenter는 별도 예약 메뉴 없음) | 예약을 독립 업무로 두지 않음 | menu.ts, IA §2 | ✅ |
| PC-004 | 2026-08-03 | 예약(RESERVED) **상태** 개념 | 원장 상태 `RESERVED/IN_USE` 2분 → **예약 "상태" 개념 자체 폐기** (예약/사용중 구분 없음) | 사용자 결정 2026-08-03: "상태 개념까지 제거" | DB `usage_status`·대시보드·데이터·엔진 | ✅ 완료 |
| PC-005 | 2026-08-03 | 기존 RESERVED 243건 **재해석** | 마스터전용(상세無)=RESERVED → **모두 '할당됨(발급)'** 으로 간주 | 사용자 결정 2026-08-03: 상세 유무와 무관하게 할당된 코드 | 데이터 파이프라인·대시보드 | ✅ 완료 |
| PC-006 | 2026-07-31 | 공통(커먼)코드 관리 모델 | 컴포넌트별 산재·`nspCommon/nwpCommon` 플래그 → **중앙 레지스트리**: 코드 정본 = `(타입 k, Section, Owner)`, 단일 소스 `commonCodes.ts` + `common-members.json` | "데이터는 항상 중앙집중식으로 관리" 지시 | `commonCodes.ts`·`commonMembers.ts`·빌드 `COMMON_CODES` | ✅ |
| PC-007 | 2026-07-31 | 공통코드 타입 k | (N에 접힘) → **N(PDS3)·G(PDS2)·A(IDS, 이력전용)** 3종 | IDS(초창기) 분리 | 빌드·레지스트리 | ✅ |
| PC-008 | 2026-07-31 | 공통코드 홀더 회사 | 1회사에 여러 코드(네오노트-27) → **1회사=1코드 분리**(네오노트-0-27/3-27/14-27/IDS-27) | 회사·프로젝트·편집·소유권 일관 | 빌드 `COMMON_SPLIT`·store v23 | ✅ |
| PC-009 | 2026-07-31 | 커먼코드 판정 기준 | 옛 `nspCommon/nwpCommon` 플래그 → **레지스트리 기준**(`isCommonCodeCompany`). 팝펜스티커-26 제외(사용 고객사 0=공유 아님), 공유코드 4개 추가(스마트클래스키트-1013·NLJ NTT-940·구몬D-37·Common964) | 옛 플래그 과다 포함 | `CompaniesView`·레지스트리 | ✅ |
| PC-010 | 2026-08-03 | 사용 서비스 항목 | 네오노트·폼솔루션·casterN·아이글·기타·없음 → **casterN·아이글·폼솔루션·NeoStudio2·서비스없음**(기타 제거, 네오노트→**NeoStudio2**) | 서비스 정리 | `customerData.ts` `SERVICE` | ✅ |
| PC-011 | 2026-08-03 | 서비스 지정 위치 | 코드 프로젝트 등록에서 지정 → **SOBP 맵 직접 코드 할당에서만 지정**. 코드 프로젝트 = **조회·검색 전용**(등록 제거) | 발급 지점에서 용도 지정 = 중앙집중 | `OwnershipMap`·`ProjectsView` | ✅ |
| PC-012 | 2026-08-03 | 편집 프로젝트 = casterN | 편집 데이터 파생 코드 service 미지정 → **service=CASTERN 자동**. 편집 프로젝트 = casterN 서비스 관리 화면 | 편집=casterN 편집툴 | 빌드 line~478 | ✅ |
| PC-013 | 2026-08-03 | 고객사 계층 구분 | 구분 없음 → **상위(공통코드 보유 대장) / 하위(공통코드 사용=멤버십 귀속) / 단독** 3분류 + 검색 필터([커먼코드만] 버튼 대체) | 고객사 성격 식별 | `CompaniesView` | ✅ |
| PC-014 | 2026-08-03 | 하위 고객사 실체 | 이름만 있는 멤버십(별도 sub-row) → **정식 회사 레코드**(담당자·사업자·주소 보유, 이름순 통합 정렬·검색, 클릭 시 회사 상세). SOBP/코드는 **코드 프로젝트·SOBP 맵**에서 관리 | 하위도 일반 고객사처럼 등록·검색·상세 | 빌드(214건 생성)·store v25·`CompaniesView` | ✅ |
| PC-015 | 2026-07-31 | 대표(상위) 고객사 자기 코드 | 자기 코드도 하위 등록 가능 → **대표 회사 수정 시 자기 코드 비활성화** | 자기 코드의 하위가 될 수 없음(논리 정합) | `CompaniesView` 등록폼 | ✅ |
| PC-016 | 2026-08-03 | 티켓 발급 모델 | 단일 → **N Key(물리·오프라인·계정불필요) / App Key(계정 연동) / 정산(과금: 유료·무료·체험·미정)**, 출처 대장(ledger)/신규발급 구분 | 실무 발급·과금 반영 | `TicketsView` | ✅ |
| PC-017 | (상시) | 원본 Excel 편집 | — → **NWP/NSP 원본 Excel은 프로그램(openpyxl)으로 편집 금지**(이미지·드로잉 손상). 변경은 사용자가 **Excel에서 직접 수정** 후 재빌드 | 시트에 도형·이미지 포함 | 빌드 파이프라인 | ✅ 운영 제약 |
| PC-018 | 2026-08-03 | 편집 정산 단가 모델 | 단일 심볼 단가(1,000) → **2026 항목별 단가표**: 소리펜(적용·편집·Compound2~8언어·슬롯전환·그룹재생·게임·프롬프트·RAG·4도출력)·필기펜(적용·none·**Custom**·action·노트서버·**교원구몬/KEP**). 각 항목 "수량×단가", **고객사별 개별 지정**(Company.rates). ※ Custom·KEP 단가는 placeholder(1,000), 확인 필요 | 실제 견적 단가 구조 반영 | `pricing.ts`·`CompaniesView`·`EditingDetailView`·customers.rates | ✅ |
| PC-019 | 2026-08-25 | 사용 서비스 항목 | casterN·아이글·폼솔루션·NeoStudio2·서비스없음 → **+ `NCODEPRINTER` (Ncode 프린터(드라이버))** 추가 | Ncode 프린터가 NDP에서 SOBP를 실시간 발급받는 서비스로 확정 — 발급 코드의 용도 식별 필요 | `customerData.ts` `SERVICE`·`OwnershipMap`·`ProjectsView`·`dbSchema.ts` | ✅ |
| PC-020 | 2026-08-25 | SOBP 실시간 발급(NDP 연동) | 규정 없음 → **§7 신설**: 서버(NDP) 중심 할당 · Book 내 Page 1..N 연속 배정 · **배경 등록 전 인쇄 금지** · **재사용 금지 기본** · 오프라인 사전 확보 블록도 원장은 '할당됨' | 프린터 드라이버 SOBP 할당 설계 문서 수령(2026-08) | §7, [SOBP 할당 참조](NcodeCenter-SOBP-Allocation.md) | 🟡 API 스펙 대기 |
| PC-021 | 2026-08-25 | SOBP 할당(NDP) 앱 화면 | `코드 관리 정보 ▸ SOBP 할당(NDP)` 탭 신설 → **미구현(탭 제거)**. 정책 §7 + 참조 문서로만 관리 | 화면까지 둘 필요 없다는 결정 — 정본이 문서에 있음 | `InfoView` 탭 4개 복원, IA `INF-01-4` 삭제 | ✅ |
| PC-022 | 2026-08-25 | 화면 기획 문서 체계 | 메뉴별 MD(`docs/menus/`) → **화면코드 단위 PRD**(`docs/prd/{화면code}_{화면명}.md`) 17개. 기준 IA = `NcodeCenter_001_IA_001` v2.0 | 개발 전달용 화면 명세 필요. 사내 PRD 관례(1화면=1파일)와 정합 | `docs/prd/`, 구조MD §8, IA | ✅ |
| PC-023 | 2026-08-25 | 메뉴 구조 — 서비스 축 도입 | 그룹 **"프로젝트 관리"**(코드/편집/PUI) → **"서비스 관리 ▸ CasterN 서비스 관리"**. 사내 Ncode 사용 **서비스별 관리 메뉴** 체계로 전환, 미구현 서비스는 `/services/{key}` 예정 화면. **코드 프로젝트(전 서비스 공통 조회)는 [코드] 그룹으로 이동** | NcodeCenter의 범위 = casterN 한정이 아니라 **Ncode를 쓰는 모든 사내 서비스의 프로젝트 현황(집계·상태) 관리** | `menu.ts` `SERVICE_MENUS`·`/services/[key]`·구조MD §4·IA·PRD | ✅ |
| PC-024 | 2026-08-25 | 서비스 관리 메뉴 범위 | 서비스 5종 메뉴(예정 포함) → **아이글·NeoStudio2·Ncode 프린터 관리 메뉴 삭제**. 남는 메뉴 = **CasterN(구현) · 폼솔루션(예정)** | 세 서비스는 별도 관리 화면이 필요 없음 — 코드는 `코드 프로젝트`의 사용 서비스 필터로 조회(Ncode 프린터의 실시간 발급 규칙은 §7 문서로 관리) | `menu.ts` `SERVICE_MENUS`·구조MD §4·IA(SVC 재번호)·PRD-00·prd/README | ✅ |
| PC-025 | 2026-08-25 | App Key 계정 — 사용처(연동 서비스) | 사용처 = `웹 편집툴 / SDK` (표기용) → **연동 서비스 값**(`CASTERN` · `FORMSOLUTION` · `SDK`)으로 확정. 계정·App Key에 저장하고 **해당 서비스에서만 로그인** | 서비스별로 자기 계정만 인증·관리해야 함 | `accountStore`(service·`canLogin`)·`TicketsView` App Key 폼/목록·`customer_users.service`(DB.md·migration·dbSchema) | ✅ |
| PC-026 | 2026-08-25 | 사용 서비스 항목 | casterN·아이글·폼솔루션·NeoStudio2·**Ncode 프린터**·서비스없음(6종) → **casterN · 폼솔루션 · 서비스없음(코드만 발급) 3종**. 아이글·NeoStudio2·Ncode 프린터 **폐지**(PC-010·PC-019 대체) | 실제 관리 대상 서비스만 남긴다 — 나머지는 코드만 발급으로 처리 | `customerData.ts` `SERVICE`·`SOB-02` 할당 모달·`ProjectsView` 필터·`dbSchema`·정책정의서 P-13 | ✅ |
| PC-027 | 2026-08-25 | 편집 단가 적용 시점 | 고객사 단가 변경이 **전체 교재에 소급 적용** → **교재는 등록 시점 단가로 고정**. 변경 단가는 **이후 등록 교재부터** 적용, 기존 교재 청구액 유지(개별 갱신 가능) | 과거 정산 금액이 단가 변경으로 바뀌면 청구·정산 근거가 흔들림 | `EditingDetailView`(교재 단가 스냅샷 `rs`/`rsAt`)·`CompaniesView` 안내·PRD MEM-02·PRJ-03·PRJ-04·P-16 | ✅ |
| PC-028 | 2026-08-27 | SOBP 자동 추천 | 조건(판형·권당 페이지·권수)으로 PDS·Section·Owner·Book 을 추천하는 **자동 추천 모드** → **폐기**. `SOB-02` 는 모드 토글 없이 항상 **직접 선택**이며, 발급 대상은 `SOB-01` 에서 사람이 고른다 | 실제 진입 경로가 없어 사용되지 않았고, 발급 좌표는 담당자 판단이 필요하다는 결정 | PRD `SOB-02` v1.1·화면정의서 SOB-02(10상태)·`OwnershipMap` 할당 창 | ✅ |
| PC-029 | 2026-08-28 | App Key 계정 — 고객사당 계정 수 | 한 고객사에 **계정 1개**(발급 흐름 안에서만 등록) → **계정 여러 개**(개수 **제한 없음**). `[＋ 계정 추가]`로 언제든 추가하고, **`[계정만 등록]`으로 App Key 없이 계정만** 먼저 만든다. 목록에서 `[이 계정으로 발급]`으로 대상 계정을 바꾼다 | 한 고객사에서 담당자별·서비스별로 로그인 계정을 나눠 써야 함 (ID(email)는 전체에서 유일, 계정 수는 무제한) | `accountStore`(`accountsOfCompany`)·`TicketsView` App Key 폼/서비스 DB 목록·PRD TKT-01 v1.2·화면정의서 TKT-01(S3·S4·S11·**S13**) | ✅ |
| PC-030 | 2026-08-28 | 티켓 발급 메뉴 위치 | **[코드] 그룹의 단일 화면 `티켓 발급`** + 화면 안 좌측 발급 메뉴(N Key / 계정+App Key / 발급 목록) → **사이드바 [티켓 발급] 그룹 3개 화면**: `TKT-04` **N Key 발급**(`/tickets/nkey`) · `TKT-01` **계정 발급**(`/tickets/account`) · `TKT-03` **발급 목록 · 정산**(`/tickets/list`). 화면명 **계정 + App Key → 계정 발급**, **티켓 발급 → N Key 발급**. 메뉴 순서 = **코드 → 티켓 발급 → 서비스 관리 → 멤버 관리 → 정보** | 발급 업무를 코드 관리와 분리해 좌측 메뉴에서 바로 고르게 한다 (화면 안 탭 이중 메뉴 제거) | `menu.ts`·`/tickets/{nkey,account,list}`·`TicketsView`(tab prop)·IA §1·§2·구조MD §4·PRD-00 §4.4·PRD TKT-04 v1.2·TKT-01 v1.3·TKT-03·화면정의서 사이드바 | ✅ |
| PC-031 | 2026-08-28 | 계정 발급 화면 · CasterN 권한 | 발급 폼 1장(계정 선택 + App Key 동시 발급) → **목록(`TKT-01`) + 등록·상세 수정(`TKT-02`) 분리**. 등록은 **① 계정 정보 → ② 사용처·권한 → ③ App Key(선택)** 3단계이고 **App Key 발급은 선택**(계정만 등록 후 상세에서 발급 가능). 계정에 **CasterN 권한 7종**(프로젝트 생성 · 심볼 편집 · 리소스 편집 · Ncode PDF 내보내기 · NCP2 내보내기 · App용 패키지 내보내기 · App 페이지 설정)을 **개별 또는 모두** 지정. 티켓 발급 메뉴 순서 = **계정 발급 → N Key 발급 → Key 발급 정산**, 화면명 **발급 목록 및 정산 → Key 발급 정산** | 계정을 목록으로 관리하고, CasterN이 계정 단위로 기능 권한을 나눠 쓴다는 전달(권한 7종) 반영 | `accountStore`(`perms`·`CASTERN_PERMS`·`updateAccount`)·`AccountsView`·`/tickets/account/{new,[id]}`·`menu.ts`·IA §1·§2·구조MD §4·PRD-00 §4.4·PRD TKT-01 v2.0·**TKT-02 신규**·TKT-03 v1.2·화면정의서 TKT-01(5상태)·TKT-02(9상태) | ✅ |
| PC-032 | 2026-08-28 | SOBP · 코드 종류 모델 | **PDS2/PDS3 안에서 SOBP 를 지정**하고 중복을 막던 방식 → **좌표(SOBP)를 먼저 지정하고 그 좌표의 속성으로 종류를 구분**한다: **PDS2 · PDS3 · PDS4(S-code = Section 44) · OID**(+IDS 이력). 좌표가 유일하므로 중복은 성립하지 않고, 해당 좌표가 어떤 종류인지만 가린다. 한 (S,O)는 PDS2·PDS3·PDS4 중 한 종류만 쓰고 **OID만 같은 S/O 를 공유**(B/P로 구분)한다. **펜 구분(소리펜 NSP·필기펜 NWP)도 좌표 속성**으로 관리한다 | 코드 체계를 좌표 중심으로 정리해 중복 판정을 없애고, 외부 코드 판독용 OID 발급분을 분류·검색할 수 있게 한다 | `build_all_sources.py`(OID k=O·pen)·`codeKind.ts`·`sobp.tsx`(KindChip·PenChip)·`OwnershipMap`(종류·펜 필터)·`ProjectsView`·`EditingProjectsView`·`TicketsView`(PatternType Scode/OID)·`NcodeInfoView`·정책정의서 P-01~03·PRD SOB-01·SOB-02·PRJ-01·INF-01 | ✅ |
| PC-033 | 2026-08-28 | OID 관리 방식 | PDS2·PDS3·PDS4 와 **같은 좌표(SOBP) 축**에서 종류로 구분 → **좌표 관리 대상에서 분리**. OID는 **index 만 갖는 코드**(외부 코드를 우리 펜으로 판독, 펜으로 찍으면 코드 값 1개, 총량 약 6만)로 분량이 적으면 **book 미분할**이며, 업체 구분은 S/O·분량이 늘어난 업체만 **book 번호(=OID index)** 로 나눈다. → **업체 + index 대장(`INF-04` /oid)** 신설, 기존 SOBP 좌표는 **메모**로만 표기. SOBP 맵·코드 프로젝트·편집 목록의 종류 축에서 OID 제거 | OID는 좌표로 발급·점유하는 코드가 아니라 index 부여 수단이라, 좌표 축에 섞으면 무겹침·정원 관리가 왜곡된다 | `build_all_sources.py`(oid-data.json 분리·code-usage 에서 제외)·`oidLedger.ts`·`OidView`(/oid)·`menu.ts`·`codeKind.ts`(CODE_KINDS 에서 OID 제외)·`OwnershipMap`·`ProjectsView`·`EditingProjectsView`·`NcodeInfoView`·정책정의서 P-02-2·**PRD INF-04 신규**·INF-01·SOB-01 | ✅ |
| PC-034 | 2026-08-28 | OID 화면 위치 | 사이드바 **[코드] ▸ [OID 관리대장]** 별도 메뉴(`/oid`) → **메뉴 분리 취소**. 화면은 **코드 관리 정보(`INF-01`)의 [OID 관리대장] 탭**으로 편입한다(탭 순서: Ncode Info · 확장 언어 슬롯 · 발급 구조 · **OID 관리대장** · 알아야 할 사항). SOBP 맵 툴바 링크도 `[OID는 별도 관리 →]` → 코드 관리 정보 탭으로 연결 | 사용자 지시(2026-08-28) — 메뉴를 새로 빼지 않고 코드 관리 정보 안에서 관리한다. OID **관리 방식 분리(PC-033)는 그대로 유지** | `menu.ts`(항목 제거)·`/oid` 라우트 삭제·`InfoView`(탭 5개)·`OidView`(embedded)·`OwnershipMap` 링크·IA §1·§2·구조MD §4·PRD-00 §4.4·PRD INF-04 v1.1·INF-01 v1.4·SOB-01·화면정의서 shell/p_inf01/p_oid01 | ✅ |
| PC-035 | 2026-08-28 | OID 용어·필터 · 직접 할당 재선택 | ① **IDS(A코드) = OID 동일 용어**로 통합(별도 종류·라벨 폐기) ② 코드 종류 = **PDS2 · PDS3 · PDS4 · OID 4종**, SOBP 맵·코드 프로젝트에서 **OID 로 필터·검색** 가능(PC-033 의 '좌표 관리 대상 아님' 표현·[OID는 별도 관리] 링크는 삭제 — OID 데이터는 다시 지도 데이터에 포함) ③ **직접 코드 할당(SOB-02)** 에서 **코드 종류 · Section · Owner 를 모달 안에서 다시 선택**(각각 단일 선택, 다중 선택 없음) | IDS/OID 표기가 혼재해 필터가 갈렸고, 할당 창에 들어온 뒤 대상을 못 바꿔 지도로 되돌아가야 했다 | `codeKind.ts`(CodeKind 4종·A→OID)·`build_all_sources.py`(OID 행 code-usage 복귀)·`OwnershipMap`(칩 4종·재선택 UI)·`commonCodes.ts`·`EditingDetailView`·`NcodeInfoView`·`OidView`·정책정의서 P-02·P-02-2·PRD SOB-01 v2.2·SOB-02 v1.3·INF-01·INF-04·화면정의서 p_sob01·p_sob02 | ✅ |
| PC-036 | 2026-08-28 | book 미분할 OID 노출 | 북코드가 없는 행은 데이터 빌드에서 **전량 제외**(중복/변형 표기 방지) → **OID 의 book 미분할 행은 지도에 싣는다**(`nb=1`, 35행). SOBP 맵에서 **OWNER 까지만** 표시하고 Book 카드로는 쓰지 않으며, BOOK 열 맨 위에 **『book 미분할 · {n}건』 안내 + 항목 목록**을 보여준다 | OID 는 분량이 적으면 book 을 나누지 않는데(한솔 S3/O25 등) 그 행이 빠져 지도에서 업체가 아예 보이지 않았다 | `build_all_sources.py`(OID nb 행 code-usage 추가)·`codeUsage.ts`(BookRec.nb · book 맵에서 제외)·`OwnershipMap`(OWNER 표시·BOOK 안내·Page 패널 종류 표기)·PRD SOB-01·INF-04 | ✅ |
| PC-037 | 2026-08-28 | 펜 필터 폐지 · 편집 이력 코드 종류 | ① SOBP 맵의 **펜 구분 필터(펜 전체/소리펜/필기펜) 폐지** — 펜 값은 좌표 속성·배지로만 남긴다(필터 불필요) ② **편집 프로젝트(목록·상세)의 코드 종류를 PDS3·PDS2·PDS4·OID 4종**으로 구분(기존 N/G 2종) ③ OID 로 작업한 교재가 편집 이력에서 빠지던 문제 수정 — **book 미분할 OID 행을 편집 데이터에도 포함**(예: 한솔교육 S3/O25 『Ready Readers Book』·『신기한 영어나라』) | 펜 필터는 실사용 가치가 없고, 편집 이력도 코드 종류로 갈라 봐야 한다는 요청 | `build_all_sources.py`(nb 필터에서 OID 예외)·`OwnershipMap`(펜 필터 제거)·`EditingProjectsView`(종류 4종·배지)·`EditingDetailView`(종류 필터·셀)·정책정의서 P-03·PRD SOB-01 v2.4·PRJ-02·PRJ-03·화면정의서 p_sob01 | ✅ |
| PC-038 | 2026-08-28 | 첫 화면 기준 | ① **SOBP 맵 종류 칩**을 고르면 그 종류의 **첫 좌표(S→O→B)** 로 이동한다 — 데이터가 있는 좌표부터 보여준다(없으면 원장의 첫 owner → 첫 Section) ② **편집 프로젝트 첫 화면**에서 특정 고객사를 미리 고르지 않고 **전체 고객사 요약 표**(고객사 · 코드 종류 · 북 · 심볼 · 리소스 · 청구액)를 보여준다. 표의 한 줄을 누르면 상세로, 상세에서 **[← 전체 고객사]** 로 복귀 | 종류를 바꿀 때마다 빈 좌표부터 나와 직접 찾아야 했고, 편집 화면은 특정 고객사(Common-21)로 열려 전체 현황을 볼 수 없었다 | `OwnershipMap`(firstSobpOf)·`EditingProjectsView`(초기 선택 없음·전체 요약 표)·`EditingDetailView`(종류 배지)·PRD SOB-01 v2.5·PRJ-02 v1.4·화면정의서 p_sob01·p_prj02 | ✅ |
| PC-039 | 2026-08-28 | 종류 배타 폐지 · 코드 필터 | **좌표(SOBP)가 코드 종류보다 상위**라는 개념을 끝까지 적용한다. ① 「같은 (S,O)는 PDS2·PDS3·PDS4 중 한 종류만」 **배타 규칙과 🚫 영역 할당됨 표시·차단을 폐지** — 좌표를 할당하고 그 좌표가 어떤 종류인지는 **용도 표시**일 뿐이다 ② SOBP 맵 상단을 **「코드」 필터 그룹(전체 · PDS2 · PDS3 · PDS4 · OID)** + **「상태」 필터 그룹**으로 묶고 고객사 검색을 오른쪽으로 정리 ③ **전체** 보기 신설(종류 무관 좌표 전체 · 섹션 정원은 종류별 최대치) · OWNER/BOOK 카드에 **종류 배지**로 용도 표시 | 실데이터 확인 결과 한 고객사가 같은 (S,O) 안에서 Book 을 나눠 PDS2·PDS3 를 함께 쓰는 사례가 **7건**(구몬D-37 · 대교-31 · 교원구몬-10 · J research-1017 · 러닝루츠-1020 · NeoLAB FW TEST-42 · NeoLAB FW SET-44) 있어 기존 배타 규칙이 데이터와 어긋났다. (S,O,B) 까지 겹치는 것은 **S3/O1017/B1 1건**뿐 — 별도 확인 필요 | `OwnershipMap`(배타 로직·차단 UI 제거 · 코드/상태 필터 그룹 · 전체 보기 · 종류 배지)·`codeKind.ts`(표시 순서 PDS2 우선)·정책정의서 P-02-1·PRD SOB-01 v2.6·SOB-02 v1.4·화면정의서 p_sob01·p_sob02 | ✅ |
| PC-040 | 2026-08-28 | 전체 고객사 화면 · 카드 칩 통일 | ① 편집 프로젝트 **첫 화면(전체 고객사)** 을 고객사 요약 표 → **고객사 상세와 같은 교재 표**로 통일 — **항목 셀렉트 필터(상태·고객사·사용 고객사·코드·타입·편집방식)** 와 **페이지네이션(50/100/200/500/전체)** 을 그대로 쓴다. 전체 보기는 **조회 전용**(수정은 고객사 선택 후)이며 **고객사 열**이 추가된다 ② SOBP 맵 **OWNER·BOOK 카드의 칩 순서를 통일** — **[번호] → [상태] → [종류(용도)] → (펜)** | 전체 화면에서 같은 방식으로 걸러 보고 싶다는 요청 · 같은 상태 값인데 O 카드와 B 카드의 칩 위치·줄바꿈이 달랐다 | `EditingDetailView`(allCustomers 모드·고객사 열/필터·읽기 전용)·`EditingProjectsView`(전체 화면 교체)·`OwnershipMap`(칩 순서)·PRD PRJ-02 v1.5·PRJ-03 v1.3·SOB-01 v2.7·화면정의서 p_prj02·p_sob01 | ✅ |
| PC-041 | 2026-08-28 | S/O 내 종류 혼용 — 신규 금지·이력 유지 | **신규 발급은 한 (S,O) 안에서 한 종류만** 한다(PDS2·PDS3 혼용 금지). 이미 혼용된 **과거 이력은 그대로 표시**한다(당시 허용 정책) — `S3/O42` NeoLAB FW TEST · `S3/O44` NeoLAB FW SET · `S3/O1020` 러닝루츠(PDS2 B20~35 · PDS3 B1~11 — 한 고객사·Book 미겹침). **원장의 동일 (S,O,B,종류) 중복 행 49건**(다국어 분할 등)도 과거 이력으로 **그대로 유지**한다. 직접 코드 할당에서 그 S/O 가 다른 종류로 쓰이고 있으면 **안내 + [할당] 비활성**(OID 는 index 부여라 제외). 데이터 정리: **구몬D-37 S3/O37 의 PDS3 B669(데일리노트) 는 발급 없음** → 빌드에서 제외(담당 확인). **원장 보강 프로젝트는 코드 종류 미정**으로 두어 지도 배지에 PDS3 로 잘못 표시되던 것을 바로잡음 | 담당 확인(2026-08-28) + 소리펜 원장 재업로드 반영. 재확인 결과 (S,O,B) 중복 **0건**, (S,O) 혼용 **3건**(S3/O42 · S3/O44 · S3/O1020) | `build_all_sources.py`(구몬D B669 제외 · 보강 프로젝트 kind 미정)·`codeUsage.ts`(kindSet)·`OwnershipMap`(신규 할당 혼용 차단·배지 정확도)·PRD SOB-01 v2.8·SOB-02 v1.5 | ✅ |
| PC-042 | 2026-08-28 | PDS4(S-code) 발급 범위 정식 수록 · 탭 개명 | Section **44 = PDS4(S-code)** 의 범위를 **owner 0~4095 · bookcode 0~255 · page 0~255 · xy 0~255** 로 확정해 **Code Info 표에 3번째 열**로 넣는다. PDS4 는 판형(**length**) 대신 **xy** 를 갖는다. 지금까지 “표에 없음 · 원장 관측치 · 개발팀 확인 필요” 로 적어 둔 주석을 지운다. 함께 **[Ncode Info] 탭 이름을 [Code Info]** 로 바꾼다(PDS2·PDS3·PDS4 를 함께 다루므로) | 담당 전달(2026-08-28) — `s44 o4095 b255 p255 xy255` | `InfoView.tsx`(SECTIONS·KEYS4·탭)·`OwnershipMap`(PDS4 owner 정원 1024→4096)·PRD INF-01 v1.6·화면정의서 `p_inf01.py` | ✅ |
| PC-043 | 2026-08-31 | PUI 용어 정정 — 페이퍼 UI | **PUI = Paper UI(종이 UI)** 의 약자다. 그동안 *Physical UI(피지컬)* 로 잘못 적어 온 표기를 모두 정정한다. 메뉴명 **`PUI 코드 (피지컬)` → `PUI 코드 (페이퍼)`**, 화면 부제 *피지컬 조작(종이 위 컨트롤러)* → **종이 위 컨트롤러(Paper UI)**, 문서의 *피지컬 기능표·피지컬 버튼* 표기도 **페이퍼 UI(PUI)·종이 위** 로 바꾼다. 경로(`/pui`)·화면번호(`PRJ-06`)는 그대로 | 담당 확인(2026-08-31) — PUI 의 뜻이 Paper UI 로 확인됨 | `menu.ts`(라벨·features)·`PuiView`(부제)·PRD PRJ-06 v1.2·PRD-00 §메뉴표·IA·IA대조표·Structure·개발팀리뷰·화면정의서 `shell.py`·`p_prj06.py`·`build_pui_data.py` | ✅ |
| PC-044 | 2026-08-31 | 공통코드 레지스트리 정리 — 3건 제외 | 공통(커먼)코드는 **여러 고객사가 함께 쓰는 코드**만 등록한다. `PC-009` 가 이미 “사용 고객사 0 = 공유 아님”(팝펜스티커-26 제외)을 기준으로 세웠는데, 같은 회차에 추가된 3건이 그 기준에 맞지 않아 **레지스트리에서 뺀다** — **NLJ NTT-940**(N3/O940 · 원장 4행, 사용 고객사 = 자기 자신 `NLJ NTT` 1곳) · **딥스원테크-303**(N14/O303 · 8행, 자사 `네오랩컨버전스` 1곳) · **구몬D-37**(G3/O37 · 383행, 사용 고객사 이력 **0곳**). 세 코드는 일반 고객사 코드로 남고, 고객사 등록·수정의 **[공통코드 사용 고객사(하위 등록)]** 목록에서 사라진다. ※ **Common 추가 언어 슬롯-964**·**네오노트-IDS-27**(이력전용) 은 사용 고객사가 0곳이어도 Common 체계라 유지 | 담당 지적(2026-08-31) — “공통코드가 아닌데 왜 들어가 있나”. 원장 재확인으로 확인됨 | `commonCodes.ts`(3건 제거·등록 기준 주석)·`build_all_sources.py`(COMMON_CODES)·`common-members.json`(10키→7키)·하위 회사 레코드 `NLJ NTT` 자동생성 해제(고객사 532→531)·PRD MEM-02 v1.4·화면정의서 `p_mem02.py` | ✅ |
| PC-045 | 2026-08-31 | 코드 발급 경로 정리 · 편집 화면 고객사 식별 | ① **공유(커먼) 코드는 사용 고객사에만 발급** — [고객사 관리]에서 **공통코드 사용 고객사(하위 등록)** 로 체크하지 않은 고객사는 `SOB-02` 에서 그 코드를 발급받을 수 없다(🚫 안내 + [할당] 비활성). 보유(대표) 고객사는 예외, 레지스트리에 없는 임의 공유 OWNER 는 검사 대상 아님 ② **편집 프로젝트의 Owner 직접 입력 폐지** — 할당된 코드가 없는 고객사는 편집 고객사로 추가할 수 없고 **코드 할당은 `SOB-02`(SOBP 맵) 하나의 경로로만** 한다 ③ **교재 화면의 고객사 식별** — 주소에 `?c={고객사}` 를 실어 owner 번호가 겹치는 고객사끼리 섞이지 않게 한다 | 담당 지적(2026-08-31) 4건 — 공유 코드 미사용 고객사 발급 가능 · 편집에서 직접 발급 가능 · 신규 고객사 교재 추가 시 좌표 오류(S0/O4 → S5/O4) · 커먼 코드 교재 클릭 시 다른 교재 노출. ③·④ 는 **owner 번호만으로 고객사를 찾던 것**이 원인으로 확인 | `OwnershipMap`(commonBlock)·`EditingProjectsView`(직접 입력 제거)·`EditingDetailView`(`?c=` 식별·조건부 훅 정리)·`app/projects/editing/[owner]/page.tsx`(Suspense)·PRD SOB-02 v1.6·PRJ-04 v1.3·PRJ-05 v1.1·화면정의서 `p_sob02.py`·`p_prj05.py` | ✅ |
| PC-046 | 2026-08-31 | 코드 할당 모달 정리 · Book 번호 더 보기 | ① **직접 코드 할당 모달을 세 묶음**(발급 대상 · 발급 정보 · 보유 코드)으로 재구성하고 설명 문구·툴팁을 걷어낸다. **Section·Owner 셀렉트 폐지** — 좌표는 지도에서 고른 값이라 **상태 배지**(🔒 전용 · 공유 OWNER · 사용 중 · 미발급)로만 보여 준다. 전용처럼 **추가 발급 대상이 아닌 좌표**도 잠금 문구 대신 상태 한 줄. **코드 종류는 모달 로컬 값**이라 바꿔도 **S/O 가 바뀌지 않는다**(이미 쓰는 종류가 있으면 그 종류로 고정). 폐기된 자동 추천(`PC-028`) 잔여 UI·상태도 제거 ② **편집 교재의 Book 번호**를 300개에서 끊지 않고 **100개씩 + [＋100개 더 보기]** 로 늘려 본다 | 담당 지적(2026-08-31) — 종류를 바꾸면 S/O 가 초기화된다 · 모달 설명이 많고 복잡하다 · 4,000번대 Book 이 목록에 안 나온다 | `OwnershipMap`(모달 재구성·aKindSel·추천 UI 제거)·`EditingDetailView`(BOOK_STEP 100 · 더 보기)·PRD SOB-02 v1.7·PRJ-04 v1.4·화면정의서 `p_sob02.py` | ✅ |
| PC-047 | 2026-08-31 | 코드 할당 모달 배치 | ① 창 **제목은 「코드 할당」** 만 — S/O 는 바로 아래 **[발급 대상]** 에 있어 중복이다 ② **코드 종류 표시 순서를 PDS2 · PDS3** 로 맞춘다(공통코드 목록·지도 필터와 같은 순서 `PC-039`). 기본 선택은 종전대로 PDS3 ③ **고객사 · 사용 서비스 · 코드 종류를 한 행**(3열)에 둔다 | 담당 지적(2026-08-31) | `OwnershipMap`(모달 제목·ALLOC_KINDS 순서·3열 배치)·PRD SOB-02 v1.8·화면정의서 `p_sob02.py`(cust_svc 3열·kind_chips) | ✅ |
| PC-048 | 2026-08-31 | 이미 발급된 좌표의 할당 창 표기 | 전용 좌표 문구를 **“{업체명}” 전용으로 추가 발급이 불가** 로 바꾼다. 이미 발급된 좌표에서는 **고객사를 입력 칸이 아니라 보유 업체 상태**로, **코드 종류도 배지(상태)** 로만 보여 준다(설명 문구 삭제). **사용 서비스만 현재 지정된 값을 띄우고 바꿀 수 있게** 하며, 버튼은 **[사용 서비스 저장]** 으로 바뀌어 그 좌표의 사용 서비스만 고친다(코드는 새로 발급하지 않는다 `P-05`) | 담당 지적(2026-08-31) — 고를 수 없는 값이 입력 칸으로 보인다 | `OwnershipMap`(usedSO·svcValue·saveService)·PRD SOB-02 v1.9·화면정의서 `p_sob02.py` | ✅ |
| PC-049 | 2026-08-31 | 잔여 페이지 종류별 표기 · 사용 서비스 복수 선택 | ① **아직 발급되지 않은 좌표**는 코드 종류가 정해지지 않았으므로 **잔여(발급 가능) 페이지를 PDS2 · PDS3 로 나눠** 보여 준다(Section 별 page 정원이 종류마다 다르다 — 예 S0: PDS2 1,024p · PDS3 4,096p). **이미 발급된 좌표**(계정 발급 · N Key 발급 · 편집 프로젝트 Book 사용)는 종류가 정해져 있으므로 **잔여 페이지 하나**만 보여 준다 ② **직접 코드 할당의 사용 서비스는 복수 선택** — 한 좌표를 여러 서비스가 함께 쓸 수 있다. **서비스 없음은 단독 선택**. 데이터는 `Project.services[]` 로 두고 옛 `service` 단일 값도 함께 읽는다 | 담당 지시(2026-08-31) | `OwnershipMap`(PageView caps · 서비스 칩)·`customerData`(services · projectServices · usesService)·`ProjectsView`(배지·필터)·PRD SOB-01 v2.10 · SOB-02 v1.10 · 화면정의서 `p_sob01.py`·`p_sob02.py` | ✅ |
| PC-050 | 2026-08-31 | Key 관리 행 클릭 · App Key 계정당 1개(사용처 공통) | ① **Key 관리 목록의 레코드(행)를 누르면 발급 상세**로 간다 — 발급 내용 링크만 눌러야 하던 것을 행 전체로 넓힌다(행 안의 [정산]·[상세]·[삭제]는 행 이동과 분리) ② **App Key 는 계정당 1개**이고 그 계정의 **사용처(CasterN·폼솔루션·SDK) 전체에 공통**으로 쓰인다. 「CasterN 사용처 전용」과 사용처 제거 시 **「연동 끊김」 처리는 폐지**한다 ③ App Key 발급 시 **Book Start · Book Volume(권수)** 를 지정한다 — **Book End = Start + Volume − 1** 이며 할당 SOBP 범위 안으로 제한한다 ④ App Key 발급 UI 를 사용처 탭 밖 **③ 단계**로 빼고 범위·Book·만료를 한 줄에 둔다 | 담당 지시(2026-08-31) | `TicketsView`(행 클릭)·`accountStore`(AppKey.services[]·bookVol·계정당 1개)·`AccountsView`(BookRangeFields·③ 단계 분리)·PRD TKT-02 v1.4 · TKT-03 v1.3·화면정의서 `p_tkt02.py`·`p_tkt04.py` | ✅ |
| PC-051 | 2026-08-31 | 코드 종류 지정 시점 이동 · App Key 배치 | ① **직접 코드 할당(`SOB-02`)에서 코드 종류 선택을 없앤다** — 좌표(S/O)만 점유하고 새 할당은 **코드 종류 미정**으로 남는다 ② **코드 종류(PDS2·PDS3·PDS4)는 `N Key 발급`·`App Key 발급`·`편집 프로젝트`에서 지정**한다 ③ N Key 발급의 **Page 볼륨 상한을 회사에 할당된 SOB(코드 종류·Section)의 정원**으로 묶는다 ④ **App Key 발급을 사용처·권한 위(② 단계)로** 올리고, Book 입력을 N Key 발급과 같은 표기(**Start Book · {min}~{max}** / **Book 볼륨 (권) · 최대 N**)로 맞춘다. 값은 불러온 SOBP 의 Book 범위가 자동으로 채워지고 화살표·직접 입력으로 고칠 수 있다 | 담당 지시(2026-08-31) | `OwnershipMap`(종류 선택 제거·issued.kind 미기록)·`TicketsView`(Page 볼륨 상한)·`AccountsView`(② App Key·Book 라벨)·PRD SOB-01·SOB-02·TKT-02·화면정의서 `p_sob02.py`·`p_tkt02.py` | ✅ |
| PC-052 | 2026-08-31 | 계정 발급 화면 정리 · 편집 프로젝트 코드 종류/Book 필터 | ① **계정 등록·상세의 ①②③ 번호를 없애고** 계정 정보 · App Key 발급 · 사용처·권한을 **영역 묶음(카드)** 으로 정리한다 — `TKT-05` 발급 상세와 같은 결로 맞춘다 ② **App Key 는 SOBP 맵에서 발급된 S/O** 를 고르면 그 **Book 범위가 따라오고**, 그 안에서 **Start Book · Book 볼륨(권)** 을 지정한다 ③ **편집 프로젝트 [＋교재(책) 추가]** 에서 **할당된 S/O · Book(사용 가능 번호, 100개씩 더 보기) · 코드 종류(PDS2·PDS3·PDS4·OID)** 를 고른다 — 좌표 할당에서 종류를 정하지 않기 때문이다 `PC-051` ④ 편집 프로젝트 목록 **S/O/B 열에 Book 필터**(B 낮은순 · B 높은순 · B 직접 입력)를 둔다 | 담당 지시(2026-08-31) | `AccountsView`(SecHead·SEC 카드·S/O 라벨)·`EditingDetailView`(코드 종류 선택·B 필터/정렬)·PRD TKT-02 v1.5 · PRJ-03 v1.4 · PRJ-04 v1.4 · 화면정의서 `p_tkt02.py` | ✅ |
| PC-053 | 2026-08-31 | SDK 연동(코드만 할당) 용어 통일 · 계정 목록 정리 | ① 코드 할당(`SOB-02`)의 사용 서비스 **「서비스 없음 (코드만 발급)」 → 「SDK 연동 (코드만 할당)」** — 네오랩 서비스를 쓰지 않고 코드만 받아 직접 연동하는 경우다(값 `NONE` 은 그대로) ② 계정 발급의 사용처 **「SDK 연동」 → 「SDK 연동 (코드만 할당)」** 로 맞춘다 ③ 계정 발급 목록에서 **레코드(행)를 누르면 계정 상세**로 가고, **CasterN 권한 열은 삭제**한다(권한은 계정 상세에서 본다) | 담당 지시(2026-08-31) | `customerData`(SERVICE·serviceShort)·`accountStore`(ACCOUNT_SERVICES)·`AccountsView`(행 클릭·열 삭제)·`ProjectsView`(SHORT_SVC)·PRD SOB-02 v1.11 · TKT-01 v1.3 · 화면정의서 `p_tkt03.py` 외 | ✅ |
| PC-054 | 2026-08-31 | 교재 편집 화면 · Book 선택 정리 | ① **교재(책) 추가·편집 화면에서는 목록용 [초기화]·[＋교재(책) 추가] 버튼을 노출하지 않는다** ② Book 의 **＋100개 더 보기를 셀렉트 목록 맨 끝 항목**으로 넣어 목록 안에서 이어 볼 수 있게 한다 ③ **코드 종류는 Book 선택 다음**에 둔다(할당 S/O → Book → 코드 종류) ④ 편집 프로젝트 목록의 **[B 직접 입력]은 셀렉트 자리를 입력칸으로 바꿔** 바로 번호를 넣는다(✕ 로 B 전체 복귀) | 담당 지시(2026-08-31) | `EditingDetailView`(bookMode 버튼 숨김·Book 셀렉트 더 보기·필드 순서·B 필터 인라인 입력)·PRD PRJ-03 v1.5 · PRJ-04 v1.5 | ✅ |
| PC-055 | 2026-08-31 | 편집 화면 이동 버튼 표기 | 편집 프로젝트의 **교재 등록·상세 상단 이동 버튼을 [← 편집 프로젝트] → [목록]** 으로 바꾼다. 같은 화면 하단의 이동 버튼은 그 고객사의 교재 목록으로 가므로 **[교재 목록]** 으로 구분해 적는다 | 담당 지시(2026-08-31) | `EditingDetailView`(상단·하단 링크 라벨)·PRD PRJ-03 v1.6 · PRJ-04 v1.6 | ✅ |
| PC-056 | 2026-08-31 | [목록] 이동처 통일 | 교재 등록·상세의 **상단·하단 [목록] 을 둘 다 그 고객사의 교재 목록**(`/projects/editing/{owner}?c={고객사}`) 으로 보낸다. 교재 목록 화면 자신은 상단 버튼을 **[편집 프로젝트]** 로 두어 상위 목록으로 나간다 | 담당 지시(2026-08-31) | `EditingDetailView`(상단 링크 bookMode 분기·하단 라벨)·PRD PRJ-03 v1.7 · PRJ-04 v1.7 | ✅ |
| PC-057 | 2026-08-31 | 사용 서비스 기준 고객사 연동 · Book 선택 개선 | ① 교재 등록·상세의 **Book 선택기는 [＋100개 더 보기] 를 눌러도 닫히지 않고** 아래로 이어 붙는다(네이티브 셀렉트 → 자체 드롭다운) ② 편집 고객사 추가는 **SOBP 맵에서 사용 서비스 = casterN 으로 지정한 고객사만** 후보로 둔다 ③ 계정 발급에서 고객사를 고르면 **그 고객사의 사용 서비스대로 사용처가 자동 체크**되고 그 서비스의 조건만 입력한다(casterN→CasterN · 폼솔루션→폼솔루션 · SDK 연동→SDK) ④ 사용 서비스로 지정한 고객사는 **각 서비스 관리 화면의 고객사 목록**으로 불러온다(폼솔루션 등 미구현 서비스도 목록은 제공) ⑤ 편집 프로젝트 목록의 **B 필터는 입력칸에 번호를 직접 치는 콤보박스**로 두고 낮은순·높은순은 목록에서 고른다 | 담당 지시(2026-08-31) | `serviceCustomers.ts`(신규)·`ServiceCustomersView.tsx`(신규)·`app/services/[key]/page.tsx`·`EditingProjectsView`(casterN 후보)·`AccountsView`(사용처 자동 체크)·`EditingDetailView`(BookPicker·B 콤보박스)·PRD PRJ-03 v1.8 · PRJ-04 v1.8 · PRJ-05 v1.2 · TKT-02 v1.6 | ✅ |
| PC-058 | 2026-08-31 | CasterN 권한 6종 | 계정 발급(App Key 발급)의 **CasterN 설정에서 「App 페이지 설정」 권한을 뺀다** — 권한은 **프로젝트 생성 · 심볼 편집 · 리소스 편집 · Ncode PDF 내보내기 · NCP2 내보내기 · App용 패키지 내보내기** **6종**이다(옛 데이터에 남아 있어도 화면에 나오지 않는다) | 담당 지시(2026-08-31) | `accountStore`(CasterPerm·CASTERN_PERMS)·`AccountsView`·`dbSchema`·PRD TKT-01 · TKT-02 v1.7 · 화면정의서 `p_tkt02.py`·`p_tkt03.py` | ✅ |
| PC-059 | 2026-09-01 | 계정 : 키 1:1 매핑 · 계정별 B·P 영역 | **키는 계정 단위로 매핑**한다. ① 한 고객사에 계정을 여러 개 두고, **계정마다 자기 Key 정보**를 갖는다 ② **좌표의 S/O 는 고객사가 갖고, 그 안의 B·P 영역은 계정마다 다르게** 잡는다 — App Key 발급에 **Start Book · Book 볼륨 · Start Page · Page 볼륨** 을 지정한다 ③ **한 계정의 App Key 1개 · N Key 1개**(1:1). N Key 발급 화면에 **발급 대상 계정** 선택을 두고 이미 발급된 계정은 후보에서 뺀다 ④ 계정 상세에 그 계정의 **Key 정보(발급 파라미터 전체)** 를 App Key · N Key 두 칸으로 보여 준다 | 담당 지시(2026-09-01) + 발급 상세 Key 정보 화면 이미지 | `accountStore`(AppKey.pageVol)·`ticketStore`(Ticket.accountId·ticketsOfAccount·hasNKey)·`AccountsView`(B·P 입력·KeyCard)·`TicketsView`(계정 선택·1:1 차단)·PRD TKT-02 v1.8 · TKT-04 v1.5 | ✅ |
| PC-060 | 2026-09-01 | App Key 발급 Key 정보 항목 정렬 | App Key 발급도 **N Key(`TKT-04`) 와 같은 Key 정보 항목**을 갖춘다. 고르는 값은 **S/O · Start Book · Book 볼륨 · Start Page · Page 볼륨 · 만료일**, 나머지는 **자동·고정**이라 화면에 회색으로 보여 준다 — **Section·Owner**(선택 범위) · **PatternType**(좌표 종류) · **TicketType**(무제한=Unlimited / 기간=Period) · **TicketVersion**(1). 티켓 파라미터 이름도 `IssuedTime`·`ValidUntilTime` 으로 N Key 와 통일한다(`SeparateEachBook` 은 티켓 파일 분리 옵션이라 App Key 에는 없다) | 담당 지적(2026-09-01) — 발급 상세의 Key 정보 항목이 App Key 발급 화면에 없다 | `AccountsView`(FixedKeyFields·issueAppKey params)·PRD TKT-02 v1.9 | ✅ |
| PC-061 | 2026-09-01 | N Key 관리 — 목록에서 App Key 제외 | 메뉴명 **「Key 관리 (N Key 발급)」 → 「N Key 관리」**. 목록은 **N Key 만** 다루고 **종류 필터·종류 열을 없앤다**. **App Key 는 계정 단위**(계정당 1개 `PC-059`)라 **계정 발급 ▸ 계정 상세의 [Key 정보]** 에서 본다(원장에는 그대로 남아 계정 상세가 참조한다) | 담당 지시(2026-09-01) | `menu.ts`·`TicketsView`(kind 필터·열 삭제)·`app/tickets/nkey/page.tsx`·`DashboardView`·PRD TKT-03 v2.1 · 화면정의서 `p_tkt04.py` | ✅ |
| PC-062 | 2026-09-01 | 계정 등록·상세 탭 구성 | 계정 등록·상세를 **탭 3개**로 나눈다 — **계정 정보 · 사용 서비스 및 권한 · App Key 발급**(상세는 App Key 탭에 [Key 정보]·발급 내역 포함). 탭에는 선택 개수(사용 서비스 n/3)·발급 여부 배지를 단다. 화면 문구의 **「사용처」 → 「사용 서비스」** 로 통일한다 | 담당 지시(2026-09-01) | `AccountsView`(AccTabs·탭 분기·문구)·PRD TKT-02 v2.1 · 화면정의서 `p_tkt02.py` | ✅ |
| PC-063 | 2026-09-01 | 메뉴명 App Key 관리 | 사이드바 **「계정 발급 (App Key 발급)」 → 「App Key 관리 (계정 발급)」**. `PC-061` 의 **N Key 관리**와 짝을 맞춰 **키 종류로 메뉴를 가른다**(App Key = 계정 단위 · N Key = 티켓 단위). 화면정의서 사이드바도 현재 구성(App Key 관리 · N Key 관리 2메뉴)으로 정정한다 | 담당 지시(2026-09-01) | `menu.ts`·`app/tickets/account/page.tsx`·`TicketsView`(안내 문구)·PRD TKT-01 v1.4 · 화면정의서 `shell.py` | ✅ |
| PC-064 | 2026-09-01 | Code Type 직접 선택 · 항목명 띄어쓰기 | ① 계정 등록의 **[목록] 버튼을 화면 아래**(취소·계정 추가와 같은 줄 왼쪽)로 옮긴다 ② **PatternType → Code Type** 으로 바꾸고 **PDS2 · PDS3 · PDS4 · OID 를 직접 고른다** — App Key 발급·N Key 발급 모두. 기본값은 고른 좌표의 종류이며, 티켓 파일의 `patternType` 값(Ncode_PDS3 등)은 고른 종류에서 환산한다 ③ **Key 정보 항목명을 띄어쓰기 표기**로 통일한다 — `IssuedTime → Issued Time` · `TicketVersion → Ticket Version` · `BookStart/BookVolume/BookEnd` · `PageStart/PageVolume/PageEnd` · `ValidUntilTime` · `SeparateEachBook` · `CompanyName` · `AccountId` · `AppKey` · `UsedCustomer` 모두 같은 규칙. 원장 시드(`ticket-ledger.json`)와 그 생성기도 함께 바꾼다(티켓 파일 자체의 camelCase 키는 그대로) | 담당 지시(2026-09-01) | `TicketsView`·`AccountsView`(Code Type 선택·라벨)·`NcodeInfoView`·`build_ticket_ledger.py`·`ticket-ledger.json`·PRD TKT-02 v2.2 · TKT-04 v1.6 · 화면정의서 `p_tkt01/02/04/05` | ✅ |
| PC-065 | 2026-09-01 | Ticket Version 의 의미 | **Ticket Version = 키 발급 항목 구성(스펙)의 버전**이다. 발급 건의 순번·재발급 횟수가 아니다. **현재 발급분은 모두 버전 1**이며, **항목이 바뀌면(추가·삭제·의미 변경) 버전 2** 로 올린다. 이미 발급된 키는 **발급 당시 버전 그대로** 남는다 — 그래서 Key 정보에서 고를 수 없는 **고정 값**으로 보여 준다. 이 내용을 **코드 관리 정보 ▸ 알아야 할 사항**에 수록한다 | 담당 설명(2026-09-01) | `NcodeGuideView`(⑤ Ticket Version)·PRD INF-01 v1.7 · 화면정의서 `p_inf01.py` | ✅ |
| PC-066 | 2026-09-01 | App Key 생성 규칙 — 영문·숫자 29자 | App Key 값은 **영문·숫자([0-9A-Za-z]) 29자 난수 문자열**로 만든다. 옛 `ncc_live_{hex}` 형식은 폐기한다. 접두어(`ncc_live_` 등) 없이 **항상 29자**다. 길이가 짧아 계정 상세·발급 내역에서 **자르지 않고 전부** 보여 준다 | 담당 지시(2026-09-01) | `AccountsView`(genKeyStr·표기)·`accountStore`(주석)·`NcodeInfoView`·PRD TKT-02 v2.3 · 화면정의서 `p_tkt02.py`·`p_tkt05.py` 예시 키 | ✅ |
| PC-067 | 2026-09-01 | 목록 App Key 노출 · 항목별 탭 이동 | ① App Key 관리 **목록의 [App Key] 열에 키 값을 그대로** 보여 준다(미발급이면 「미발급」) ② 목록 셀을 누르면 **그 항목의 탭**으로 간다 — **[사용 서비스] → 사용 서비스 및 권한**, **[App Key] → App Key 발급**, 그 외 셀 → **계정 정보** (주소에 `?tab=svc|key` 를 실어 첫 탭을 정한다) ③ 계정 상세의 **[Key 정보] 카드는 스크롤 없이** 항목을 모두 펼쳐 보여 준다 | 담당 지시(2026-09-01) | `AccountsView`(accountHref tab·셀 onClick·KeyCard)·`app/tickets/account/[id]/page.tsx`(Suspense)·PRD TKT-01 v1.5 · TKT-02 v2.4 | ✅ |

> **PC-004·005 확정 결과 = 단일 할당 상태**: 코드는 **'할당됨(발급)' / '미발급(빈 코드)'** 두 가지로만 본다. `예약(RESERVED) vs 사용중(IN_USE)` 2분 구분은 폐기.
> **정리 완료(2026-08-03)**: 데이터 `ownership-data.json` status 제거(479건) · 파이프라인(`build_all_sources.py`·`build_ownership_data.py`) 태깅 제거 · `DashboardView` 예약 KPI·도넛 제거(→할당 규모) · `codeUsage`/`OwnershipMap`/`BrandGuide` 문구 정리 · 문서 배너/문구 정리.
> **레거시 유지(사용자 지시 "문구만 정리")**: DB `allocations.usage_status` 컬럼·`reservations` 테이블은 **구조를 남기고 문구만** 정리(신규 로직 미사용). 완전 제거 시 새 PC 항목으로 관리.

---

## 용어 기준

- **코드 좌표 = SOBP** (Section–Owner–Book–Page). **좌표가 먼저이고 1차 키**다 — 좌표가 정해지면 코드 중복은 성립하지 않는다 `PC-032`.
- **코드 종류 = 좌표의 속성** — `PDS3`(Ncode) · `PDS2`(Gcode) · `PDS4`(**S-code**, Section 44) · `OID`(index 전용) `PC-032` `PC-035`.
  - **좌표(SOBP)가 상위 개념**이라 한 **(S,O)** 안에서 Book 을 나눠 **여러 종류를 함께 쓸 수 있다** `PC-039`. 종류는 그 좌표의 **용도 표시**다. (예전의 종류 배타·🚫 영역 할당됨은 폐지)
  - **옛 `IDS`(A코드) 표기 = `OID` 와 같은 것**이다. 화면·필터에서 구분하지 않는다 `PC-035`.
- **OID = index 만 갖는 코드** — 외부 코드를 **우리 펜으로 읽으려고** 만든 방식이다 `PC-033`.
  - 우리 펜으로 OID 책을 찍으면 **코드 값이 1개만** 나온다. 총량은 **약 6만 개**.
  - 분량이 많지 않으면 **book 으로 나누지 않는다**. 업체 구분은 **S/O**, 분량이 늘어난 업체만 **book 번호(=OID index)** 로 나눈다.
  - 좌표 조회는 **SOBP 맵의 종류 [OID] 필터**, 업체별 index 목록은 **`INF-04` 관리대장**(코드 관리 정보 탭)에서 본다.
- **펜 구분 데이터 = Ncp(소리펜·NSP) / Ndp(필기펜·NWP)** — **좌표 속성**으로 함께 관리한다. 동일 page(동일 SOBP)라도 Ncp/Ndp로 **다르게 관리**.

---

## 1. 연속 페이지 부족 시 분리 정책 (확인1)

**정정**: (기존) 연속 부족 → *page 분리*(같은 book의 떨어진 페이지 사용) → **(정정) 연속 부족 → book 분리, 각 book 내 page 연속성 유지**. 같은 book의 떨어진 두 빈 구간을 **합쳐 쓰지 않는다**.

**적용 원칙**
- **무겹침(최우선)**: 신규 할당은 기존 할당과 **절대 겹치지 않는다.**
- **SO 원칙 유지**: 소유권(Section–Owner)에 따라 할당하면 다른 업체/프로젝트와 코드가 겹치지 않는다.
- **페이지 연속성 요건은 대상 애플리케이션에 따라 분기**:
  - **CasterN**: 페이지가 떨어져 있어도 무관(**연속성 불필요**).
  - **NeoStudio2**: **book 단위 관리** → book에만 종속되면 page는 떨어져도 무관.
  - **외부 업체 제공**: 해당 업체 개발물이 **떨어진 코드를 읽을 수 있는지**에 따라 발급 방식이 달라짐.

**개발 반영**
- 자동 할당 기본 = **연속 부족 → book 새로 분리**, book 내부 조각 병합 금지. (엔진 `allocationEngine.ts` 이미 구현)
- **추가 필요**: 서비스별 `pageContiguityRequired` 플래그 → CasterN 등 연속 불필요 서비스는 조각 page 활용 허용(효율↑), NeoStudio2는 book 종속 기본.

---

## 2. 고객 등급(Grade)별 할당 범위의 의미 (확인2)

**판단: 의미 있음** — "사용량 규모"에 근거.
- 폼솔루션 사용량, 편집툴(책 편집–출판사) 사용량을 대략 파악해 범위 산정.
- Owner 코드는 **고객사마다 다름**, **프로젝트마다 Owner 별도 발급이 원칙**.
- 단, **물량 작은 프로젝트는 내부적으로 NeoStudio2의 Owner 하위 book 할당으로 흡수**.
- **폼솔루션은 Owner 코드를 따로 생성해 운영**.

**정리(이원 운영)**: 대형/일반 프로젝트 = **Owner 별도 발급** · 소규모 = **NeoStudio2 Owner 하위 book으로 흡수**.

---

## 3. 자동 할당 대상 Owner & 등급별 SO 설계 (확인3)

- **대상 Owner 코드는 고정**하되, **등급마다 SO(Section–Owner)를 다르게** 할당.
- **G / N 구분 필요.**

**등급별 예시 설계**

| 등급 | 할당 규모 | SO 설정 |
|------|-----------|---------|
| a | 1,000 페이지 | SO – S 0 |
| b | 100 페이지 | SO – S 3 |

- **개발 요건**: 폼솔루션 개발 시, 할당 코드를 **등급별로 S를 둘 다 가질 수 있도록** 개발. page 규모에 따라 사용할 **OBP 배정**. → *개발팀 확인 필요.*

**"사전 예약"의 의미**
- 용도: **B2B용 / 대표님 연구용 / 소리펜 고객사용**.
- 특히 **소리펜과 팝펜이 혼용되지 않길 원하는 고객사**에 **사전 확보**하여 제공.
- **펜 혼용 관리**: 소리펜·필기펜이 **동일 SOBP**를 써도 **Ncp/Ndp로 다르게 관리**되므로 동일 page 사용 가능, 단 **구분 관리가 원칙**.

---

## 4. 고객별 코드 종류 할당 정책 (확인4)

> 코드 종류는 **좌표(SOBP)의 속성**이다 `PC-032`. 아래 N/G 기준은 **PDS3(N)·PDS2(G)** 선택 기준을 말하며, PDS4(Section 44)·OID 는 좌표로 판별한다.

**사례**
- 교원 + 구몬: **N, G 모두** 사용(소리펜·필기펜 함께).
- 소리펜 중심 고객: 주로 **G코드**.
- NeoStudio2: X노트(코디언 등) 다른 고객사라도 **우리(Neo) 코드 할당**.
- 신규 서비스: **G/N 선택은 Neo가 정함.**

**코드 종류 할당 기준**

| 구분 | 할당 코드 |
|------|-----------|
| 해외 | N 코드 (원문 N/S이나 **S코드 제외 결정**) |
| 국내 – 소리펜 | G 코드 |
| 국내 – 소리 + 필기 | N 코드 |

> 주의: **편집 신규 할당은 편집 파트를 끼고 진행**하는 것이 안전.
> **결정(2026-07-15): S코드는 NcodeCenter 범위 제외** — 취급 코드 종류는 **N/G** 두 가지.
> **정정(2026-08-28 `PC-032`): S-code 는 `PDS4`(Section 44) 로 다시 편입**해 분류·조회한다. 여기에 **OID**(인덱스 전용)를 더해 **PDS2 · PDS3 · PDS4 · OID** 4종으로 구분한다.

---

## 5. Section / Owner 공유 가능 여부 (확인5)

**답: 공유 불가.**
- **Section / Owner 는 공유하지 않는다.** 프로젝트(고객사)는 **SO로 구분**해 관리.
- book을 달리해 쓰는 것 자체는 가능하나 **관리가 어려워지므로 지양**.
- **빈 공간이 남더라도 다른 프로젝트가 같은 SO를 재활용하지 않는다.**

> 참고: 앞서 데이터에서 발견한 **공유 owner(S5/O100 등)** 는 이 원칙상 **지양 대상**(내부 재사용/명명 이슈일 가능성). 대시보드에선 정보로 표기하되, 신규 발급은 SO 미공유 원칙을 따른다.

---

## 6. 공통(커먼)코드 · 고객사 계층 · 사용 서비스 · 티켓 (프로토타입 반영, 2026-07~08)

> 프로토타입에서 raw 데이터(NSP/NWP 엑셀·커먼 시트)를 정리하며 확정한 정책. 상세 근거는 §0 Changelog PC-006~PC-017.

### 6.1 공통코드 중앙 모델 (PC-006~009)
- **코드 정본 = `(타입 k, Section, Owner)`** 중앙 레지스트리(`commonCodes.ts`). 옛 `nspCommon/nwpCommon` 불리언 플래그 폐기.
- 타입 **k = N(PDS3) · G(PDS2) · A/O(OID, 옛 IDS 표기 포함 · historyOnly)**. A(옛 IDS)는 코드 할당·편집 없이 검색/이력만 `PC-035`.
- **홀더 회사 1개 = 코드 1개**로 분리: 네오노트-27 → 네오노트-0-27 / 3-27 / 14-27 / IDS-27. 회사·프로젝트·편집·소유권이 모두 코드 단위로 일관.
- **커먼코드 판정·필터는 레지스트리 기준.** 사용 고객사가 없는 코드(예: 팝펜스티커-26)는 공유코드로 보지 않음.

### 6.2 고객사 계층 = 상위 / 하위 / 단독 (PC-013~015)
- **상위(대표) 고객사** = 공통코드를 **보유**한 대장.
- **하위 고객사** = 공통코드를 **사용**(멤버십 귀속)하는 고객사. **정식 회사 레코드**로 관리 — 담당자·사업자·주소 등 상세를 갖고, 목록에서 이름순으로 섞여 노출·검색되며, 클릭 시 회사 상세가 열린다. **하위의 SOBP/코드 내역은 코드 프로젝트·SOBP 맵에서 관리**(고객사 상세에는 코드를 넣지 않음).
- **단독 고객사** = 공통코드와 무관한 일반 고객사.
- 대표(상위) 회사는 **자기 코드의 하위가 될 수 없다** → 수정 폼에서 자기 코드 비활성화.
- 사용 고객사(cu)는 별도 회사가 없더라도 빌드가 **최소 정보 회사 레코드를 생성**해 계층에 편입(정식 고객사화).

### 6.3 사용 서비스 (PC-010~012)
- 항목 = **casterN(편집툴) · 폼솔루션 · 서비스없음(코드만 발급)** 3종. (아이글·NeoStudio2·Ncode 프린터는 **폐지** — PC-026)
- **서비스 지정은 SOBP 맵의 [직접 코드 할당]에서만** 한다(발급 지점에서 용도 지정 = 중앙집중).
- **편집 데이터로 만들어진 코드 = casterN**(자동). **[편집 프로젝트] 메뉴 = casterN 서비스 관리** 화면.
- **[코드 프로젝트]는 조회·검색 전용**(업체·서비스별). 프로젝트 등록 기능 없음.
- 외부에 코드만 발급(서비스없음)하면 사용량 모니터링 불가, 자체 서비스는 모니터링 대상.

### 6.4 티켓 발급 (PC-016)
- **N Key** = 물리 키(오프라인 편집툴, 계정 불필요). **App Key** = 계정과 연동되는 키(온라인·SDK). 좌측 메뉴로 구성.
- **정산(과금)** = 유료 / 무료 / 체험 / 미정. 발급 목록에서 티켓별 등록.
- 출처는 **대장(ledger, 과거 발급 이력) vs 신규발급**으로 구분.

### 6.5 예외·운영 제약
- **미할당 코드 예외(PC 참조)**: 편집 서비스가 미할당 코드도 쓰던 시기의 S3/O12(901플래너 스프링) — **코드는 네오노트12 소유(코드발급), 편집 실적은 네오노트-1012 귀속**. 향후 미할당 코드 사용 차단 예정.
- **프로젝트 종료 고객사**(closed) = 코드 발급 이력만 보관.
- **원본 Excel 편집 금지(PC-017)**: NWP/NSP 엑셀은 도형·이미지를 포함하므로 프로그램으로 저장 금지. 정책/데이터 변경은 **사용자가 Excel에서 직접 수정** 후 재빌드.

---

## 7. SOBP 실시간 발급 (NDP 연동) — Ncode 프린터

> 근거: 프린터 드라이버 SOBP 할당 설계(2026-08 수령). 상세 = [SOBP 할당 (NDP 연동) 참조](NcodeCenter-SOBP-Allocation.md). ※ 별도 앱 화면 없음 — 문서로만 관리.
> 상태: **인증 체인 ✅ 구현 완료 / 발급 API `/ncode/issue` 📋 설계 확정·스펙 미정.**

### 7.1 할당 경로 2가지 (배타 아님)

| 경로 | 주체 | 단위 | 상태 |
|---|---|---|---|
| **A. 관리자 할당** | 내부 직원 — `[SOBP 맵] ▸ 직접 코드 할당` | S/O/B/P **블록** | ✅ 구현 |
| **B. 실시간 서버 발급** | 서비스 클라이언트 — NDP `/ncode/issue` | **페이지** | 📋 스펙 미정 |

기본 구성 = **관리자가 A로 코드 풀(블록)을 잡아 주고, 클라이언트는 그 안에서 B로 소비**한다.

### 7.2 원칙

1. **서버 중심 할당** — 클라이언트가 SOBP를 임의 생성하지 않는다(고객사·기기 간 충돌 방지). 기존 **무겹침·SO 공유 불가** 원칙과 같은 근거.
2. **Owner = 고객사(리소스오너) 전용** — 고보안 고객은 전용 Section/Owner 범위.
3. **Book = 문서 1건(원본 버전과 1:1)** — 원본이 바뀌면 새 Book. **Page 1..N 연속 배정**(§1 연속성 원칙과 동일).
4. **등록 전 인쇄 금지** — 배경 문서 등록이 끝나기 전에는 실제 인쇄를 진행하지 않는다(미등록 Ncode 종이 = 필기 매칭 불가).
5. **재사용 금지 기본** — 원본 해시·페이지 수·크기·방향·배율이 모두 같을 때만 재사용 가능. 검토자별 출력은 검토자마다 별도 SOBP.
6. **오프라인 사전 확보 블록** — 오프라인 대비로 블록을 미리 받아 둘 수 있으나, **NcodeCenter 원장에는 '할당됨(발급)'** 으로 기록한다(예약 상태 없음 — PC-004·PC-005).
7. **용도 식별** — 이 경로로 나가는 코드는 사용 서비스 **`Ncode 프린터 (드라이버)`** 로 지정한다(PC-019).

### 7.3 실패 게이트

| 실패 지점 | 정책 |
|---|---|
| 토큰 무효 | silent refresh → 실패 시 재로그인 유도(발급 진행 안 함) |
| SOBP 발급 실패 | **인쇄 중단.** 재시도 / 관리자 코드 재고(잔여 블록) 확인 |
| 배경 문서 등록 실패 | **인쇄 중단(기본).** |
| 실프린터 출력 실패 | 등록 유지 — **같은 SOBP로 재인쇄** |
| 발급 후 미사용 SOBP | 회수(반납) 정책 **미정** → 협의 항목 |

### 7.4 미결 (협의 필요)

- `/ncode/issue` **엔드포인트 스펙**(요청/응답·블록 크기·재시도) 확정 — 개발팀
- **미사용 SOBP 회수(반납)** 정책 — 발급됐으나 인쇄되지 않은 코드의 처리
- 실시간 발급 결과의 **NcodeCenter 원장 동기화 주기·방식**(이벤트 `sobp.allocated` 수신 vs 배치)
- Ncode 프린터 전용 **Section/Owner 풀 배정 기준**(고객사별 전용 Owner 원칙 적용 범위)

---

## 핵심 원칙 요약

1. **무겹침**: 신규 할당은 기존과 절대 겹치지 않는다.
2. **연속성은 book 단위**: 연속 부족 시 book 분리, book 내 page 연속 유지(조각 병합 금지). 연속성 요건은 **서비스별 상이**(CasterN 불필요, NeoStudio2 book 종속).
3. **SO = 프로젝트/고객사 식별 축**: Section·Owner는 **프로젝트 간 공유 불가**.
4. **Owner 발급 이원화**: 프로젝트 단위 별도 발급 원칙, 소규모는 NeoStudio2 Owner 하위 book으로 흡수. 폼솔루션은 별도 Owner 운영.
5. **등급별 SO 차등**: 등급마다 S를 다르게(예: a=S0/1000p, b=S3/100p), G·N 구분.
6. **코드 종류 규칙**: 해외 N · 국내 소리펜 G · 국내 소리+필기 N. 최종 선택은 Neo 결정. (**S코드 제외**, N/G만)
7. **펜 혼용 관리**: 동일 SOBP라도 Ncp/Ndp로 분리 관리 가능(구분 관리 원칙).
8. **실시간 발급도 서버 중심**: 클라이언트 임의 생성 금지, 배경 등록 전 인쇄 금지, 재사용 금지 기본(§7).

---

## 개발 반영 매핑 (정책 → 메뉴/모듈)

| 정책 | 담당 메뉴/모듈 | 반영 상태 |
|------|----------------|-----------|
| 1 book 분리·연속성·서비스 분기 | 6 할당 · `allocationEngine.ts` | 🟡 book분리 구현 / 서비스 continuity 플래그 미구현 |
| 2 등급 범위·이원 운영 | 7 고객사 · 11 설정 | ⬜ 미구현 |
| 3 Owner 고정·등급별 SO·사전예약·펜혼용 | 2 가입승인 · 3 요청/할당 · 11 설정 | ⬜ 미구현(예약 상태만 대시보드 반영) |
| 4 코드 종류 N/G 기준 | 3 요청/할당 · 7 고객사 · 11 설정 | ⬜ 미구현(현재 product=PDS2/3만) |
| 5 SO 공유 불가 | 6 할당 · 5 예약 · 대시보드 | 🟡 대시보드 공유표기 / 발급 규칙 미구현 |
| 펜 구분 Ncp/Ndp | 3·4·6 · DB | ⬜ 미구현(DB 필드 필요) |
| 7 SOBP 실시간 발급(NDP) | SOBP 맵(사용 서비스=Ncode 프린터) · 운영정책 §7 문서 | 🟡 서비스 구분 반영 / 발급 API 연동 대기 |

**DB 반영 필요(추후)**: `allocations`/`books`에 `code_type(N/G)`, `pen_type(Ncp/Ndp)` 개념 추가, 등급별 SO 매핑 테이블, 서비스별 연속성 요건. → [DB 구조](NcodeCenter-DB.md) 갱신 시 반영.

---

## 개발팀 / 편집파트 협의 필요

- [ ] 폼솔루션 코드: 등급별로 **S 값을 둘 다** 가질 수 있게 개발 가능한지 (개발팀)
- [ ] 서비스별 **페이지 연속성 분기**: CasterN(불필요) vs NeoStudio2(book 종속) 처리 방식 확정
- [ ] **편집 신규 할당**: 편집 파트 참여 프로세스 정립
- [ ] **소규모 프로젝트 흡수 기준**: 별도 Owner 발급 vs NeoStudio2 book 할당의 임계 물량 정의
- [ ] **SOBP 실시간 발급(§7)**: `/ncode/issue` 스펙·미사용 코드 회수·원장 동기화 방식 (개발팀)

---

## 신규 화면 정책 준수 체크리스트 (템플릿)

> 새 메뉴/화면 MD를 만들 때 이 체크리스트를 복사해 "정책 반영" 섹션으로 넣고 해당 항목을 확인한다.

- [ ] **무겹침** 보장(기존 할당과 겹침 금지)
- [ ] **SO 공유 불가**(프로젝트/고객사 = SO 식별, 재활용 금지)
- [ ] **연속성**: book 분리·page 연속 기본 + 대상 서비스(CasterN/NeoStudio2/외부) 분기 고려
- [ ] **코드 종류(N/G)** 규칙(해외 N·국내 소리펜 G·국내 소리+필기 N) — S코드 제외
- [ ] **펜 구분(Ncp/Ndp)** 분리 관리
- [ ] **등급별 SO 차등** 및 **사전 예약(선점)** 의미 반영
- [ ] Owner 발급 이원화(별도 발급 vs NeoStudio2 book 흡수)
